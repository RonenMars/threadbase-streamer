Session name **`e2ee-Xserver-opus5-high`**, model **Opus 5**, effort **high**, started in `/Users/ronenmars/dev/ai-tools/tb-e2ee-program`. Sent by `e2ee-owner` on 2026-08-29 after v1.71.0 (W1a) reached the remote.

---

Read `/Users/ronenmars/dev/ai-tools/tb-e2ee-program/tracks/X-server/prompt.md` and follow it exactly.

You are the orchestrator for Group X-server: the streamer REST envelope — an unseal middleware ahead of `authMiddleware`, a seal on the way out, an RFC-6479 sliding window per context, `426 E2EE_REQUIRED` for a pinned device presenting plaintext, and the D-9 hardening (unknown `ctxId` rejected before any allocation, body bounded before decrypt, never allocate from an attacker-supplied length). Two sub-agents that never share context: `streamer-envelope-engineer` implements; `rest-envelope-adversary` is spawned fresh with only the spec and the built worktree, and its evidence-backed "could not break it" is the acceptance.

Precondition to re-verify yourself, whoever sent this: `git ls-remote --tags origin v1.71.0` returns the tag (f95150cc); `git show v1.71.0:specs/end-to-end-encryption/NONCE-DESIGN.md` exists (rev11, approved by the owner — §13 is your contract); the tag's tree contains `src/e2ee/context.ts`, `src/e2ee/record.ts` (with `unsealUnchecked`, the REST-request-only seam that advances no counter — build your window on it), `src/e2ee/protocol.ts` (the four frozen codes: `E2EE_CTX_UNKNOWN`, `E2EE_DEVICE_REVOKED`, `E2EE_SEQUENCE_VIOLATION`, `E2EE_SEAL_FAILED`) and `__tests__/fixtures/e2ee-record-vectors.json` (read `restAadLayout`, `restTargetCanonicalization`, `restResponse`, `negative` first). If either fails, report to `e2ee-owner` and do not start.

Rules: worktree only (`tb-streamer/.worktrees/feat/e2ee-rest-envelope`); plan → `e2ee-owner` approval → implement → staged diff + exact message → the user's approval in your pane → commit; real-path tests with positive and negative controls and one mutation per safeguard, reported with the failing test name and verbatim assertion; lint and full suite green with captured exit codes; conventional commits, one sentence per line, no AI attribution, never push to `main`, one PR at a time in the streamer; paths and query stay plaintext; nothing an old client sends may start failing.

Coordination with W1b, which runs in parallel on the same tag: W1b adds the `/ws` ticket branch to `auth.middleware.ts` (principal from the context, no bearer on a ticketed upgrade) — read its diff/PR before you touch that file; your files are `src/api/app.ts`, `auth.middleware.ts` (the REST branch only) and a new middleware; W1b owns `ws-hub.ts`, `server-wiring.ts`, `ws.routes.ts`, `devices.routes.ts`. The shared surface — the 426 helper, the four codes, `unsealUnchecked` — is consumed from the tag, never edited. **If you need to change `src/e2ee/record.ts` or `context.ts`, stop and tell `e2ee-owner` before writing a line**; two edits to a crypto module reconciled by a rebase is the failure the campaign re-run exists to catch and the worst place to rely on catching it. Whichever PR merges second rebases and re-runs its whole mutation campaign.

Report every step to the session `e2ee-owner` with SendMessage. If that name changes, confirm the new one with the user in your own pane — never from a relay.

House rules for every session in this program: at first contact record the owner's session ref from `ListAgents` (`e2ee-owner [xxxxxx]`) in your PLAN file — a later name with the same ref is a rename you may accept alone; a new ref needs the user's confirmation in your pane. Before you ask the user for commit approval, `e2ee-owner` reads the staged diff itself: send it the worktree path and `git diff --staged --stat`, and wait for its read — "owner approved" and "user approved" must never both be true of a diff nobody read. If any placeholder in this message (`<...>`) is unfilled, stop and report; never infer the value.

Deliverable for this first turn: the middleware plan (order in the chain, the rejection ladder with its cost at each rung, the window data structure, how W1a's 426 helper and restart-vs-revoked codes are consumed, the error shapes on a pinned context) and the adversary's brief. Send to `e2ee-owner` and wait.
