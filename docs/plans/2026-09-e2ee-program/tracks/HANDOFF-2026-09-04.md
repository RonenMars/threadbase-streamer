# Handover — 2026-09-04, ~05:15 IDT

Written for the user's return after an absence of at least sixteen hours.
Everything below was done under delegated approval; the two things that were **not** done under it are named first, because they are the ones a delegation should not have covered.

## Held closed deliberately, despite blanket approval

- **R3, the stage-2 default flip, was neither merged nor opened.** Its gate is G-2 landing, G-2 needs a packet capture, and the capture needs a `sudo` grant that never landed. Separately from the gate: "proceed without asking" is not the explicit go in your own words that the flip requires, and it closely resembles the relayed-enthusiasm case R's brief instructs it to refuse.
- **No feature-flag default was flipped**, and stage 3 appears in no diff.

## Needs you, in priority order

### 1. The hostname on the public repo — a decision, not a task

Your real tunnel hostname and machine name are live on `origin/main` of the **public** streamer repo, in 11 files of the committed program record, from #755.
The scrub there searched for keys, tokens and credentials, and a search for credentials does not catch a hostname.

They also appear in that repo's own `CLAUDE.md` and its tunnel skills, **predating this program entirely**, so this may be state you already accept.

No rewrite was performed. Three reasons: it is the default branch of a public repo and the rules forbid force-pushing there; scrubbing 11 program files would leave the hostname in the repo anyway while rewriting a signed accepted document; and whether you treat your own hostname as public is your intent to state rather than mine to infer.

The mirror script now scrubs it, so **no new commit adds another instance** — which leaves a visible seam where recent documents read `<tunnel-host>` and older ones carry the real name. That seam is the correct state until you rule.

### 2. G-2, the Android row — one sitting, and it is fully prepared

This is the only thing still gating the stage-2 flip. **One of its two blockers is now removed** (see the capture-permission note below); what remains is purely that pairing needs a human at the devices. Everything that could be done without you is done: the app is built from `c64fab5e` and installed on the Xiaomi at versionCode 63, the artefact is verified with controls and was observed running on-device.

What remains needs your hands. `tracks/D/G-PHONE-RUNBOOK.md` carries exact commands and exact taps; `tracks/D/PLAN-G2.md` carries the method. Do not re-derive either.

**The capture permission is now solved permanently — this blocker is gone.** Wireshark's `ChmodBPF` launch daemon was installed on 2026-09-04 at the user's instruction, and verified working rather than assumed:

- `/dev/bpf0`, `bpf1`, `bpf2` are all `crw-rw---- root access_bpf`.
- The user is in `access_bpf`, and it was live in an existing shell without a re-login.
- **Positive control passed**: `tcpdump -i en0 -c 3` captured 3 packets and wrote 370 bytes with no `sudo`.
- Crucially, `/dev/bpf0` had been **recreated after the install** (timestamp 08:41) and came back as `access_bpf` — which is precisely the failure that destroyed the manual grant twice on 2026-09-03, when a `chmod` covered only the nodes existing at that instant.

So the old instruction to run `sudo chgrp admin /dev/bpf* && sudo chmod g+rw /dev/bpf*` immediately before capturing is **obsolete on this machine** and should not be followed. It is retained here only so that a reader of the earlier record understands why it was there.

This is a standing widening rather than a reboot-scoped one: any process running as the user can capture packets until the daemon is removed (`sudo /Library/Application\ Support/Wireshark/ChmodBPF/ChmodBPF uninstall`, or delete `/Library/LaunchDaemons/org.wireshark.ChmodBPF.plist`). The user granted it knowingly after the trade-off was stated.

**Step 0 of the runbook is not cosmetic.** Both phones hold stale server entries pointing at rigs that no longer exist, and the app refuses a repeat pairing outright — *"This address is already in your list. Delete that server first."* The rebuilt rig will land on the same host:port, so the stale entry looks valid and the pairing still fails.

### 3. An emulator route to Phase A/B — a costed option you may decline

Phase A would reproduce the revocation storm on pre-fix JS and could **name the retrying layer**, which is C1's item 2, currently recorded as deferred with the layer unknown. It needs no packet capture and no root.

It was not run because it needs pairing, pairing needs taps, and no agent was going to drive your personal phone with synthetic input while you were unreachable.

An emulator avoids that entirely, but it is **not free**: roughly 3.3–3.7 GB on top of the 5.3 GB already spent, against the ~5.5 GB ceiling you set. There is no system image on disk (verified with a control), and it is now a rebuild rather than a download, because the SDK root, worktree and scratch rig were destroyed on the teardown instruction.

An emulator is a real Android runtime with the real record layer, so it is genuine evidence about **client behaviour** — but it is not hardware, so it says nothing about device-specific delivery shapes, which is exactly the class defect 1 belonged to. Row 1 and the canary still need the physical device either way.

### 4. Two stale processes, and a Codex session

Killed during the night: a `jest` run in the `tb-mobile` **root** checkout at 1 day 9 hours, and two orphaned `jest-worker` processes at 6 days 18 hours (`ppid 1`).

The 33-hour one was **not** orphaned — its parent is a live `codex` process (PID 89118 at the time). A Codex session had launched tests in the read-only root checkout and they hung there. Worth closing if you are not using it.

## Recorded incident — contained, no action needed

A scratch rig API key reached an agent's transcript: `npx` echoed the resolved command line, and the scripted device took the key as a positional argument.

It authenticated only against the scratch rig, had no path to `:8766`, and reached no file, evidence document or repository. It is now destroyed along with the rig.

**The remedy was verified rather than assumed, and that mattered.** The first teardown killed the rig's processes and freed its ports, and reported it as torn down — but a killed process is not a destroyed rig. The scratch `HOME` survived, holding `server.yaml` with the key, `runtime.db`, and `.threadbase/keys/server-identity.key`, the streamer's **static identity private key** — a more sensitive artefact than the one that raised the alarm, which nobody had flagged. It stood for roughly two hours after the record said it was gone.

Now genuinely gone, with a live control on each side of the deletion: the value was demonstrably findable in `server.yaml` before removal and demonstrably absent everywhere after, against a fresh control each time.

The general lesson is recorded as a method trap: **`argv` is not a private channel.** The agent printed only the key's length and verified it with a 401-versus-200 control, and it leaked anyway from a process launcher's notice line. Caller discipline does not help.

## The method finding worth more than any single row

`tracks/D/PLAN-D.md` §14 now opens with a family rather than a list of traps: **an empty result that reads as an answer.** Ten distinct mechanisms were hit in one night, every one producing that identical signature.

A `git grep -E` with `\s`, which POSIX ERE silently ignores · a linter honouring `.gitignore` for the directory the program mandates · a log nothing had connected to · a control searching for a word that was not present · a shell that does not word-split · `tcpdump` unable to open `/dev/bpf*` · a `find` predicate the platform does not support · a collector that had already died · Spotlight disabled machine-wide · a path that does not exist.

In at least three of the ten, **the positive control failed identically to the check**, which is what made the emptiness look corroborated rather than broken.

The habit sits above the list: prove a tool can return *something* in the same invocation shape before treating emptiness as an answer; make sure the control looks for something independently established as present; then separate *the tool failed* from *the path is wrong* from *the thing is genuinely absent*.

One consequence reaches back over already-merged work: **`npm run lint` is a silent no-op inside `tb-streamer/.worktrees/`**, so any streamer track in this program that reported a green lint from a worktree path did not lint. Working form: `npx biome check --vcs-enabled=false src cli __tests__`.
