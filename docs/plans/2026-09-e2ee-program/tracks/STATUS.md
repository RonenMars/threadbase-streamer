# E2EE program status

Owner: session `e2ee-owner` (run from `~/dev/ai-tools/tb-e2ee-program/`). Updated on every report; sampled every 25 minutes once wave 1 is live.

Last update: 2026-09-04 ~07:50 IDT — **PROGRAM CODE WORK COMPLETE. Four of six done-criteria met; two unmet for one reason.** Seven PRs merged across two repositories: mobile **#942** (permanent open-refusal verdict) and **#948** (Send-Escape + the client half of the ping contract); streamer **#758** (D-8 resolved as 2+4, released `v1.75.0`), **#761** (the app-level ping, released `v1.75.1`), **#763** (`date`-header flake), **#764** (ciphertext-marker flake) and **#760** (this record, 84 files). Issues closed: mobile #946, #947; streamer #756, #759, #762. Open with analysis attached: streamer #757. Every merge was verified **in the tree** with a working control rather than inferred from the merge commit. **Unmet, and honestly so: G-2 and R3.** There is no Android capture, no canary and no ciphertext claim for that platform, so the stage-2 default flip stays ungated and R3 was never opened. Both trace to one unavailable human action, not to execution. **Two nondeterministic tests were found and fixed that had nothing to do with this program** (#759, #762). Both surfaced only because the program merged often enough to roll the dice repeatedly, and both live in tests whose purpose is proving nothing leaks — so each false red read as a security regression. **R corrected its own mutation count unprompted**, from eight sibling reds to 24 of 46: the eight came from a listing it had truncated to eight lines and read as complete — the session's own failure shape turned on its own output. The corrected figure is far stronger evidence, since removing one line of sealing should break more than half the file. Close-out note written to `tracks/CLOSE-OUT-2026-09-04.md`; the merge-queue lesson was also written into the user's Claude, Codex and Cursor instruction files as dotfiles PR #86.

Earlier: 2026-09-04 ~08:45 IDT — **The capture-permission blocker is permanently removed, verified by capture rather than by inspection.** At the user's instruction Wireshark's `ChmodBPF` launch daemon was installed. Verified: `/dev/bpf0`, `bpf1` and `bpf2` are `crw-rw---- root access_bpf`; the user is in `access_bpf` and it was **live in an existing shell without a re-login**, contrary to the owner's expectation; and the **positive control passed** — `tcpdump -i en0 -c 3` captured 3 packets and wrote 370 bytes with no `sudo`. **The decisive detail is that `/dev/bpf0` had been recreated *after* the install** (timestamp 08:41) and came back as `access_bpf`. That is exactly the failure that destroyed the manual grant twice on 2026-09-03, when a `chmod` covered only the nodes existing at that instant and the kernel minted fresh `root:wheel` ones on demand. The daemon closes it; the manual command is now **obsolete on this machine** and both `HANDOFF-2026-09-04.md` and `G-PHONE-RUNBOOK.md` say so at the top, since a runbook that still instructs a superseded step is worse than one that omits it. **This removes one of G-2's two blockers, and it is honest to say it removes only one.** Capture can no longer fail on permissions — member 6 of the §14 family (`tcpdump` silently unable to open `/dev/bpf*`) is retired for this machine. What remains is that pairing needs a QR scan and a confirmation tap, and the canary is a string the user chooses seconds before sending it. G-2 still needs a human at the devices; it just can no longer fail for the reason it failed before. Recorded as a standing widening rather than a reboot-scoped one: any process running as the user can capture packets until the daemon is removed, and the removal command is in the handover so the grant stays reversible. The user accepted that after the trade-off was stated rather than assumed.

Earlier: 2026-09-04 ~05:35 IDT — **R2 is a RELEASED artefact — streamer `v1.75.0` at `ebb75a33` contains `04090873`, verified from the remote. Both remaining streamer PRs are open and building.** **#760** carries the program record (20 commits, docs-only) and **#761** carries the app-level ping emission. They were opened simultaneously because the owner's hold and P's conditional-go push crossed. **No action was taken to unwind it**: the diffs are disjoint — `docs/plans/` against `src/ws-hub.ts` and one test — so the interaction the one-PR rule protects against is not present, and drafting or closing would have discarded a CI run for nothing. **P surfaced the crossing immediately rather than letting it be discovered**, declined to draft or close on its own judgement because the action is outward-facing even though it is cheap and reversible, and offered three options with a recommendation. That is the fourth time tonight it has left an outward-facing call with the owner despite believing it knew the answer. **A gap in the program's own rule surfaced and is worth preserving:** #753 and #754 were open all night, so the invariant actually maintained is *one **human** PR open*, not one PR open. As written the rule was in continuous violation while the hazard it protects against never occurred once — recorded so nobody later reads a night of bot PRs as evidence the rule was ignored. **The night's method finding reached its final form**, and P's table states it better than the owner's prose. Three failures shared one shape — the instrument answered the question asked correctly, while the question that mattered was never put to it: the linter was asked *does biome pass?* (exit 0) and never *did it read any file?* (zero); the grep was asked *does this match?* (no hits) and never *can it match at all?* (no — `\s` is not ERE); `filter-repo` was asked *is the leaked value gone?* (yes) and never *what else changed?* (the entire ancestry). The owner's refinement is the load-bearing one: **a positive control proves the instrument *can* answer, not that you asked it the right question.** That is why all three slipped through despite controls being in use.

Earlier: 2026-09-04 ~05:20 IDT — **R2 is SHIPPED: streamer #758 merged as `04090873`. Three of four tracks are closed. And a suite failure traced back to an orchestration error of this owner's, not to any agent.** **#758's re-run went 11/11 on the identical commit**, which confirms the `§11` flake diagnosis by behaviour as well as by source. R had already closed the question with CI history rather than inference: the same test failing on run `33651939753` at `e9e6b704` — a Snyk dependency bump, **Linux Node 22**, 2026-09-02 — two days before its branch existed, verified independently by the owner. Filed as **#759**, and R takes it as its own one-line PR after P merges, citing the prior occurrence in the body so a reviewer's first question (is this a real flake or a test weakened to go green?) is answered before it is asked. R's 25-run local loop returned 0/25 and **R correctly refused to bank it**: at roughly 1–2 % per run locally, 25 runs predict zero, so a null result from an underpowered test is consistent with the diagnosis without being evidence for it. That is the exact inverse of tonight's family — a zero read as insufficient rather than as an answer. **A second suite failure appeared on P's tree and was `EADDRINUSE`**, failing at `httpServer.listen()` in `resume-enrichment-store.test.ts` — a test that opens no WebSocket and mentions neither `WSHub` nor `ping` (positive control confirms the grep works on that file), passing 3/3 in isolation. P led its four reasons with the decisive one rather than the easiest: it fails while *binding*, and `EADDRINUSE` is not a failure mode a message frame can produce. **The cause was this owner's orchestration, not P's process.** P wrote it up as its own error; it is not. Four agents were dispatched onto one machine and left to run full suites in parallel in sibling worktrees of the same repo, each standing up real loopback servers — ten concurrent vitest processes at one point. The record says what actually happened rather than accepting the generous version. P then refused to let a clean green stand on a run it knew was contended, which is the second time tonight an agent declined a true-but-worthless number. **Program-wide process note recorded:** two agents running `vitest` simultaneously in sibling worktrees of this repo intermittently fail *unrelated* tests with `EADDRINUSE`, naming a test connected to neither change — another member of the family, a failure that points confidently at the wrong thing.

Earlier: 2026-09-04 ~05:30 IDT — **Mobile #948 is MERGED, G is closed with a ten-member method catalogue, and streamer #758 has a real CI failure that is not being merged around.** **Mobile PR #948 merged** as `69083367`, 24/24 green, no rebase needed. C2's track is complete. **Issue #947 did NOT auto-close and the reason is a trap worth recording**: a squash merge takes closing keywords from the **PR body**, not from individual commit messages, and C2's `Closes #947` lived in commit 2's body which the squash discarded. Closed manually with the merge reference; R warned to check its own PR body for the same gap. **#946 and #756 correctly remain open** — the idle churn is not fixed until the streamer's ping ships. **Streamer #758 has a FAILING check — `Smoke (macos-latest)`, the `Tests` step — and is not being merged.** Linux and the Node 22/24 matrix pass; Windows smoke is still pending; so this is macOS-specific or smoke-only. Leading hypothesis given to R: its change **broadened the boot warning's trigger** so it now fires for rungs it previously did not, and a smoke test that boots a server and snapshots or greps boot output would see a new line — a genuine defect rather than a flake, and exactly the shape that ships when a broadened warning meets a test nobody thought was related. The alternative — the `release-notes.test.ts` failure both R and P hit locally and attributed to a stale `node_modules` symlink (#745) — **is testable and cuts the other way**: CI installs fresh from the lockfile, so that theory *predicts the test passes on CI*. If it is that test failing here, that is evidence **against** #745's diagnosis rather than a known-issue write-off. R was told to name the failing test from the CI log rather than reproduce locally first, because the local environment carries the stale symlink and therefore cannot distinguish the two hypotheses — and explicitly **not** to re-run CI hoping for green, since a retry that passes says nothing about whether the failure was real. **G closed with §14 now headed by the family rather than by its members.** Ten distinct mechanisms are catalogued, every one producing the identical signature: a blind `git grep -E` with `\s`; a linter honouring an ignore file for a mandated directory; a log nothing had connected to; a control searching for a word that was not there; a shell that does not word-split; `tcpdump` silently unable to open `/dev/bpf*`; a `find` predicate the platform does not support; a collector that had already died; Spotlight disabled machine-wide; and a path that does not exist. **Number 7 was the owner's own**, from a `find -newermt` run earlier tonight that returned nothing for a file visibly newer — noticed as odd and shrugged off at the time. The transferable habit now sits above the list: prove a tool can return *something* in the same invocation shape before treating emptiness as an answer; make sure the control looks for something independently established as present; then distinguish *the tool failed* from *the path is wrong* from *the thing is genuinely absent*.

Earlier: 2026-09-04 ~05:20 IDT — **CORRECTION: the stop-work was closed on a remedy that had not actually happened, and the verification I attached is what caught it.** This record stated at ~05:10 that the rigs were down and the leaked key had died with them. That was published on G's report and it was **wrong**: G had killed the rig processes and confirmed the ports free, but **a killed process is not a destroyed rig**. The scratch HOME survived, carrying `server.yaml` with the API key, `runtime.db`, and `.threadbase/keys/server-identity.key` — **the streamer's static identity private key**, a materially more sensitive artefact than the credential that triggered the alarm. Anyone could have restarted that rig. It stood for roughly two hours after this record said it was gone. **The ruling that closed the stop-work therefore rested on a false premise for that whole period**, and the only reason it surfaced is that the ruling had required the remedy be reported as a *result* rather than an assurance — the two verifications attached to it (scratch HOME confirmed gone; a controlled grep proving the value appears in nothing written) are what forced the check that found the gap. That is the clearest demonstration this program has produced of why "report it as a measurement" is not pedantry. **Now genuinely complete, with a live control on each side of the deletion:** the key value was **findable in `server.yaml` before deletion** (count 1, proving the method could see it) and simultaneously 0 in `tracks/`, 0 in the scratchpad, 0 in the streamer worktrees; after deletion, 0 everywhere against a **fresh** control. Scratch HOME, config dir and `server-identity.key` all confirmed gone. Rig logs scanned before deletion against a control of 4,339 readable lines: 0 pair tokens, 0 bearer headers, 0 private-key markers. Recorded as a trade rather than an oversight: destroying that directory also destroyed the previous session's manifests, rig logs and generator (308 MB). The results survive in `g-row1-large-payload.md` and `g1-continuation-frames.md`; what is gone is the ability to re-derive them without re-running the rig — the correct trade when the same directory held private key material. **EMULATOR DECLINED, with the number.** G enumerated before downloading, with controls: `~/.android/avd` holds only a `.DS_Store`, there is no system-images tree (control: the same `find` pattern does locate `~/Library/Android`, so the empty result is real), and no emulator binary. The owner's guess that an image "may well be on disk already" was a hope, and it was tested rather than inherited. Cost is **3.3–3.7 GB on top of the 5.3 already spent**, against a ceiling the user set at ~5.5 GB — and it is no longer a download but a rebuild, since the SDK root, worktree and scratch HOME were all destroyed on the owner's own teardown instruction. Declined on four grounds: it is far past what the user authorised and they are unreachable; it means re-standing everything just torn down; C1's item 2 is already a legitimate deferred state rather than a hole; and the user returns within a day and can run Phase A/B on the physical device, which is strictly better evidence for the same effort. It goes to the handover as a **costed option**, with G's framing that an emulator is genuine evidence about client behaviour but says nothing about device-specific delivery shapes — the class defect 1 belonged to.

Earlier: 2026-09-04 ~05:10 IDT — **G's track is CLOSED and stood down; both PRs are open with CI running; one finding needs the user and is deliberately not being acted on.** **FOR THE USER — the real tunnel hostname and machine name are live on `origin/main` of the PUBLIC streamer repo**, in 11 files of the committed program record, from #755. The scrub there looked for keys, tokens and credentials, and **a scan for credentials does not catch a hostname**. **Ruled: no history rewrite, escalate instead.** Three reasons in order of weight — it is the default branch of a public repo and the rules forbid force-pushing there, with no orchestrator-judgement exception; the same identifiers already sit in the repo's own `CLAUDE.md` and its tunnel skills *predating this program*, so scrubbing 11 program files would leave the hostname in the repo and cost a rewrite of a signed accepted document for no reduction in exposure; and whether the user treats it as public is a fact about their intent that cannot be derived from the filesystem. The mirror script already scrubs it, so **no new commit adds another instance** — which leaves a visible seam where recent documents read `<tunnel-host>` and older ones carry the real name. That seam is the correct state until the user rules, and is better than papering over it. **G closed all five items.** App built from `c64fab5e` and installed on the Xiaomi at versionCode 63 while the device was attached, artefact verified with controls (added lines 1/1, removed 0/0) and then *launched and read from the device's own log* — `fabric:true`, boot line, 14 `ReactNativeJS` lines, zero fatals — which retires by measurement the risk used to reject the old APK. **G-2 written up as prepared-and-unrun with "no Android ciphertext claim, the stage-2 flip is not cleared" stated flatly.** Rigs down. Relay built, validated end-to-end with real crypto and no human, and left unused, with its controls *inside the same capture* (sealed markers 0, and a 608-byte plaintext body in the same stream proving the sweep reads plaintext where it exists). **Teardown reported as a comparison, not a claim:** 9.9 GB removed (5.4 GB SDK/NDK, 4.5 GB worktree); `~/.gradle` preserved and its delta **measured** — 1,369 files, 75.6 MB, only four in `caches/modules-2`, which is why `du -sh` reads 6.6 G before and after. The Homebrew command-line tools were kept, argued in the record, with the removal command written down so the choice stays the user's. **The fifth member of tonight's family.** G's own end-of-session scrub first returned all zeros and was wrong: zsh does not word-split an unquoted variable, so the file list collapsed into one bogus filename and every check returned a clean zero — caught only because the control ("this must find the word Android") also returned zero. That is now five distinct mechanisms producing one signature: a blind `git grep -E` with `\s`; a linter honouring an ignore file for a mandated path; a log nothing had ever connected to; a control searching for a word that was not there; and a shell that does not split. **An empty result that reads as an answer** is the family name, and it will have a sixth member. **Phase A/B did not run**, because it needs pairing, pairing needs taps, and G declined twice to drive the user's personal phone with synthetic input while they were away. The two-Metro recipe is written down and is one sitting's work.

Earlier: 2026-09-04 ~05:00 IDT — **R has shipped, P and C2 are approved, and this record carried a dead claim next to its own retraction.** **Streamer PR #758 is OPEN** (`55a7f090` on `d9148f25`), implementing R2's 2+4 decision, CI running. R's lint re-check under P's working invocation returned "Checked 481 files" with four errors, all in files this branch does not touch and none naming its three — so its earlier green was real, but, in R's own words, it *"had not proven the instrument saw them until now"*. That distinction is the whole lesson of the lint finding. **A correction to this record, marked inline above rather than only appended.** P disproved its own `E2EE_SEAL_FAILED` claim at gate 2; the owner then restated the dead version back to P at gate 3 as "the best part of the report", having read the correction. The superseded text was already committed to the public branch, so it sat beside its own retraction with nothing marking the first as dead — a reader stopping at the first mention would take the wrong mechanism away. It is now struck through in place. **P's sequencing constraint was correct and the owner's instruction was not runnable.** `rebase.autoStash` is off deliberately, so a rebase refuses over a dirty tree — that refusal is the setting working, not an obstacle. Order is now approve → commit → R merges → rebase → re-run all six mutations and the full suite → report anything that moves → push, with **re-approval required if the rebase changes a single line**. The stash alternative P named was declined on P's own reasoning: churn through a stash to satisfy an ordering preference, when a commit will do. **P made the frozen wire contract a build failure rather than a discipline** — annotating the literal as `WSMessage` instead of passing it to `JSON.stringify` (which accepts anything) means both forbidden edits fail `tsc` with `TS2353` naming the declared type, verified in each direction. **And the traffic-analysis answer came back stronger than the question:** nothing is newly revealed, because `client.ping()` has put a WebSocket *control* frame on this same 30 s timer all along, and a control frame is as visible on the wire as a data frame. The change makes that two records where there was one; it creates neither the period, the predictability, nor the liveness signal. P explicitly does **not** claim the channel resists traffic analysis generally, since sealed terminal output still varies with what the agent prints — which is what makes the narrow claim believable. **C2 approved on two commits**, 11 files, +110/−7, all suites green. Defect 5 landed as the authorised analysis rather than a test, on environmental grounds rather than preference: no simulator devices despite the iOS 26.5 runtime, no fresh Release build (only two from unrelated worktrees — the stale-bundle trap), and `:8081` belonging to another session. Its residual risk is named in the words *"reasoned, not observed"*, and it carries a recipe with its own negative control so the next person executes rather than re-derives.

Earlier: 2026-09-04 ~03:35 IDT — **A correction to this record, a program-wide false green, and a contained stop-work.** **CORRECTION — the lesson published at ~02:58 was wrong in its mechanism and is retracted here.** This record stated *"a log file that exists is not a log file that is recording"* after `metro-8081.log` was found holding 735 bytes. G re-tested by launching the app against that same Metro and the file grew immediately, carrying exactly the wanted content. **Metro was recording.** The file was empty because no client had ever connected to it. The published lesson would have sent the next session to replace a working tool. The corrected rule, which is firmer rather than weaker: **before grepping any log for the ABSENCE of a behaviour, establish that the subject was connected to that log during the window** — a byte count plus a positive control — because a log that never saw its subject is byte-for-byte indistinguishable from the behaviour not happening. The false-negative risk to C1 was real either way; only the cause was wrong. `adb logcat` remains the better Android source on its own merits (OS-level timestamps, no taps, not contingent on a bundler being attached), but Metro is valid rather than broken. **PROGRAM-WIDE FALSE GREEN — `npm run lint` is a silent no-op inside `tb-streamer/.worktrees/`.** `biome.json` sets `vcs.useIgnoreFile: true` and `.gitignore` lists `.worktrees/`, so biome ignores every file in the worktree this program **mandates** for streamer work, prints "Checked 0 files" and exits 0. The instrument was blind in exactly the place the rules require people to stand, and it does not error — it agrees with you. P proved it with an injected formatting error and a clean revert rather than reasoning from the config. Working form: `npx biome check --vcs-enabled=false src cli __tests__` (481 files). **Any streamer track in this program that reported a green lint from a worktree path did not lint**, which reaches back over several already-merged PRs; that is the honest scope. R was sent back to re-verify before committing. **STOP-WORK, ASSESSED AND CONTAINED.** G reported per §6 that an API key reached its transcript: `npx` echoed the full resolved command line, and the previous session's scripted device takes the key as a positional argument. It is the **scratch rig's** key, authenticates only against `:8790`, has no reach to `:8766`, and touched no file, evidence document or repository. Ruled: teardown stands as the remedy rather than rotation, because destroying the rig removes what the key opens rather than merely the key; two verifications were added so "teardown is the remedy" becomes a fact rather than a promise — the scratch HOME confirmed gone, and a controlled grep proving the value appears in nothing written. The user was **not** woken: the exposure is not live, not widening and not theirs to contain, and it goes into the handover as a recorded incident in G's own words. The general trap is worth more than the incident and is recorded as such: **`argv` is not a private channel**, a secret passed there will be logged by something you did not write, and the caller's discipline is irrelevant — G printed only the key's length and verified with a 401-versus-200 control, and it leaked anyway from a process launcher's notice line.

Earlier: 2026-09-04 ~03:05 IDT — **A silent grep failure was traced to its mechanism, and the interface hazard turned out to be worse than recorded.** **P found why a search this owner ran returned nothing:** `git grep -E` is POSIX ERE and does not support `\s`, so `type:\s*"ping"` matched nothing — *including its own positive control*, which is what makes the failure look like an answer. The owner had reached the right conclusion ("my grep form is blind") for the wrong reason, and a right conclusion for the wrong reason is precisely how a method note ends up teaching the wrong lesson, as a retracted trap already demonstrated this session. Recorded as §14 trap 8: use `[[:space:]]` or `-F`, and distrust the zero of any `\s`-bearing search in this program's history. **G's §14 trap 7 is the strongest method finding of the session.** The rig does not merely sit on an ambiguous subnet — it **chooses** its interface by `os.networkInterfaces()` enumeration order (`src/lan-url.ts::resolveServerUrl` returns `publicUrl` if set, else the first non-internal IPv4), and the candidate list is **four**, not two: `en0` 192.168.68.125, `en9` 192.168.68.102, a link-local `en12`, and a **Tailscale `utun16`**. Nothing pins the winner, so a reboot, a Wi-Fi drop or a VPN coming up silently hands the pair URL a different address, the device talks over an interface nobody is capturing, and `tcpdump` on the remembered one writes an empty file the sweep reports as "no plaintext". It is the best of the seven **because it is invisible while it is correct** — `en0` is right today, so nobody would have questioned `G-PHONE-RUNBOOK.md`, and the trap only fires on the day something unrelated changes, which is the day someone is standing at a phone under time pressure. The owner pushed it further: the runbook is corrected now rather than later so it cannot be followed wrongly by someone who never reads §14, and the Tailscale case gets its own sentence — a `utun` winning the enumeration is a **scope violation**, not a missed capture, because the LAN-only bound every ciphertext claim depends on would no longer hold. **P's counter analysis retired concern 1 as structurally void rather than merely small:** there is no rekey on `origin/main` to perturb, because NONCE-DESIGN §6 abolished in-place rekey on the user's own 2026-08-29 ruling and the code matches — three comments asserting absence, zero implementation. The only threshold is §7 exhaustion at 2⁶⁴−1, against 2,880 counters a day. **§4's adversarial verifier is therefore NOT triggered**, stated with its reasoning rather than assumed: the diff is confined to `ws-hub.ts`, and the change adds traffic *through* the record layer rather than changing it.

Earlier: 2026-09-04 ~02:58 IDT — **G caught a hollow instrument that would have retired C1's item 2 on an absence meaning nothing.** A previous G session reported that `scratchpad/logs/metro-8081.log` "doubles as C1's client log", and the owner relayed that to C1 as fact. It is **735 bytes** — the Metro startup banner and nothing else, unchanged for sixteen hours: no bundle requests, no client console output, no `platform=android` line. Grepping it for retry cadence returns nothing, and nothing reads as *"the storm did not reproduce"*, which is the §14 false-negative shape exactly. Replacement is `adb logcat`, verified with a non-zero positive control (202 lines) and strictly better than Metro: no taps, and every line timestamped at OS level. The §14 entry this earns is that **a log file that exists is not a log file that is recording**, and the check is a byte count plus a positive control rather than the file's presence. **G also found an ordering constraint nobody had stated:** C1's merged fix is what stops the storm, so reproducing the storm requires **pre-fix** JS. One native shell serves both phases because a dev client loads JS from whichever Metro it is pointed at — Phase A on `26815a16` (pre-fix, storm reproducible, logcat plus the rig's HTTP log, **no root needed**), Phase B on `c64fab5e` (post-fix, row 1 plus the canary, under capture). Taken deliberately as a bonus: **A-then-B is a hardware negative control on C1's own fix** — storm present pre-fix, absent post-fix, one variable, same device and rig. C1's fix merged tonight with no device evidence behind it, so this is the cheapest strong result in the plan. **NDK approved on firmer accounting than before:** it is **six** modules declaring `externalNativeBuild`, not five (`@sentry/react-native` is the extra), none ship prebuilt `.so`, and no reusable CI artefact exists — `e2e.yml` builds `assembleRelease -PreactNativeArchitectures=x86_64`, the emulator arch rather than the device's `arm64-v8a`, and never uploads it. The SDK goes to a scratch `sdk_root`, so teardown is one `rm -rf` rather than picking components out of a shared prefix. **A no-root fallback is authorised as a hedge but not preferred:** a logging TCP relay, built and self-validated against the sealed rig with G's own scripted device and then left unused. The grant observes the LAN segment that every other ciphertext claim in D2 rests on; the relay is a relay hop, proving what leaves the device rather than what crosses the segment, with the compensating strength of 100 % of payload both directions and no dissection gap. It exists because the grant has failed to land twice and the next session should not discover a third failure with the user standing at the phone.

Earlier: 2026-09-04 ~02:52 IDT — **The user delegated approval and left for at least sixteen hours, so the program is now running to a close-out rather than to a finish.** Planning assumes the longer reading of that gap, because every choice that differs between sixteen hours and sixteen days is cheaper to get wrong conservatively. **Two things are explicitly held closed despite the delegation, recorded here so a later reader can hold the decision to account:** R3 is neither merged nor even **opened** — its gate is G-2 landing, G-2 needs a packet capture, and the capture needs a `sudo` grant on `/dev/bpf*` that will not arrive; and no feature-flag default flips. A blanket "proceed without asking" is not the explicit go in the user's own words that the stage-2 flip requires, and it is close to the "owner relays enthusiasm" case R's brief instructs it to refuse. **G-2 is blocked on capture and will be written up as prepared-but-unrun**, which is a scheduling fact rather than a failure: the device is identified (`eb57e2b6`, Xiaomi 11 Lite 5G NE, the same family mobile #727 was reported on), the method is settled, the stale-§5 trap is recorded, and the interface question is resolved to `en0` pending its own positive control. G builds and installs the Android artefact **while the device is still attached** — the time-critical step — then tears the rigs down as a comparison against the start-of-session record, because an idle scratch rig held for days is a liability and PLAN-D §§3–4 makes the rebuild about ten minutes. **A cross-agent contract risk was caught before it landed.** C2 had already staged `{ type: 'ping'; ts: number }` into the mobile `WSMessage` union with a test pinning it, while P was about to design that same frame in the streamer — two agents, two repos, one wire contract, neither aware of the other. The shape is frozen at what `src/types.ts:339` already declares, and the 30 s cadence is now load-bearing on both sides. That is the same mismatch class that cost this program a day, since defect 1 was React Native delivering `ArrayBuffer` where `Uint8Array` was expected on this very receive path. **Both halves of #946 now ship without any device row behind them**, so P was told that is a reason to raise the bar precisely where hardware would have caught it: the measured defect (3.1 context opens per minute against a limit of 5, on an idle session) was found on a device and nothing in the suite would have surfaced it.

Earlier: 2026-09-04 ~02:40 IDT — **C1's item 1 is MERGED, R2 is DECIDED, and R and C2 are re-dispatched on Fable 5.1.** Mobile **#942 merged as `c64fab5e`** on `origin/main`, squashed, branch deleted. It was rebased first: `main` had moved to `59a4d04f` while the PR sat, so the branch was rebased onto it and force-pushed with lease, and the diff came through byte-identical (641 insertions, 1 deletion, the same six files). CI re-ran on the rebased head and returned **23/23 pass, zero failures**. **Disclosed rather than glossed:** the five mutations were *not* re-run locally after the rebase. The sub-agents did not survive an overnight session resume, so C1 was gone by then; the two commits `main` gained were an Android version bump and a FAB tint fix with **zero file overlap** with C1's change, and CI re-ran the full suite on the rebased head. That is weaker than a local mutation re-run and is recorded as such rather than claimed as equivalent. **R2 decided by the user**, in their own words: *"hand-off to proceed with 2+4"* — option 2 (retire D-8's mechanism rule, documentation only, `src/feature-flags.ts` untouched) plus a new **option 4** (source-agnostic consequence rule: a boot serving plaintext announces itself, naming the pinned count and the deciding source). **Option 5 was considered and dropped.** **Option 4 was not on R's list.** It came out of a brainstorm that turned on a mobile-client fact: both channels decide sealed-vs-plaintext from client-held state only (`authed-fetch.ts:177`, `ws-client.ts:190`, `isPinned = requireEncryption && serverPublicKey && id`), nothing consults what the server currently claims, and the only path that clears `requireEncryption` anywhere in the app is a deliberate user tap at `ServerEncryptionSection.tsx:49`. So a server that disables E2EE does not silently downgrade its pinned devices — it breaks them loudly. The residual harms are first-contact pairing and operator self-deception, not downgrade, which makes D-8 a mechanism rule guarding a harm the mechanism largely cannot cause. **Model change at the user's request:** R and C2 now run on **Fable 5.1**, as a bounded trial on well-specified work; the orchestrator seat stays on Opus 5, because the remaining risk is concentrated in G-2 — the row that gates the stage-2 flip and already carries three logged false-pass traps.

Earlier: 2026-09-02 ~20:52 IDT — **C1's item 1 is built, green, mutation-proven and approved by the user; the PR is opening.** 584 insertions of which **90 are production code** across three files: `openContext` is renamed `runOpenHandshake` with its body untouched and re-wrapped by a new `openContext` holding a `Map<serverId, {error, serverPublicKey}>` — enforce before any network call, record only a non-retryable `OpenError`, clear on success and on a differing pin — plus one `clearOpenRefusal(id)` in `stores/servers.ts` `addServer` and one loop in `hooks/useSession.ts` `retryFailed`. `mapOpenFailure` is untouched, because the `429` → `E2EE_TRANSIENT` mapping is correct in itself and the bug was that it could overwrite a verdict already reached. `ws-client.forceReconnect` is deliberately untouched, because it fires on every foreground **and** on the 45 s silence timer, so clearing there would rebuild the storm. Gates: unit **1848/1848**, integration **472/472**, e2e **59/59**, `tsc` 0, `eslint --max-warnings=0` 0. **All five mutations seen red**, each turning exactly one test red, each reverted with the baseline re-confirmed. **The owner reviewed the diff rather than the summary** and verified two claims against `origin/main`: the `_inFlightCount` test-helper convention is real at `context.ts:424`, and `mapOpenFailure` genuinely has no `E2EE_CTX_UNKNOWN` branch. **C1's positive control caught an error in its own brief.** The dispatch instruction named `E2EE_CTX_UNKNOWN` as a retryable `/open` refusal; it is not, and never was — a 409 at `/open` falls through to `E2EE_HANDSHAKE_FAILED`, and the code is produced in exactly one place, `authed-fetch.ts:383`, for a sealed request whose `ctxId` the server forgot. C1 corrected its test to a 5xx and added a second test pinning the 409 reading so a later edit cannot quietly make `/open` retryable on it. **Two disclosures that raised confidence rather than lowering it:** mutation M2 first came back *green* because the race test asserted on the error code and the mutated build re-earned an identical one — C1 re-cut the assertion onto requests reaching the server, which is what a storm physically consists of, and it went red; and C1 reverted M1 with `git checkout --`, wiping its own uncommitted fix, noticed within one step when the next patch anchor failed, restored, switched to file-backup restores, and re-measured every number against a re-confirmed baseline. **Unplanned case closed:** D2 row 9's Access gate answers `/open` with an HTML redirect, which falls through to `E2EE_HANDSHAKE_FAILED` and is therefore now remembered — the row-9 note that it "compounds with the retry defect… retry forever" no longer holds.

Earlier: 2026-09-02 ~20:35 IDT — **The rig is up and isolated, and G-1's premise turned out to be unsatisfiable, so the gate is split.** Rig pinned `@threadbase-sh/streamer@1.74.0` (`dist/cli.cjs` sha256 `e106cb52…`) in a scratch npm prefix inside the scratch HOME: sealed rig on **:8790** (`spk=…&v=1` in the pair URL), legacy control on **:8791** (no `spk`, `/api/info` reports `e2ee.enabled:false`). Isolation was verified **with a positive control on the check itself** — both rig PIDs show 0 open files under the real `~/.threadbase` while production PID 13943 shows 11, so the zero is a result rather than a broken grep. PLAN-D's `spawn-helper` trap was confirmed (both prebuilds shipped `0644`) and pre-empted before boot; a session spawned cleanly. The agent sits at the first-run login screen, unauthenticated, as PLAN-D requires. The rig advertises **192.168.68.125**, resolving finding 1 to **en0**, still to be confirmed by the control rather than assumed. **The finding: this server cannot emit a WebSocket continuation frame at all.** `src/ws-hub.ts:299` is the single server→client send path and passes no options; `ws` resolves to **8.21.3**, whose `send()` hardcodes `fin: true` at `lib/websocket.js:477`. **Independently verified by the owner** with a positive control on the same grep invocation. So G-1 as written is not a hard row but an unsatisfiable one — no payload size produces an opcode-0 frame, and the gate would either never close or close on an absence that means nothing. **G-1 is therefore split, explicitly changed rather than reinterpreted:** (1) G still pushes the large payload and reports the observed opcode breakdown with the coverage figure, because a dependency read says what the code should do while a capture says what the wire did, and this program exists because those have diverged before — **that clears R2**; (2) a client-side test that deliberately hands the RN WebSocket layer a fragmented sealed message and asserts it unseals, which is the only strong evidence about the client and is **mobile work — routed to C2 as its defect 5, gating the stage-2 flip rather than R2**. Two caveats carried into the addendum in G's words: an intermediary can re-fragment and the Cloudflare tunnel is exactly one on the production remote path, but that leg is TLS so `tcpdump` can neither confirm nor deny it; and `fin: true` is a fact about `ws` 8.21.3 — a pin, not a permanent property, so a dependency bump could reintroduce fragmentation with no code change in either repo.

Earlier: 2026-09-02 ~20:24 IDT — **C1's reproduction is RED, observed against unmodified `origin/main` rather than argued for, and the verdict's scope is ruled.** C1 found the single production chokepoint — `services/e2ee/context.ts::openContext`, through which both consumers funnel (`ws-client.ts:200` → `openContextOnce`, and `sealedFetch` → `acquireRestContext` → `rest-session.ts` opener) — so a verdict consulted at its top, before `createOpenInitiator` and before any fetch, closes every candidate retry layer at once **including the client-log shipper** if that is what G's log names. That is item 1's caller-independence, made concrete. **The negative control exceeded its requirement**: rather than needing a test-only disable flag (a stubbed seam on exactly the transition under test, which §4 forbids), C1 ran twelve sequential attempts against one `serverId` on unmodified `26815a16` and *executed* the storm — 12 requests reached the server, and the caller-visible text degraded from "This device is not paired for encryption" to "The server is busy; retrying shortly" at the 429 boundary. The storm and the laundering are both reproduced in a unit test with the real `mapOpenFailure` and the real classifier on the production path. **Owner ruling on the one judgement call**: the verdict stays **uniform across all non-retryable codes** and is *not* narrowed to `E2EE_DEVICE_REVOKED`. The decisive fact is that `E2EE_HANDSHAKE_FAILED` is the code behind the *unprompted field storm* (168 × 400 and 43 × 429 over six minutes from an untouched device), so narrowing would fix the case produced in a lab and miss the case that happened on its own. Accepted cost, surfaced by C1 rather than buried: the mispinned-device case stops self-healing and needs one Retry tap — carried by the fleet argument, since through a tunnel every device collapses to `127.0.0.1` and shares one budget, making a self-healing storm a cost paid by every other device on that server. **R** completed all three requested additions to `PLAN-R2.md` and is still correctly holding presentation pending an explicit G-1 confirmation; nothing opened in the streamer and the slot stays free.

Earlier: 2026-09-02 ~20:12 IDT — **G-2 is unparked: the user has an Android device, chose a local SDK install, and is clearing the phone entries themselves.** Route is a local Android SDK, explicitly **not** EAS, so no `eas login` is needed. The user attached a condition in their own words — *"remember to clean up the build files after the tests"* — which is now a teardown obligation rather than a courtesy: G extends its start-of-session record with the pre-install state of `~/Library/Android`, `~/.gradle` and `~/.android` **before** installing anything, installs the smallest set that builds (command-line tools plus the platform and build-tools the project actually pins, read from `android/build.gradle` on `origin/main`, no emulator images since real hardware is present), and reports teardown as a comparison against that record rather than as a claim. Anything materially bigger stops and comes back to the user, who authorised an SDK rather than an open-ended download. G must verify the installed artefact actually contains `443771a8` before capturing a single Android packet — an Android build that cannot unseal a WS frame produces precisely the empty-looking pass finding 1 warns about, which would be the third such trap in one day. The device model and OS version are to be discovered with `adb devices` and recorded, since G-2's whole value is covering a platform D2 never touched. Sequencing: G-1 first on the cabled 13 Pro because it is unblocked and it clears R2, with the phone trip batched — G-1's taps and the revocation-storm client-log capture for C1 in one visit, Android in a second once the build exists.

Earlier: 2026-09-02 ~20:05 IDT — **G's arrival report changes the Android plan, and R2's escalation is drafted and held.** G re-verified every pin, recorded the start-of-session state to `scratchpad/start-of-session-2026-09-02-G.txt`, and returned four findings, two of which would each have produced a *false pass* rather than a failure. **Finding 1:** `en0` (192.168.68.125) and `en9` (192.168.68.102) are both on `192.168.68.0/24`, so capturing the wrong interface writes an empty pcap, the sweep returns "no plaintext", and the row reads clean — mobile #727's own field report used the `en9` address, so this is not hypothetical. No interface is accepted until a positive control proves bytes flow on it. **Finding 2:** `PLAN-D` §5's Android note is **stale** — mobile **#727 is CLOSED** (fixed 2026-08-15 by `13e21e22`, cleartext permitted to the local network and refused elsewhere), so Android reaches the rig over plain LAN http and is capturable. Following §5 would have run G-2 over the tunnel's TLS, where the canary is absent *because of TLS rather than because of E2EE* — a row certifying nothing while reading as a pass. **Finding 3 (blocker, independently verified by the owner):** `443771a8` (#940, the ArrayBuffer receive-path fix) is an ancestor of `ios-v215` but of **no** Android tag, including `android-v62` — which was cut at 01:30 today, seventeen hours before the fix merged at 18:47. The discriminating control passes (`ios-v214` correctly shows missing; `origin/main` contains it). No Android build in existence can unseal a sealed WebSocket frame, so row 1 cannot run on one, and the Android SDK has been deleted from this machine (`~/Library/Android/sdk` absent, `adb` fork-server holding a deleted binary, `eas` present but logged out). **Finding 4:** Spotlight indexing is disabled machine-wide, so `mdfind` returns empty for every query — G caught it with a positive control rather than reading the empty result as an answer. **G-1 is unblocked and proceeding** on the cabled iPhone 13 Pro (owner's call — a second device model is a bonus against D2's single-model limitation). **G-2 is parked** pending the user's Android decision; no EAS build started and no SDK installed. **R** drafted `tracks/R/PLAN-R2.md` and is correctly holding presentation: it verified the three D-8 options against `origin/main` rather than against the brief, and found that the boot warning at `src/server.ts:2404` fires **only** for CLI-sourced disablement, so an env- or yaml-disabled run is silent today — the invisible off switch is already present, and the stage-2 flip makes it consequential rather than creating it.

Earlier: 2026-09-02 ~19:52 IDT — **The finish plan is dispatched: G, C1 and R are live; C2 is held.** Every precondition was re-verified from the remote rather than from the briefs. Streamer `origin/main` = `d9148f25` (#755), latest tag **`v1.74.0`** = `67f2d05e` — R1's `--no-e2ee` is shipped and closed. Mobile `origin/main` has moved past what STATUS recorded: **`26815a16`** (`chore(ios): bump build number to 215 [skip-ci]`, #941, tag `ios-v215`); both groups are pinned to that, not to the older `3684727b` the root checkout still holds. **Both PR slots are free**: the streamer carries only #753 (dependabot) and #754 (Snyk, opened under the user's login) and mobile only #879 (dependabot); per the program rules bot PRs do not hold a slot and none of the three were touched. **The rig is confirmed down** — nothing is listening on :8790/:8791/:8081 and no simulator is booted; the one Threadbase server running is the user's production menubar instance on `:8766` (launchd, `~/.threadbase`), which every agent is instructed to leave alone. The docs branch `docs/e2ee-program-record-updates` already exists on the remote at `d0ab262d` with a clean worktree one commit ahead of `origin/main`, so the first milestone had somewhere to land. **C2 is deliberately not started** — it shares the mobile PR slot with C1 and an early branch only rots behind a rebase.

Earlier: 2026-09-02 ~19:42 IDT — **The plan set now carries a documentation cadence.** At the user's instruction the record is committed after every milestone rather than at the end. Mechanism: the workspace is not a git repository, so the durable copy stays `docs/plans/2026-09-e2ee-program/` in the streamer, and the orchestrator mirrors changed markdown onto a long-lived branch `docs/e2ee-program-record-updates`, scrubs it (that repo is **public**), commits with a message naming the milestone, and pushes — pushing a branch is not opening a PR, so it never contends for R's streamer slot. **One writer**: sub-agents keep their own track documents current and report; only the orchestrator commits, which avoids the two-writers hazard the program has a stop-work trigger for. Milestones are enumerated per group in `PLAN.md` (rig up with the control's coverage known, each row captured and swept, each fix merged, the escalation drafted, the decision recorded, R3 opened) and each agent's task file now lists the documents it must keep current, since the orchestrator can only commit what exists.

Earlier: 2026-09-02 ~19:35 IDT — **The remaining work is split into four parallel groups**, in `tracks/finish-2026-09-02/`: `PLAN.md` (groups, dependency graph, sync points, PR-slot ordering), `ORCHESTRATOR-PROMPT.md` (Opus 5 / high; coordinates, does not implement), `KICKOFF.md` (the message the user sends to start the orchestrator, plus the four the orchestrator sends onward), and one task file per group. **G** (Opus 5 / high) owns the single rig session — G-1 continuation frames, G-2 Android, and the client-log capture C1 needs. **C1** (Opus 5 / high) owns the retry loop; its item 1, a sticky per-server permanent verdict a later 429 cannot reset, is caller-independent and starts immediately. **C2** (Sonnet 5 / medium) owns the 45 s silence timer and the pending-prompt guard, and waits for C1's PR because they share the mobile slot. **R** (Sonnet 5 / medium) owns the D-8 escalation (present three options, decide nothing) and the stage-2 flip PR (open, never merge). Serialisation is by PR slot and by hardware, not by task count: G ∥ C1 ∥ R start together, C2 follows C1, R2 waits on G-1, R3 waits on G-2 plus export compliance plus the user's explicit go.

Earlier: 2026-09-02 ~19:25 IDT — **The program record is committed to the streamer and merged.** PR **#755** merged `d9148f252742` (10/10 green, including the umbrella doc-links check): 65 files under `docs/plans/2026-09-e2ee-program/`, mirroring the 2026-08 prompt-contract program's shape — `README.md`, `WORKSPACE-CLAUDE.md`, and the whole `tracks/` tree with STATUS, every group's prompts and plans, `D2-REPORT.md` and the evidence write-ups. Raw captures, decoded payloads, screenshots and logs were deliberately excluded, as that program's rule requires; the only non-markdown file is `d2-timings.ts`, the measuring instrument. **`threadbase-streamer` is a PUBLIC repository**, so the Cloudflare account id and the Zero Trust team domain were scrubbed to placeholders before the commit; a full scan for keys, tokens and credentials across all 65 files came back clean, and every other secret-pattern hit was the identifier `deviceToken` in prose. The next-session brief is `tracks/PROMPT-FINISH-E2EE-2026-09-02.md` (recommended: Opus 5 at high effort, because the remaining risk is methodological rather than mechanical). Two bot PRs are open on the streamer — #753 dependabot and #754 Snyk (`hono` 4.13.3→4.13.5, a row on issue #745) — untouched, per the no-replying-to-bots rule.

Earlier: 2026-09-02 ~19:15 IDT — **D2 IS ACCEPTED AND CLOSED.** The reviewer re-derived every number from the pcaps and confirmed the revision: the coverage gap, the full-payload sweep (379 483 B, zero canary, zero markers, zero screen strings), the `d2-demo` correction, the row 5 write-up, the 429 disclosure and the restored 17 Pro storm. Acceptance is stamped in `tracks/D/D2-REPORT.md`. **TWO NAMED GATES now stand between here and rollout, recorded so R2 cannot start without them:** **(G-1) continuation frames** — no capture contains a WebSocket continuation frame, so client-side fragmentation is untested on hardware, and defect 1 proved that path is where platform delivery bugs live; **(G-2) Android** — every D2 row is iOS on one device model, so row 1 plus the canary must be repeated on Android before the stage-2 default flip. Both are rollout preconditions, **not** D2 preconditions — holding the sign-off for them would conflate the track's deliverable with the gates it feeds. **Rig policy**: do not rebuild for the sign-off. Rebuild once, when G-1 and G-2 run together in a single rig session sharing one positive control — and that control must itself carry a body and a frame larger than one segment, with its coverage figure reported beside the sealed runs (`PLAN-D.md` §14). **Method note carried forward**: the screen-string marker list was hand-written by both reviewers and incomplete both times; the next capture derives its markers from the run's own artefacts rather than from memory.

Earlier: 2026-09-02 ~19:10 IDT — **D2-REPORT.md reviewed and revised; the verdict stands, the evidence behind it changed.** The review found the report's central method fault: the §14 field-decode pipeline greps only what tshark dissects, and on the sealed captures **~30 % of payload bytes never reached the decoded files** — including one server-to-client WebSocket frame per capture larger than the MSS, i.e. the class most likely to carry terminal output. The original "0 marker hits" were therefore computed over a subset. The control could not have caught it, because every body and frame on the legacy rig fitted in one segment and 100 % of it dissected — a control that exercises only the easy path certifies only the easy path. **Independently verified and closed**: a full raw sweep of all 379 483 B of TCP payload across the three captures finds zero canary, zero markers, zero screen strings; the longest printable run outside HTTP headers and Noise blobs is the server's plaintext 429 body. One genuine correction: `d2-demo` **does** appear once, in the plaintext request line `GET /api/browse?path=d2-demo` — D-7 by design, but the first draft's "0 hits" was wrong as written. Also added: the 429 refusal body to the in-the-clear-by-design list; the disclosure that the replay capture ran under **27** rate-limit responses (negative-pressure evidence); a row 5 write-up of its own; the morning's 17 Pro storm, previously omitted; row 9 softened from "sealed" to "functions" end-to-end through the tunnel, since sealing there is inferred, not observed; and the `grep -c` line-count caveat. **Defect 2's brief is NOT accepted**: the retrying layer is still unidentified (close-time path and the 45 s timer both fail to match the ~1.5 s cadence; the client-log shipper is a hypothesis needing a device log), so only fix item 1 — a sticky per-server permanent verdict a later 429 cannot reset — is caller-independent. `PLAN-D.md` §14 rewritten: the raw sweep is now the primary grep, the field pipeline is required alongside it because masked client-to-server frames need unmasking, and the control must include a body and a frame larger than one segment and report its own coverage. **New D2 gaps, both recorded as preconditions**: no WebSocket continuation frame was ever captured, so client-side fragmentation is untested on hardware (precondition for R2); and Android is entirely uncovered (precondition for the stage-2 flip). Recommended order: this revision, then one large-payload iOS row, then the Android leg.

Earlier: 2026-09-02 ~18:52 IDT — **ALL THREE PRs MERGED; streamer released as `v1.74.0`.** Streamer **#752** merged `a3a6adf08e3d` (10/10 green, both Node versions plus macOS/Windows smoke) carrying all four D2 follow-ups — refusal logging, `--no-e2ee`, the Access boot probe, and the docs correction — and semantic-release cut **`v1.74.0`**. Mobile **#940** merged `443771a89c31` (the ArrayBuffer fix, 22/22 green; it superseded #937, which was closed and re-opened re-dated at the user's request) and mobile **#938** merged `ef9169604c89` (E2EE client documentation, rebased onto the new main first, 23/23 green). Streamer has 0 open PRs; mobile has 2 open that belong to other work. **Housekeeping done**: the `tb-streamer` root checkout was stale at `9b5f367d` with 28 dirty entries — every one proved to be a byte-identical copy of an older commit or a file present on `origin/main`, so nothing was lost; the two untracked prompt files were the only unique content, and the user confirmed both could go (`dep-upgrade-scan-prompt.md`'s output already exists as issue **#745**, which also documents the `conventional-changelog-conventionalcommits` 10.4.0 breakage that caused today's lone recurring suite failure — a tracked issue, not machine drift). The root is now clean at `origin/main`. **Remaining**: the user's acceptance line for `D2-REPORT.md`, and a shutdown of the scratch rigs (`:8790`, `:8791`, Metro `:8081`) plus the phone's three server entries.

Earlier: 2026-09-02 ~18:26 IDT — **The evening PR run is out.** At the user's request every local commit was re-dated to 18:23 by cherry-picking onto fresh branches (content verified identical to the approved versions — `git diff` empty against the old branches, and the streamer's three affected suites re-ran 66/66). **Streamer PR #752** (`feat/e2ee-d2-followups`, 4 commits: refusal logging, `--no-e2ee`, the Access boot probe, and the docs correction) and **mobile PR #938** (`docs/e2ee-client-notes`, documentation only) are OPEN with CI running; **mobile #937** (ArrayBuffer fix) is untouched and still green. Note two mobile PRs are open at once, against the one-PR-at-a-time rule — the user asked for #938 explicitly; suggested order is #937 first, then rebase #938. #937 keeps its original 17:44 timestamp because re-dating it would abandon a green CI run. The `tb-secured` Access application has been **deleted by the user**; the edge is back to the pre-Access baseline (`/healthz` 200, `/api/info` 401), so `ACCESS-APP-TEARDOWN.md` is discharged apart from the phone's stale tunnel entry. **D2-REPORT.md still awaits the user's acceptance line.**

Earlier: 2026-09-02 ~18:15 IDT — **D2 IS COMPLETE. `tracks/D/D2-REPORT.md` is written and awaits the user's acceptance.** Nine rows PASS on hardware plus both timing measurements. Headline: no plaintext on any sealed channel by any probe — 183 WS frames and 334 HTTP bodies across three captures, zero marker hits, every on-screen string absent, and a **chosen-plaintext canary** (`ZZTOPMARKER123`, typed by the user, proven received because the streamer named the session after it) absent from the decoded WS bytes, the decoded HTTP bodies **and the raw pcap**. Timings measured with a scripted device doing real Noise handshakes: the WS first-frame deadline closed a silent socket at **10 009 ms** while a socket that sent a valid frame survived past 25 s; a REST context used immediately still answered at t+35 s while one never used answered **`409 E2EE_CTX_UNKNOWN`**, which is the provisional-vs-promoted boundary in one run. Two plan corrections landed: §14's grep would have produced a **false pass** on the REST leg (hex payloads, plus `"type":` being WS-only), and its "foreground past threshold" wording describes something the client does not do — every foreground rotates, unconditionally. **Seven defects found, none in the protocol**: three mobile (ArrayBuffer frames — fixed, #937; the 429-laundering retry loop — reproduced, brief written, not implemented; the 45 s silence-timer churn at 62 % of the per-device budget), one mobile UX (a pending prompt blocks the Terminal view too), two streamer (silent handshake-failure branch, and nothing warning about Access — both fixed locally), one environment (`spawn-helper` exec bit). **Local chain, nothing pushed**: streamer `21bdcfd4` → `4c4d6e53` → `4740d6c6` → `5a3875b3`, mobile docs `53ce4890`, plus mobile PR #937 open and green. **Owed**: the user's acceptance line for D2, the evening PR run, and the Access app teardown.

Earlier: 2026-09-02 ~17:05 IDT — **Row 9 complete, both halves, on hardware.** With interactive Access on `<test-tunnel-host>`, the phone's pairing **failed closed** — "this server offered an encrypted pairing and then did not finish it" — and the rig log shows **no `/api/pair/exchange` at all**: the request never arrived. The guardrail (a missing msg2 is a failed pairing, never plaintext success) held against a real edge gate. With a **Bypass** policy added to the same application, the same phone paired through the same hostname and opened 3 WS + 2 REST contexts with every request 200 — **sealed transport works end-to-end through a Cloudflare tunnel**, which no earlier row had shown. Same hostname, same device, two outcomes, one variable. **Service Token is not a usable remedy today**: `tb-mobile` has no `CF-Access-Client-*` support anywhere, so the options for a streamer behind Access are remove Access or bypass the device paths — that goes to R's rollout guide. Also recorded: the client's pairing error blames the server for a gate's refusal, a diagnosis gap worth its own mobile issue. **In flight**: streamer `feat/e2ee-access-probe` (link 3 of the local chain) — boot-time probe of the server's own public URL, warns loudly and never blocks, off via `--feature accessProbe=false`, service-token creds read from `server.yaml` when present. Then docs across streamer + mobile, the two timing measurements, and `D2-REPORT.md`. The Access app is **still live with a Bypass policy** — teardown steps in `tracks/D/ACCESS-APP-TEARDOWN.md`, to run once the probe work is verified against a real gate.

Earlier: 2026-09-02 ~16:35 IDT — **Rows 5 and 9 done; D2 now has 9 of 9 rows answered, 2 timing measurements left.** Row 5 PASS: the owner's foreground rotated the REST context (new context, not an in-place rekey) with **zero failed requests** across the rotation. Also corrects the plan — `PLAN-D.md` §14 says "foreground past threshold", but `services/e2ee/rest-session.ts:60-63` sets `needsRollover` on **every** foreground, unconditionally; there is no threshold. Row 9 **settles review carry-in N-L1 by measurement**: the owner created a self-hosted Access application over `<test-tunnel-host>`, and every probe that previously reached the streamer now gets a 302 to an interactive login — `/healthz` (was 200), `/api/info` (was 401), and critically `POST /api/e2ee/open` with no `Authorization` (was 400 from the streamer). Control: the identical POST straight to `127.0.0.1:8790` still returns 400, so the shape is fine and the edge is what stops it. **An E2EE device cannot pair or open a context through an interactive Access gate** — the remedy is Access off for that hostname or a Service Token (Cloudflare's own redirect JWT carries `service_token_status: false`), never `Authorization` re-added to sealed requests. Compounds with the retry defect: the client would get HTML where an envelope belongs, fail, and retry forever. `d2-row9-access-blocks-sealed-requests.md`. **Owed cleanup**: the `tb-secured` Access application is live and must be deleted when D2 closes (owner-created; `CF_API_TOKEN` lacks Access edit rights, `auth.forbidden` 1010). The tunnel stays. **Not tested**: whether a Service Token gets a device through — the obvious next measurement, and it needs dashboard rights.

Earlier: 2026-09-02 ~15:35 IDT — **Rows 6 and 8 PASS, and both turned up client defects worth more than the rows themselves.** Row 6: a Wi-Fi toggle produced a fresh `POST /api/e2ee/open`, a new ticket and a new WS context, no reuse. Underneath it, **the socket redials every ~45 s at idle** — `WS_SILENCE_TIMEOUT_MS = 45_000` in `hooks/useTerminalStream.ts:25` force-reconnects when no WS traffic arrives, and cannot tell a dead socket from an idle session; measured **3.1 context opens/min against a 5/min per-device limit, 62 % of the budget burned doing nothing** (`d2-row6-silence-timer-churn.md`). That is the baseline this morning's 429 storm sat on top of. Row 8: revoking the live device refused its next frame in the same second (`e2ee.frame_refused revoked`) and answered the re-handshake `403 E2EE_DEVICE_REVOKED` — immediate, no restart. It then reproduced the 17 Pro storm **deliberately**: 10 × 403 then 60 × 429 in under two minutes, and identified the mechanism — **a permanent refusal launders itself into a retryable one**, because retries charge the failure budget, the server answers 429, and `services/e2ee/context.ts:212-214` maps 429 to `E2EE_TRANSIENT` (`retryable: true`). The on-screen diagnosis degrades with it: the accurate "This device is not paired for encryption" is replaced by the false "The server is busy; retrying shortly" (`d2-row8-revocation-and-the-429-laundering.md`). `tracks/X-client/PROMPT-stop-polling-hard-refusal.md` is updated from hypothesis to **CONFIRMED**; only the identity of the retrying layer is still open. Every finding here is client-side — the streamer behaved as designed throughout, and its budget is what kept one broken device off the fleet. **The D2 device is now revoked**, so rows 5 and the two timing measurements need a re-pair or a scripted client. Row 9 still waits on the user's Access decision.

Earlier: 2026-09-02 ~15:25 IDT — **D2 rows 1–4 PASS, with a chosen-plaintext canary.** Three sealed LAN captures on `:8790` (294 / 979 / 514 packets; 183 WS frames and 334 HTTP bodies in total, ~226 KB decoded): zero marker hits on either leg in all three. **Row 4 is the decisive one** — the user typed `ZZTOPMARKER123` by hand as a new session's first message; the streamer named the session `ZZTOPMARKER123` (`startedAt 12:20:50.680Z`, inside the capture window), proving it was received, and the string appears **0 times in the decoded WS bytes, 0 in the decoded HTTP bodies, and 0 in the raw pcap** — the raw search rules out the decode step hiding it. Rows 1/3: every string visible on the phone's screen during the capture (`Let's get started`, `Select login method`, `Dark mode`, `d2-demo`, `console.log`, `Anthropic Console`) is absent from both legs. Row 2 (replay, incl. the 2 s HTTP fallback) covered by the 979-packet capture. Longest printable run in 74 KB of WS frames: 10 characters; outside the Noise blobs the HTTP leg has no printable run of 16+ characters. Evidence: `tracks/D/evidence/d2-rows-1-2-3-4.md` + three pcaps. **Two incidents, both mine, neither a protocol fault**: the rig's `node-pty` `spawn-helper` had no exec bit (postinstall blocked at install time) so every session start died as `posix_spawnp failed`; and an unanswerable agent prompt blocks sending in the **Terminal** view as well as chat, making the session uninteractive from the phone — worth a streamer issue and a mobile issue respectively. **Still open in D2**: rows 5 (REST context rotation + 10 s drain), 6 (reconnect fetches a fresh ticket), 8 (revoked device's socket closes), 9 (Access/tunnel functional pass — Access still unconfigured, and that is a user decision), plus the two timing measurements and `D2-REPORT.md`.

Earlier: 2026-09-02 ~13:15 IDT — **Local streamer chain committed, nothing pushed.** `feat/e2ee-open-refusal-log` `21bdcfd4` → `feat/e2ee-no-e2ee-flag` `6b99a700`, both off `ab15fc2c`, stacked so the second is a trivial rebase once the first squash-merges tonight. **R1 is implemented per the approved PLAN-R1.md**: `--no-e2ee` as a `serve` option only (new `cli/no-e2ee.ts`, the `boot-log.ts` testability precedent), `/api/info` reason now names the rung that decided (`disabled by --no-e2ee (or --feature e2ee=false) for this run`), and a boot warning through console + JSON `e2ee.disabled` carrying the pinned-device count with #744's never-connected caveat in the text. Ruling taken while implementing: `--no-e2ee` together with `--feature e2ee=true` **refuses the boot** rather than picking a winner, matching how `--feature` already treats a typo. `env` still outranks `cli`, deliberately — R2's escalation is not pre-empted, and a test pins that. Gates: 16/16 new tests, biome clean on all five files (named explicitly), tsc 0, full suite **2737 passed**; **5/5 mutations seen red** with verbatim assertions, each reverted. One pre-existing failure throughout, not ours: `release-notes.test.ts` fails identically with the branch stashed — the shared root `node_modules` has `conventional-changelog-conventionalcommits` 10.4.0 while `origin/main`'s lock pins 9.3.1, and 10.x needs `conventional-changelog-writer@9+` against the locked 8.4.0. CI on main is green; the root checkout sits on a stale commit and is shared, so reinstalling is the user's call. Next gates: user's go to open both PRs this evening, and D2's hands-on capture for rows 1/2/4.

Earlier: 2026-09-02 ~12:05 IDT — **Mode change at the user's instruction: local only.** Work is staged as local branches and opened as PRs this evening, not on landing. #937 stays open (already pushed, CI-green). Chain so far: (1) streamer `feat/e2ee-open-refusal-log` — the `readMessage1` failure branch now logs `e2ee.open_refused` with `reason: "handshake"`, the unknown-device line gains `reason: "unknown_device"`, the post-handshake bad-payload branch logs too, and the uncharged branches (malformed body, both 429s) stay deliberately silent because a line there is an unbounded disk write an unauthenticated caller controls — a test pins that silence. 31/31 in the route suite, biome clean on both files (named explicitly, the `.worktrees` ignore quirk), tsc 0; **3/3 mutations seen red** with verbatim assertions, each reverted. (2) `PLAN-R1.md` written and **awaiting approval** — pinned to `ab15fc2c`, five rules with a mutation each, and it flags that `env` outranks `cli`, so `THREADBASE_FEATURE_E2EE=1` beats `--no-e2ee` by documented precedence, which is R2's collision and is not pre-empted. (3) Mobile poll-storm: **prompt only, no implementation**, at `tracks/X-client/PROMPT-stop-polling-hard-refusal.md`. **Correction to the 11:52 entry**: the open route was never silent in general — `e2ee.open_replayed` and the unknown-device `e2ee.open_refused` already logged on `origin/main`; the gap was specifically the handshake-failure branch. That also diagnoses the 17 Pro without its client log: a 400 rather than a 403 means the handshake itself failed, so it is pinned to a server identity that is not this scratch rig's, and it recovers when the hostname points back. Evidence file corrected.

Earlier: 2026-09-02 ~11:52 IDT — **Mobile PR #937 open** (`fix/e2ee-ws-arraybuffer`, commit `979ec994`, rebased onto `origin/main` `fe5d37b7`): the on-device ArrayBuffer defect found during D2 is fixed and covered. Gates before opening: focused 41/41, typecheck 0, lint 0, full unit suite 1802/1802 across 180 suites, and the falsifiability mutation (revert the source fix) turns exactly one test red — `unseals an ArrayBuffer pinned-server frame before dispatching it`. CI running; note mobile also has dependabot #879 open, so confirm the one-PR-at-a-time reading before merging. **D2 field observation recorded** (`tracks/D/evidence/d2-field-observation-open-failure-storm.md`): the second paired iPhone (UA `Darwin/25.6.0`) storms `POST /api/e2ee/open` — 168×400, 43×429, pinned at exactly 30 failures/minute, i.e. `OPEN_SOURCE_FAILURE_LIMIT` confirmed working in the field for the first time, with the healthy D2 device unaffected on its own LAN address. Two follow-ups: through the tunnel both devices collapse to `127.0.0.1` and would share one budget (the scenario `rate-limit.ts` itself warns about, now produced by a real device); and a failed handshake emits no `e2ee.*` event at all, so a stuck device is undiagnosable server-side without a capture. Left running at the user's request; no plaintext, key or ticket exposure.

Earlier: 2026-09-02 ~11:45 IDT — **D2 sealed LAN capture taken and it is clean** (`tracks/D/evidence/d2-sealed-8790.md`): 92 packets on `:8790`; 4 binary WS frames (442 B, both directions) and 25 HTTP bodies (4412 B) decode to **zero** plaintext markers on either leg, and `strings -n 6` over the WS bytes returns nothing. In the clear by design only: request paths/query (D-7) and the two `POST /api/e2ee/open` Noise handshake bodies. The same pipeline scored 1 WS + 4 HTTP marker hits on the legacy control, so the zero is meaningful. **Coverage gap, stated plainly**: the scratch rig had no live session, so rows 1 (terminal output), 2 (replay) and 4 (user messages) were never exercised, and row 3 saw only an empty conversation list — those rows need a re-capture with a running session. Row 7 (transparent re-handshake after the streamer restart) *is* covered: one REST + one WS context reopened with no user-visible failure. **New observation**: from 11:40 a second device (UA `CFNetwork/3860.700.1 Darwin/25.6.0` — the iPhone 17 Pro, not the D2 device) is retry-storming `POST /api/e2ee/open` → 400. Not a plaintext or key exposure; it pollutes captures and needs isolating before the next run.

Earlier: 2026-09-02 ~11:40 IDT — **D2 LAN positive control PASSED** on the legacy `:8791` rig (83 packets captured; 8 WS frames, 6 HTTP bodies decoded): plaintext WS marker `"type":` found ×1, plaintext REST markers `"conversations":`/`"sessions":`/`"serverIdentityKey":` found on the HTTP leg. **Runbook defect found and fixed** (`tracks/D/PLAN-D.md` §14): tshark prints payloads as hex, so the old `grep -c '"type":' ws-payloads.txt http-bodies.txt` could never match, and `"type":` is a WS-only field — on the plaintext control the HTTP leg scored 0 with it, i.e. it would have reported a false "sealed" for REST. Both legs now decode hex→bytes and use a per-leg marker set. Sealed rig `v1.73.0` restarted on `:8790` (health 200, two paired iPhone device rows; in-memory contexts cleared by the restart, so the client's next connect doubles as row 7's transparent re-handshake). Next: sealed LAN capture on `:8790` for rows 1–4 (needs the user's sudo tcpdump + phone taps).

Earlier: 2026-09-02 ~11:16 IDT — D2's isolated streamer `v1.73.0` rig is live on `:8790` under a fresh scratch HOME/config/API key; `tshark` 4.6.8 is installed. The existing launchd production streamer is unloaded and `:8766` is closed, as requested. Dedicated tunnel `tb-e2ee-d2-20260901` is connected and `<test-tunnel-host>` is routed to it; edge-forced `/healthz` is 200. The selected wired iPhone is paired, Developer Mode enabled, and has the D2 app development build running the fixed JS bundle from Metro on :8081. Pairing succeeds and the first post-fix sealed WebSocket context remained stable for more than 45 seconds; functional UI confirmation and sealed transport capture remain pending.

Earlier: 2026-09-01 ~12:10 IDT — XC2 isolated adversary **broke it** on `AUTHORIZATION` casing and POST `x-tb-env` dual carriers (`tracks/X-client/ADVERSARY-XC2.md`). Case-insensitive header strip landed; those two probes are now unit tests; mutation campaign **12/12 seen red** after the fix. PR not yet opened (six mobile PRs open at that time).

Earlier: 2026-08-31 ~18:22 IDT — Release-notes PR #750 **MERGED** (merge commit `c2774364fff6ad7217353c05cd0eaf75a2064826`), independently confirmed by the coordinator via `gh pr view`; user gave fresh explicit squash-merge approval after 10/10 green CI was shown. Branch `fix/release-notes-rendering` auto-deleted by GitHub. Now waiting for semantic-release to publish the new tag and for the tag/notes to be verified before the Streamer PR slot is declared free for X-server. User also approved PLAN-X-client.md, the C-5 scope addition (noise.ts/pair-handshake.ts psk-less IK branch), the bigint counter ruling, and relaying C-4 to the streamer side; XC1 has moved into implementation.

Earlier: 2026-08-31 ~18:10 IDT — Release-notes PR #750 opened against `main` (`fix(release): render conventional commit entries in notes`), independently verified OPEN/MERGEABLE by the coordinator via `gh pr view`. Local gates all green pre-open: focused tests 6/6 (incl. real-plugin `generateNotes` control), tsc 0, pinned Biome 473 files clean, lock-protected full suite 258 files/2655 tests passed. X-server (rebased onto `v1.72.0`, header remedy + #743 finding closed) and XC1 (plan at the approval boundary) both continue in parallel; X-server still holds off opening its own PR until this one is MERGED and the slot is confirmed empty.

Earlier: 2026-08-31 14:09 IDT — W1b is released as streamer `v1.72.0`, and X-server/XC1 remain unblocked on that exact tag. The separate release-notes fix is committed and pushed as `87354b1` on `origin/fix/release-notes-rendering`, with matching local/remote SHAs and all local gates green. PR creation is blocked by unrelated open Streamer PR #749, which is behind `main` and red on lint; the one-Streamer-PR-at-a-time rule prevents opening a competing PR.

Current W1b override: the historical W table row below records the pre-merge state. W1b's product artefact is **RELEASED** at `v1.72.0`, and downstream transport work is unblocked. **W1b is now FULLY COMPLETE**: the release-notes defect is fixed and independently verified via PR #750 → tag `v1.72.1` (2026-08-31 ~18:27 IDT); Task 1 Step 11 is checked off in `PLAN-FINISH-E2EE-2026-08-30.md`; details in `tracks/W/REPORT-W1b.md`. No gates remain open for W1b. **The Streamer PR slot is now free** — independently confirmed empty by the coordinator (`gh pr list --state open` → `[]`) — and X-server has been told it may rebase and open its PR.

Earlier: 2026-08-31 08:43 IDT — W1b commit `6d71e774` is pushed and PR #748 is open and mergeable with 11/11 GitHub checks green, including Node 22/24 full suites and macOS/Windows smoke. The next gate is separate user approval to refresh/rebase and squash-merge; X-server remains frozen until the semantic-release tag is published and independently verified.

Pins at kick-off: streamer `v1.70.6` (0069afc1); mobile `origin/main` 229faf6b (`ios-v210`, `android-v59`); expo 57.0.18, react-native 0.86.3, `@react-native/jest-preset` 0.86.3, `@stablelib/*` 2.0.1, jest 29.7.0; vitest 4.1.11, typescript 7.0.2, hono 4.13.3, ws 8.21.3, better-sqlite3 13.0.3, `@threadbase-sh/scanner` 0.14.6, `@threadbase-sh/agent-types` 1.0.0; Node v24.15.0; Xcode 26.6 (17F113); Maestro 2.8.0; Claude Code 2.1.250.

| Group | Session | State | Last report | Next gate | Blockers |
|---|---|---|---|---|---|
| PR 0 — plan of record into the streamer | `e2ee-owner` | **#737 MERGED 20:57 → 76d6d420** (docs-only, no release commit); worktree and branch removed; `tracks/` mirrors `specs/end-to-end-encryption/parallel-execution-plan.md` | 20:58 | — | — |
| P — pairing contract close-out (streamer #590) | `e2ee-P-sonnet5-medium` [e517a2] | kicked off 20:45; precondition re-verified; pinned 0069afc1; worktree `.worktrees/fix/e2ee-pairing-closeout`; P reads item 4 as already correct on main (`server.ts:2185/2275/2284`, owner-confirmed) with the test stubbing `repo.register` (`pair-exchange-authenticated.test.ts:297`) — a coverage gap, not a feature gap; audit sub-agent dispatched for items 2–5; 21:55: audit content in hand (items 2, 3, 5, item-4 rule + real-path spike, P verified item 4 itself), full-suite exit code pending under the lock; lint quirk relayed. **Audit table 21:58**: item 2 mutation RUN (banner `if (true)` → 6/10 red); item 3 covered by real-HTTP/real-Noise tests, mutation reasoned only; item 4 gap confirmed (L293 stubs `register`), real-path spike run (DB handle closed → 500, 0 rows on fresh open, retry 401 + replay warn); item 5 vectors untouched since #631, legacy key-set test at L353, mutation reasoned only. **Owner 22:00: rule APPROVED**; conditions: run item-3 and item-5 mutations, read the two unread gate-test rows, fix the stale `revoked_at` comment (design §4.4 covers it). **22:25 all four conditions closed**: item-3 mutation red (`expected 'relabelled-in-transit' to be 'Ronen's iPhone'`), item-5 mutation red (L353 key-set), gate rows read, comment corrected; item-4 permanent test `pair-exchange-e2ee-registration-real-failure.test.ts` (real DB-handle close → 500; fresh-open `COUNT(*)=0`; retry 401 + `pair.token_replayed` via real logger), falsifiability red (`expected 200 to be 500`); lint 0/0/0. Diff = comment + new test only. Two lock-protected suite runs pending (second covers the new file). **22:39 final suite green** (249 files/2462 tests, +1/+1 = the new test); staged diff (+176/−6: comment-only repo change, new real-failure test); **owner read 22:55 → cleared to ask the user**; suggested title `test(e2ee): prove a failed registration burns the token and leaves no device row`. **User approved; PR #739 open 22:44** (bff966ff, rebased over docs PRs #737/#738 — #738 is the user's own prompt-contract docs PR, outside this program; mutation re-run red post-rebase). **#739 MERGED 22:52 → origin/main 3f7f9924** (10/10 green; `test:` commits release false → no tag, correct); **#590 close-out posted 22:53 (comment 5457103249): items 1–5 ticked with real-path + mutation evidence, 6–7 obsolete. GROUP P CLOSED 22:55**; worktree removal ordered; session closable **GATE 1 PASSED 03:02 — analysis accepted, shape approved, no code yet.** Worktree `.worktrees/fix/e2ee-ws-app-ping`. **Concern 2 (seal-then-send reorder) proved absent rather than argued unlikely:** `sendTo` is synchronous end to end, `RecordState.seal` is synchronous (`record.ts` and `context.ts` contain no `async`/`await`/`Promise` at all), and the emission site is the existing synchronous `setInterval` callback, so two `sendTo` calls cannot interleave on one thread — counter order equals send order, and `ws` is FIFO per socket. A proof from the absence of suspension points, with the corollary commented at the site: the hazard would be **created** by making that callback `async`. **The ordering hazard that is real** is placement relative to the two guards already inside `startPing`. ~~[SUPERSEDED — see the 03:27 entry: the claim that this closes the socket `E2EE_SEAL_FAILED` instead of `E2EE_CTX_UNKNOWN` was disproved by P's own mutation run, and the owner then restated the dead version back to P before catching it. `sendState` on an invalidated context raises `E2EE_CTX_UNKNOWN` itself, so §9 is untouched either way.]~~ The corrected cost, which still justifies the placement: emitting above the stale-context guard attempts a send on a socket the sweep has **already decided to close**, and logs the refusal as phase `"send"` rather than phase `"maintenance"` — the sweep's own verdict recorded as if the frame had been at fault. Two existing assertions pin the correct placement. Shape: reuse the existing 30 s interval rather than add a timer; emit to **every** socket, sealed and plaintext alike, after verifying against mobile `origin/main` that an unknown type misses with no default and no throw before wildcard handlers fire — §3's "never break released clients" discharged by evidence; protocol ping untouched. Owner added one requirement: a paragraph on the **known-plaintext-at-known-intervals** property the change creates, since an idle sealed socket now emits the same small frame shape every 30 s forever, and that is the first question a crypto reviewer asks. **GATE 3 — 03:27, all gates green, holding for the slot.** 2 files, +249/−8, `src/ws-hub.ts` and its test; `server.ts` untouched, so overlap with R is **zero files**. Emission on the existing 30 s heartbeat through the same `sendTo`, after the stale-context and `readyState` guards; protocol ping untouched; `PING_INTERVAL_MS` and a new `CLIENT_SILENCE_TIMEOUT_MS` (45 000, mirroring the mobile constant) exported so a test can assert the relationship. **P corrected its own gate-1 claim after the mutation run disproved it:** emitting above the maintenance guard does *not* change the §9 close code, because `invalidateContext` clears the send-state map and `sendState` then raises `E2EE_CTX_UNKNOWN` itself. Both the note and the **code comment** were fixed — a comment asserting a false mechanism is worse than none, since the next reader reasons from it. The mutation still earns its place on the real cost: a send attempt on a socket the sweep has already decided to close. Six mutations red. Two are better than the brief asked for: cadence 30→60 s fails loudly, but **cadence 30→36 s** is the change a maintainer actually makes believing it harmless — it stays inside the window and merely eats the margin, and pinning `expected 9000 to be greater than or equal to 10000` turns the margin into a tested invariant rather than a comment nobody reads. **Slot released to P at 05:17.** Rebasing `dc36bd8` onto `04090873`, then all six mutations plus the full suite on the rebased head, with re-approval required if the diff moves a single line. Told two things to watch that are interactions rather than conflicts: R's change now fires the boot warning for env- and yaml-sourced disablement, so any test booting with e2ee off and asserting on log output will see a new line; and the `§11` replay flake has a few-percent chance of firing, in which case P reports rather than re-running — the re-run authorised earlier was legitimate only because the cause was already established, and that judgement stays with the owner rather than becoming a reflex. **PR #761 OPEN and mergeable at 05:32.** Post-rebase suite uncontended: 1 failed / **2768 passed** / 5 skipped, the single failure being the known `release-notes.test.ts` (#745). **`EADDRINUSE` count 0, with a positive control on the same log**, so the zero is a measurement rather than a grep that found nothing — and that comparison **falsifies the port collision as a defect**: same tree, same test, same machine, failing at `listen()` contended and passing uncontended, one variable. Neither watch item fired. Test totals rose 2768 → 2774, which is exactly R's six new tests arriving — a free confirmation that the rebase pulled R's work in rather than silently dropping it. P re-fetched to confirm its base before pushing rather than trusting state from ten minutes earlier. | 05:32 | owner merges after #760 | streamer PR slot held by R until it merges |
| M — mobile pairing trust-boundary audit (#698) | `e2ee-M-opus5-high` [109d97] | kicked off 21:21; **arrival report 21:25**: preconditions re-verified (#768/#766/#782/#833 MERGED; v1.70.6 = 0069afc1; mobile main 229faf6b pinned; streamer pin 1.70.6 exact); worktree `../tb-mobile-worktrees/e2ee-pairing-audit`, own `npm ci` exit 0; owner ref recorded; **audit table delivered 21:34** (`tracks/M/AUDIT-M.md`): rows 2–6 HOLD; row 1 coverage defect (scanner and paste have no malformed-`spk` test; paste runs a `__DEV__` mock that never parses — the positive control that passes green and should not; production unaffected); row 7 coverage defect (`loadPersistedServers` asserts only the pin; `servers.ts:555` device-token read covered by nothing; stale "cannot be executed" comments). **Owner ACCEPTED 21:40; two test-only PRs approved** in order (a) item 7, (b) item 1; interop bytes vs 1.70.6 first. Observations: DEV-only unverified fingerprint → P3 issue; double unordered `persistServerList` in `addServer` → P2 issue (F candidate); #698 "wrong `v`" wording → owner corrects; expo-router `''` param → P3 (expo-router mocked wholesale in `jest.setup.js:29`). **Interop vs real 1.70.6 done 21:42**: items 3 and 4 HOLD on bytes (`publicUrl: null` on the wire; same `D_priv` + same device row across two tokens; msg1's authenticated name on the registry); rig hygiene clean. **Defect 3 found on the wire**: a well-shaped 43-char `spk` that is not a valid X25519 point throws a bare `Error: X25519: invalid shared key` from `writeMessage1` (`pair-exchange.ts:307`, outside both try/catches) → both entry paths show the generic error and offer "Try again" on an unrecoverable QR. Fails closed (no server, no plaintext). **Ruled 21:45: third PR `fix(pair):` after (a) and (b)**, map to `e2ee-malformed` non-retryable, real-curve tests, plan-gated. **PR (a) staged 21:55** (3 files, +75/−21; `servers.ts` comment-only; two real-loader cases + negative control; mutation `deviceToken = undefined` → 48/0 silent before, `toBe('dt_9')` red after; tsc/lint/unit/integration/e2e all exit 0); **owner read the diff 21:57 → cleared to ask the user**; title `test(servers): cover the pinned key and device token through the real loader`. **PR #900 open 21:50** (3c7ca880, user commit approval 21:49, rebase no-op on 229faf6b), CI watching; sub-agent delivered late (21:54) — M worked first-hand. **#900 MERGED 21:57 → e35124b0**; (b) started on `fix/e2ee-pairing-surfaces` (scanner + paste real-path tests, item 6 web-module test; mutations = two `serverPublicKey` deletions + web flag flip); sub-agent moved to its own worktree after a two-writers near-miss; item-6 test shape depends on explicit-path import working under the unchanged preset — explicit-path import confirmed working. **PR (b) staged 22:33** (+252, test-only; 3 mutations red: scanner key deletion, hook key deletion, web flag flip; tsc/lint/unit/integration/e2e 0); **owner read 22:35 → cleared to ask the user**; title `test(pair): watch the QR server key on every pairing surface, and web's refusal`; sub-agent M1–M7 all red; G1/G3 green-with-guard-removed proves the defects. **#901 MERGED 22:48 → b97449e3** (items 1 and 6 closed). **(c) staged 22:53** on `fix/e2ee-pair-invalid-key` (+96/−8): `writeMessage1` inside a try, mapped to `PairUriError('bad-server-key')` (non-retryable, damaged-key copy) — deviation from `e2ee-malformed` approved (that kind is retryable by design); real-curve unit test + deep-link screen test, mutation red, both controls green; owner read 23:30: narrow the bare catch. **23:35 narrowed structurally** (only `writeMessage1` in the try; `/X25519/` pinned as a test so a library change breaks the test, not the classification); mutation red 2/90, four distinct outcomes; tsc/lint/unit/integration/e2e 0; owner re-read → cleared. **User approved 00:59 → #902 MERGED → f3e82287.** Issues #903 (P2 persist crash window), #904, #905, #906 (P3s) filed; #698 six items ticked with evidence. **GROUP M CLOSED 01:08**: 4/7 hold, 3 coverage defects fixed, +1 interop-found defect fixed; hygiene verified; worktree removal ordered | 01:08 | seven-row audit table → owner verdict | user go |
| W — WS record layer (W0 → W1a → W1b) | `e2ee-W-opus5-high` [e16afe] | **GROUP W CLOSED** — W1b **RELEASED** `v1.72.0` (#748); release-notes **v1.72.1** (#750). Earlier detail retained below. **W1b refrozen 00:10** — A1 landed (three-arm `authenticateContext`, `DevicePrincipal` intersection, `no-device-store` → 503 `STORE_UNAVAILABLE`, deadline closes `E2EE_CTX_UNKNOWN`); campaign 25/25 red, 0 halts; suite 2627 green; HEAD f95150cc uncommitted; **adversary round 1 (00:25): 68 probes / 12 rows, 11 rejected, one real break** — `??` default for `firstFrameMs` at a trust boundary (prototype pollution moves the 10 s thief deadline both ways; `own()` rule from W1a round 5 not applied in new code) — plus three contract gaps in the frozen helper (`revoked_at` read outside the `try`; principal `deviceId` taken from the row, not the context; stale `contextIsDead` comment), two cheap items (frame-refused log drops the RecordError distinction; send path may map peer codes) and two orphan-context residuals (capability-refused ticket, falsy `ws.raw`). **Owner: fix all, re-campaign, suite, refreeze, targeted re-adversary**; 03:25: none implemented yet (implementer cut at 00:26, tree verified clean, restarting; + header bracket-read `own()` sweep added); then staged stat + `feat(e2ee)` message → owner read → user approval → PR → tag. Earlier: **W1b in progress (23:22)** — worktree `.worktrees/feat/e2ee-ws-sealing` on v1.71.0; sealing/ticket-header/`everSealed`/revocation/10 s deadline built; A1 = reshape `authenticateContext` to the frozen seam (decisions log 23:08), A2 invariant test, NONCE-DESIGN §8/§11/§13; then campaign → lock-protected suite → refreeze → adversary → commit approval → PR → tag. Must merge with the pure-verdict helper. Earlier: **W1a cleared 19:00 (day 2)** → #741/#742 → v1.71.0. Earlier: kicked off 21:22; first report 21:27 (preconditions re-verified, pinned 76d6d420, six W0 sites exact, no new hub method, wire-identity + no-bypass tests designed, 13-section NONCE-DESIGN outline, contradictions C1/C2 found); **W0 plan + outline APPROVED 21:35** with rulings on C1/C2 and Q1–Q5; `PLAN-W.md` persisted. **W0 staged 21:47** on `feat/e2ee-ws-hub-routing` (7 files, +241/−33; six sites only in `server-wiring.ts`; wire-identity captures diff empty; M1 bypass-restored → `ws-no-hub-bypass.test.ts` red with `expect(offenders).toEqual([])`; M2 one-byte perturbation → wire-identity red; five-of-six-frames harness catch; `host-pressure` assertion narrowed to "no host_pressure frame"); **owner read the diff 21:52, accepted pending suite exit code under the lock**; findings: biome checks 0 files inside `.worktrees` (pre-existing; P told), bare `catch {}` in `handleWsMessage` = W1b design input. 22:02: W killed its contended suite run (exit 144) and re-queued behind P under the lock; **commit message approved verbatim** (`refactor(ws): route every socket send through WSHub`); seal-failure close reason to be distinct from `E2EE_SEQUENCE_VIOLATION` (frozen in W1a). **22:23 W0 suite green under the lock** (250 files/2465 tests, 1079 s, sole occupant; new files proven run by 249→251 count); W asking the user for commit approval. W0 was done by the orchestrator itself (accepted: no crypto); ruling: W1a/W1b through the implementer sub-agent so reviewer ≠ author. **User approved 00:57 → commit 3cc15f9b**, rebased clean onto 3f7f9924, M1/M2 re-run red, scanner controls hold, lint 0/0/0 (459 files); suite green (2466/0, 1029 s) → **PR #740 MERGED 01:24 → 91ce3f18**, 10/10 CI, no tag (`refactor` not in releaseRules; Release workflow ran and chose not to). W1a: NONCE-DESIGN.md drafted 01:33 (13 sections; found `E2EE_EXCHANGE_VERSION` duplicate and the conditional `summarizeQuery` ticket leak — all-digit ticket logged verbatim); **owner read in full, APPROVED 01:45** with the context-per-channel ruling, `summarizeQuery` sensitive-key set in W1a, constant collapse, test-only counter seed. **Independent review of PR #748 delivered 09:02 (2026-08-31)** — fresh session, no branch context, brief `tracks/W/REVIEW-PROMPT-PR748.md`; report `tracks/W/REVIEW-2026-08-31-PR748.md`. Verdict **merge-safe as-is**; 9 of 10 invariants hold with traced evidence; one low-severity finding **F1** (a `/ws` request carrying `X-TB-Ticket` that is not an upgrade spends the ticket, promotes its context to 24 h and 404s — orphan with no socket and no first-frame deadline; `auth.middleware.ts:90-91`), plus one optional coverage gap (`no-device-store` → 503 untested through the route). Fix dispatched to Codex against `tracks/W/CODEX-BRIEF-PR748-F1.md` in the existing `feat/e2ee-ws-sealing` worktree | 09:02 | — | — |
| X-server — REST envelope (streamer) | `x-server-lead` | **RELEASED** as streamer **`v1.73.0`** (`ab15fc2c`) via PR #751. Envelope contract is the XC2 pin. Streamer PR slot empty. | 2026-09-01 | — | — |
| F — #759 evidence, #831 single gate host, F3 (#903/#904 + modal rename gap) | `e2ee-F-sonnet5-medium` [c35b57] | **CLOSED 21:15 (day 2)**: #908 (#759), #915 (#831), #917 (#903), #919 (#904) all MERGED; #698 updated; #909 (Maestro infra on iOS 26.1) filed for the user; worktrees removed; session closable | 21:12 | — | — |
| X-client XC1 — mobile WS transport | `x-client-xc1-lead` | **MERGED** mobile #927 → squash `bb5cd7f0`. Pin for children is that squash, not a streamer tag. | 2026-09-01 | — | — |
| X-client XC2 — mobile REST envelope | resume of Codex `01a050c1` in Cursor | **PR #934 OPEN** — `feat/e2ee-rest-envelope`, commit `5709337d` (`feat(e2ee): add REST envelope transport`). Worktree `../tb-mobile-worktrees/e2ee-rest-envelope`. Pin streamer **`v1.73.0`** (`ab15fc2c`, X-server #751). `sealedFetch` REST envelope + `rest-session.ts` lifecycle; adversary rows 4–5 fixed (case-insensitive header forbid; `tracks/X-client/ADVERSARY-XC2.md`); **12/12 mutations seen red** (`scripts/xc2-mutations.js`). Branch base predates **`main` `8e27ea72`** (#933 lint shard) — rebase before merge. | 2026-09-01 | squash-merge approval after rebase + green CI | `main` #933 not yet in branch base |
| D — device evidence (D1 pairing gates, D2 sealed transport) | `e2ee-owner` | **D2 IN PROGRESS — FUNCTIONAL BLOCKER RECORDED** — preconditions independently verified: #934 MERGED `b0e55f65`, #927 MERGED `bb5cd7f0`, streamer tags `v1.72.0`/`v1.72.1`/`v1.73.0` remote with `v1.73.0`=`ab15fc2c`; runbook `tracks/D/PLAN-D.md` §14. `tshark` 4.6.8 installed. The requested mobile worktree remains detached at `b0e55f65`. Isolated scratch rig runs `v1.73.0` on :8790; lsof confirms its DB/config handles are scratch-only. Production launchd service unloaded and :8766 is closed. Dedicated tunnel `tb-e2ee-d2-20260901` (`877706d3-1f47-4ac9-8832-c970547a379e`) is connected with explicit credentials/config and routes `<test-tunnel-host>` to :8790; edge-forced health is 200. Selected physical device `Rbv1000-13-Pro` is wired, paired, Developer Mode enabled, and has D2 build `1` installed; Metro is healthy on :8081. Sealed pairing succeeded, but the client repeatedly opens then reconnects its WS, exhausting the server's five-opens-per-device-per-minute limit and receiving 429. Separate legacy :8791 positive-control rig **has now delivered the required positive control** (pcap 83 packets; 8 WS frames / 6 HTTP bodies; WS `"type":`×1; HTTP `"conversations":`/`"sessions":`/`"serverIdentityKey":` present) — §14's grep was corrected first (hex decode + per-leg markers). Sealed rig restarted on :8790, health 200. | 10:38 | isolate the ticketed WebSocket close/reconnect; then user grants local sudo capture authorization and runs LAN plaintext positive control | WS functional failure; macOS tcpdump authorization; Access configuration unverified |
| G — device evidence (G-1 continuation frames, G-2 Android, C1's client-log capture) | `G` (Opus 5 / high) | **DISPATCHED 19:52** — owns the single rig session for the whole program; stands it up once from `PLAN-D` §§3–4 and serves all three purposes together. Pinned to streamer `v1.74.0` (`67f2d05e`) and mobile `origin/main` `26815a16`. Instructed to record the start-of-session port/process/simulator state **before** touching anything, so the "left as found" teardown is a comparison rather than a memory, and to treat `:8766` as untouchable production. Carries the three hard-won lessons explicitly: a control that exercises only the easy path certifies only the easy path (its positive control must push a body **and** a frame past one TCP segment and must report its own coverage figure); an empty result is evidence only after a positive control; the marker list is derived from the run's own artefacts, never from memory. Both capture pipelines are mandatory — the raw full-payload sweep is primary, the tshark field pipeline is required as well because client-to-server frames are XOR-masked. **REPORT 1 at 20:02**: pins re-verified, start-of-session recorded, four findings (see the top entry) — two of them false-pass traps rather than failures. Rig standing up now per PLAN-D §§3–4, pinned `v1.74.0`, scratch HOME/config/DB under /tmp, sealed rig plus legacy control rig, launched unsandboxed, `spawn-helper` chmod applied. Owner ruled: iPhone **13 Pro** (cabled) for G-1. G-2 parked on the user's Android decision. Owner has asked G to write findings 1, 2 and 4 into `PLAN-D` §14 before the next milestone commit, since the record carries only what exists. **20:12 — G-2 unparked.** Android device confirmed present; user chose a local SDK install over EAS and required build-file cleanup afterwards. Owner attached the pre-install state record, the smallest-install rule, the `443771a8`-in-the-artefact check, and teardown-as-comparison. Phone entries are the user's to clear and they are doing so; G writes taps for a clean pair and confirms the entries are gone as its first observation at the phone. Owner also required findings 1, 2 and 4 to be written into `PLAN-D` §14 before the next milestone commit — they currently exist only in G's report, and §14 exists because three earlier traps were not written down. **REPORT 2 at 20:30 — rig UP and isolated** (see the top entry for the fragmentation finding and the gate split). Owner corrected one of G's proposed §14 traps before it was recorded: `git grep <pat> origin/main -- 'src/*.ts'` returns real hits with a passing positive control, so the false negative G saw is almost certainly this machine's known unquoted-glob trap — the shell expanding the pattern against the working directory — rather than a property of `git grep`. Recording a wrong trap is worse than recording none, so G re-runs it quoted and records whichever actually reproduces. The interface and `mdfind` traps stand. Owner approved the controlled output generator via a `.zshrc` inside the scratch HOME, on G's own stated reasoning: real PTY, real ws-hub, real seal, real socket, only the content controlled — the same reasoning PLAN-D already applies to the unauthenticated agent — and it must be disclosed in the addendum, because a disclosed generator is a method and an undisclosed one is a confound. **02:52 — G-2 blocked on capture; re-scoped to prepare-and-hand-over.** Android device connected and authorised (`eb57e2b6`, `2109119DG` / `lisa_global`). NDK authorised by the owner on the user's standing instruction after G escalated it at ~5–5.5 GB with evidence (five modules declare `externalNativeBuild`, none ship prebuilt `.so`, so C++ compiles from source). Order set: build and install the Android artefact while the device is still attached, verifying `443771a8` in the artefact rather than the branch · write G-2 up as prepared-but-unrun · tear the rigs down against the start-of-session record · clean build artefacts, **never `~/.gradle`**, which was 6.6 GB before this session and belongs to the user's other projects · put the phone's now-stale server entry at the top of the runbook as step 0, since that exact condition made this session's pairing fail at the first tap. **PLAN-G2.md written 02:56**, with state re-verified rather than taken from the dispatch: Xiaomi `2109119DG`, Android 14, API 34, `arm64-v8a`; rigs alive at 1d06:38 uptime; `/dev/bpf0` still `root:wheel`, node re-created 02:44. Pre-install disk state recorded before anything was touched — `~/Library/Android` 8.0K with no `sdk/`, `~/.gradle` **6.6 G**, `~/.android` 6.7 M, 364 Gi free. The artefact check already passes on the JS half with two positive controls proving the grep can match in that bundle at all, against the real entry path (`/.expo/.virtual-metro-entry.bundle?platform=android`, 28.4 MB) rather than the 404-ing `/index.bundle`. G refused an available shortcut and said why: the Xiaomi already carries a debuggable dev client from 19 Aug that loads JS from Metro, but `package.json` moved under it (RN 0.86.3, Expo 57.0.18, gesture-handler 2.32→3.2.1 major, NativeWind v5, native liquid glass), and an August native shell under current JS fails quietly — the one shape this row cannot have. Caveat carried into the write-up: a debug build may permit cleartext through the debug network-security config, so this row does not re-verify #727's production fix. **03:00 — prep landed while blocked.** The sweep pipeline passed §14's known-answer test **to the digit** against the accepted `d2-sealed-rows-2-4.pcap`: 221,557 total `tcp.len` bytes, raw sweep 100.00 %, field pipeline 66.46 %, misses 33.54 %, with the opcode breakdown reproducing §14's correction (binary 128, ping 11, pong 11, close 12, **zero** opcode-0, largest WS frame 126 bytes). That is the property that matters most in this track: later coverage numbers are checkable against an agreed baseline rather than self-asserted, and the tooling is not certified by the run it certifies. The relay fallback is **confirmed feasible rather than merely offered** — `resolveServerUrl` honours `publicUrl`, so the rig can advertise any address and the relay enters the path without hand-editing a QR, and `spk` is host-independent. G also distinguished the three cases a previous session conflated: its own empty search was checked and found to be a **wrong path** (`src/cli/` does not exist on `origin/main`) rather than a broken tool or a real absence. **MILESTONE 2 at 03:10 — built, installed and PROVEN RUNNING on the Xiaomi.** APK 110,132,956 B, `lib/arm64-v8a/` the only ABI, versionCode 63, no bundled JS. Installed with `adb install -r` so existing app data was preserved. On-device confirmation rather than the installer's word: `dumpsys` reports versionCode 63, `primaryCpuAbi=arm64-v8a`. **The risk used to reject the shortcut is now retired by measurement rather than argument** — G launched it via the dev-client deep link against Metro and read the device's own log: process alive, `Running "main" with fabric:true`, boot line, 14 `ReactNativeJS` lines, zero fatals. That is what the 5.3 GB bought. Three carried findings: Android `versionCode` **is** readable off the device where iOS's Bundle Version is not, retiring half of §14 trap 6 for one platform; `adb shell curl` is a system binary outside the app's network-security config, so the LAN probe proves routing and **not** the app's cleartext policy (§14 entry 10, and the debug-build caveat stands); and §14 entry 9 — configuration succeeding proves nothing about the native path, since the JBR configured cleanly for 2m06 then failed every `configureCMakeDebug` at execution under JEP 472. Relay run failed at `fetch failed` before any handshake, so there is **no relay evidence yet**; `curl` through it returns 200, so it proxies. Reported separately so the stop-work is not misread as a relay result. **TRACK CLOSED 05:05 — stood down.** All five items done; see the top entry. Seven documents delivered and mirrored into the committed record: `PLAN-G2.md`, `PLAN-D.md` (§14 entries 7–12), `D2-REPORT.md` (ADDENDUM II appended, the signed body untouched), `G-PHONE-RUNBOOK.md` (step 0 first, Step A hardened), and three evidence files. Verified by the owner rather than taken on report: all seven exist, and the only differences between workspace and mirror are the scrub's own hostname substitutions — every §14 marker count matches exactly, so nothing of G's work was lost to a stale mirror. Runbook Step A now carries the bpf fact observed directly rather than inferred: the grant does not survive node recreation (`bpf0` re-created at 02:44 and again at 02:52, root-owned each time, *after* the command had been run), so it must run in the same sitting as the capture and then be proved. **§14 now opens with the family rather than its members** — ten distinct mechanisms, one signature, with the transferable habit stated above the list rather than left to be inferred from ten anecdotes. `PLAN-D.md` verified structurally intact at 556 lines with entries 7–14 and the header present and the tail undamaged. | 05:05 | — | G-2 needs a sitting with the device, the phone and the bpf grant |
| C1 — the retry loop (mobile defect 2) | `C1` (Opus 5 / high) | **DISPATCHED 19:52** — holds the mobile PR slot. Item 1 (a permanent verdict per server that a later `429` cannot reset) started immediately because it is caller-independent by construction and closes every candidate layer at once. Item 2 (naming the layer) waits on the client log G will capture; the owner relays it. Told explicitly that the obvious candidate is already eliminated — `ws-client.ts:231` returns without scheduling a reconnect and `sealedFetch` throws `retryable: false`, so both honour the classification — and that the client-log shipper is a hypothesis nobody has checked. `tracks/X-client/PROMPT-stop-polling-hard-refusal.md` is handed to it as evidence and a shape, not as an accepted specification. Negative control first: the storm must be **visible** before the fix suppresses it. **MILESTONE 20:20 — reproduction RED.** Worktree `../tb-mobile-worktrees/e2ee-permanent-refusal`, branch `fix/e2ee-permanent-refusal` off `26815a16`, own `npm ci` exit 0. Chokepoint identified at `services/e2ee/context.ts::openContext`; `mapOpenFailure` deliberately untouched, because the `429` → `E2EE_TRANSIENT` mapping is correct in itself and what was wrong is that it could overwrite a verdict already reached. Seven rules (record · enforce · retryable-never-clears · clear-on-pin-change · clear-on-success · clear-on-re-pair via `stores/servers.ts` `addServer` · clear-on-explicit-retry via `hooks/useSession.ts` `retryFailed`), with four mutations planned. Committed suite `__tests__/unit/e2ee-open-refusal-verdict.test.ts`, 16 tests in five groups. Seam disclosed and accepted: `createOpenInitiator` is mocked as the keystore boundary per existing precedent, not the refusal→verdict transition under test. Owner ruled the verdict uniform across non-retryable codes and required two additions — a test proving the Retry affordance actually clears a standing `E2EE_HANDSHAKE_FAILED` verdict, so "one tap" is a tested fact rather than a claim, and the deleted negative-control run recorded verbatim in the findings note, since the record carries only what exists. **MILESTONE 20:52 — item 1 complete, user-approved, PR opening.** Branch `fix/e2ee-permanent-refusal` off `26815a16`. See the top entry for the diff shape, the gates, the five red mutations and the two volunteered disclosures. Owner required one further property in the findings doc: the verdict map is per-process, so an app restart forgets it — correct behaviour worth stating deliberately rather than leaving as an accident, since exactly one request per launch earns the 403 back and one request is not a storm. PR body must name the D2 evidence, state the uniform-verdict trade and its one-Retry-tap cost rather than leaving it for review to discover, and say plainly that item 2 remains open so nobody reads this as the whole fix. Documents delivered: `tracks/X-client/PROMPT-stop-polling-hard-refusal.md` rewritten from hypothesis into the record of what was found, with the elimination table (`ws-client.ts:229-231` and `sealedFetch` ruled out with code quoted; close-time path and 45 s timer cadence-mismatched; the client-log shipper still explicitly a hypothesis nobody has checked, marked "do not implement against it"), plus the new `tracks/X-client/FINDINGS-C1-permanent-refusal.md`. **TRACK CLOSED 02:35 — MERGED as `c64fab5e`.** Rebased onto `59a4d04f`, diff byte-identical, CI 23/23 on the rebased head. Final milestone state: reproduction RED (observed executing against unmodified `main`, not argued for) · item 1 **merged** · the layer recorded **UNKNOWN** with the elimination evidence in `tracks/X-client/PROMPT-stop-polling-hard-refusal.md` · item 2 **deferred**, held on a client-log capture that does not exist yet because G's phone trip has not run. | 02:35 | — | item 2 deferred: needs G's client log, which needs the phone trip, which needs the bpf grant |
| C2 — the silence timer and the pending-prompt guard (mobile defects 3 and 4) | not yet dispatched (Sonnet 5 / medium) | **HELD by design.** Not started, and will not be, until C1's PR merges — they share the one mobile PR slot and a branch opened early only rots behind a rebase. Its two defects are documented on mobile `main` in `docs/e2ee-client.md`; neither issue is filed yet, and filing them is C2's first act. Neither fix may be "raise the server limit" — the 62 % budget burn measured on 2026-09-02 is the client spending its allowance on nothing, and the limiter is doing its job. **Scope grew 20:35: defect 5 added** — the fragmented-frame receive test, which is G-1's second half, routed here because it is mobile work and G is evidence-only. Its brief now carries why the capture route is closed on principle rather than by effort, the two facts that must appear in the PR body so a future reader does not delete the test as redundant, and the requirement that the mishandling case be shown red first — a fragmentation test that never fails proves only that the harness runs. This item gates the **stage-2 flip**, not R2. **DISPATCHED 02:36 on Fable 5.1.** Started before the merge on the one task needing no slot — filing the two issues — then released to branch once #942 landed. Told explicitly that `ws-client.forceReconnect` was deliberately left untouched by C1 (it fires on every foreground *and* on the 45 s timer, so clearing a verdict there would rebuild the storm), and that defect 3 is the next thing to touch that timer, so it inherits that as a constraint rather than a decision to revisit. **MILESTONE 02:48 — defect 3's mobile half staged**, worktree `../tb-mobile-worktrees/e2ee-silence-prompt` off `c64fab5e`. Four files, +34/−3, **no production logic change**: a `ping` entry in the `WSMessage` union, a comment on why "any kind" is load-bearing, a new watchdog test, and a `docs/e2ee-client.md` correction — that file currently prescribes a client change which shipped in June. Watchdog 7/7, tsc 0, lint 0. **Its M2 mutation is the best test reasoning in the program so far**: keeping the wildcard subscription but adding `if (msg.type !== 'ping')` — the exact optimise-it-away edit `NONCE-DESIGN` §18 warns about — turns *exactly one* test red, the new one, while the pre-existing test stays green. That does not merely kill a mutant; it demonstrates the new test is not redundant with the old one, which is the question a reviewer would actually ask. Mutations restored from file backups rather than `git checkout`, which is not hypothetical: C1 wiped its own uncommitted fix that way two days ago. **TRACK CLOSED 05:04 — PR #948 MERGED as `69083367`**, 24/24 green, `MERGEABLE/CLEAN`, zero commits behind main so no rebase was needed. Two commits: the ping contract pin plus docs correction, and the Send-Escape affordance. Pre-commit hook disclosed verbatim, and C2 corrected its own earlier disclosure when the hook named a **second** story-less component (`TerminalView.tsx`) it had not previously mentioned — both are now in the PR body's "Skipped in the open" section. Its PR body states in bold that this PR does **not** reduce the idle churn, that the client behaviour shipped in June, and what changes once a streamer carrying #756 runs. | 05:04 | — | none — track complete |
| P — mobile #946's streamer half (the app-level ping the streamer documents but never sends) | `P` (Opus 5 / high) | **DISPATCHED 02:46** at the user's request to fix P1 #946. The substantive fix is **not** a mobile change: the client has reset its silence timer on any inbound frame since June 2026, and the streamer sends an idle client nothing JavaScript can observe — its only liveness signal is a WebSocket **protocol** ping at `ws-hub.ts:488`, invisible below `onmessage`. `{ type: "ping"; ts: number }` is declared at `src/types.ts:339` and `ws-hub.ts:257` carries a comment describing that frame as "sealed here like every other frame", but **nothing in `src` ever constructs one** — verified by the owner with a working positive control after a first grep form came back blind for both the target and the control. Opus tier because the diff is small but sits on the program's most sensitive invariant: a sealed ping consumes a server→client nonce counter every 30 s per client forever, and `ws-hub.ts:243` warns in the file itself that an await between `seal` and `ws.send` reorders frames — a periodic timer firing alongside normal traffic is exactly that shape. Counter-and-ordering analysis is required **before** any code, and a change reaching into the record layer rather than its caller stops for §4's adversarial verifier. | 02:46 | the counter-and-ordering analysis, before code | streamer PR slot held by R |
| R — negotiated rollout (R1 `--no-e2ee`; R2 D-8 escalation; R3 stage-2 PR opened, never merged by R) | `e2ee-owner` | **R1 SHIPPED** in streamer `v1.74.0` (PR #752). **D2 accepted 2026-09-02 19:04**, so that precondition is met — but two named gates now replace it: **G-1 continuation frames** (a large-payload iOS row; client-side fragmentation is untested on hardware) blocks **R2**, and **G-2 Android** (row 1 + the canary on Android) blocks the **stage-2 flip**. Both run in one rig session with one §14-compliant control. Stage-2 default flip also remains blocked on the user's explicit go and on export compliance. **DISPATCHED 19:52** as `R` (Sonnet 5 / medium): drafting the R2 escalation now into `tracks/R/PLAN-R2.md`, with presentation held until the owner clears G-1. Told in the strongest terms that R2 is the user's decision — three options with their real costs and then stop, the reviewer's option-3 preference recorded as the reviewer's and not as R's. R3 may be **opened** only after G-2 and is never merged by R under any phrasing. Streamer slot confirmed free (only bot PRs #753/#754); the owner's `docs/e2ee-program-record-updates` worktree is off limits to it. **MILESTONE 20:03 — escalation drafted**: `tracks/R/PLAN-R2.md` (12 KB), three options with code/test costs verified against `origin/main`, no recommendation of R's own, the reviewer's option-3 preference recorded as a labelled data point quoted from `REVIEW-2026-08-29-mega-brain.md:173`. Cites the precedence test `__tests__/e2ee-no-e2ee-flag.test.ts:96` and distinguishes options that flip its assertion (option 1) from those that only stale its comment (option 3). Correctly holding presentation — it checked STATUS rather than trusting the dispatch message. Owner asked for one reframing (the silent env/yaml path is context, not an argument) plus two additions: what a stage-1 user relying on `THREADBASE_FEATURE_E2EE=1` experiences under each option, and the cost of deferring twice. **20:18 — all three owner additions landed**: the silent-path reframing lifted into its own section before the options and explicitly marked as context rather than an argument; per-option operator impact for someone relying on `THREADBASE_FEATURE_E2EE=1` today; and a deferral cost curve covering stage 2 **and** stage 3, stated as the curve's shape rather than a case for deciding sooner. R re-read the file and confirms no recommendation of its own appears in it. Still holding. **R2 DECIDED 02:26 and re-dispatched 02:36 on Fable 5.1.** The decision is recorded verbatim in `tracks/R/PLAN-R2.md`; a first Sonnet-tier session recorded it and re-verified the mobile citations before being stopped for the model swap, and that work was kept rather than redone. Scope: `dilemmas.md` D-8 resolution note plus `design.md` §6.4/§6.5 (documentation), and code at `src/server.ts:2406` (guard broadens off the `cli` rung, method renamed off "OnTheCommandLine", `reason` carries the real source) and `misc.routes.ts:205-211` (`disabledReason()` names the actual rung). The existing pinned-count machinery at `:2415` is reused, not rebuilt. One design question was handed to R with the owner's reading and an instruction to argue if it disagrees: a strictly consequence-gated trigger would *remove* the warning that fires today at zero pinned devices, so the owner's reading is to keep warning on any explicit off-source and let the count enrich the text. `e2ee-no-e2ee-flag.test.ts:96`'s assertion is unchanged; only its stale comment moves. **MILESTONE 03:20 — staged, six mutations red, gates green, commit APPROVED by the owner subject to a lint re-check.** 5 files, +169/−25. **R found a real harness defect, and found it the only way it could be found.** The boot tests never sandboxed `THREADBASE_CONFIG_DIR`, so `loadFeatureFlags()` read the developer's real `~/.threadbase/server.yaml`, which carries `e2ee: true` on this machine — every plain `boot(undefined)` resolved e2ee **on** from the yaml rung, so both the `origin/main` positive control and the new default-rung negative control were passing for a reason unrelated to what they claimed to test. Two controls, both green, both hollow. It surfaced because **M3 stayed green when it should have died**: a passing suite cannot report that it is testing the wrong thing, and only a mutation that fails to kill can. Fixed with a per-test config dir plus a positive control that writes `{"e2ee":true}`, asserts the production loader returns exactly that, boots clean, then rewrites the same file to false and reboots to a `reason: "yaml"` warning — only the file content varying. Recorded as a local trap rather than a CI defect, with the note that other files booting a `StreamerServer` inherit it. The owner's catch is fixed via a shared `E2EE_OFF_SWITCH` table: operator wording at the human surface, bare rung in the JSON `reason`, the cli `/api/info` string byte-identical to `origin/main`, and M6 pinning it so nobody regresses to printing a rung name at a person. The anchoring check came back checkable rather than reassuring — a count-gated diff would have had to delete an existing `origin/main` test R says it would have flagged on sight. **R2 SHIPPED 05:16 — #758 MERGED as `04090873`.** Re-run 11/11 on the unchanged commit. What landed: the boot warning no longer fires only for the CLI rung — any non-default source that disables encryption announces itself **in the operator's own words**, and `/api/info` names the deciding rung, sharing one `E2EE_OFF_SWITCH` table between both surfaces while the JSON `reason` keeps the bare rung for machine readers. `src/feature-flags.ts` untouched, so the registry stays uniform; D-8's mechanism rule retired on client-pin evidence and replaced by a consequence rule; `design.md` §6.5's false precedence sentence corrected as its own line in the PR body. No `Closes` keyword, deliberately — R2 is an item on the #590 worklist, not a whole issue. **R3 remains neither merged nor opened.** Next: #759 after P lands. **R2 RELEASED 05:33 — `v1.75.0` (`ebb75a33`) contains `04090873`**, verified by the owner from the remote rather than taken from the report. Per program rule §5, children key off release tags rather than merges, so R2 is now a consumable artefact. R holds for #759 until #761 merges. | 05:33 | #759 after #761 merges | none — R2 complete; R3 gated on G-2, export compliance, and the user's explicit go |

## Caveats on this record's own evidence

Things a later reader could honestly and wrongly cite. Written down because a caveat that is not recorded becomes a claim somebody makes in good faith.

- **"Lint was green locally" is not available as evidence for any change developed in a streamer worktree.** `npm run lint` is a silent no-op inside `tb-streamer/.worktrees/` — biome honours `.gitignore`, which excludes that directory, so it reports checking zero files and exits successfully. Several already-merged PRs in this program carry a local lint result that never ran. This is **not** a correctness problem: CI lints from the repo root and that is the gate every one of them actually passed. But the local green does not exist. Working form: `npx biome check --vcs-enabled=false src cli __tests__`.
- **Mutation reports written before 2026-09-04 ~06:00 IDT are red-only.** They show tests failing under mutation but carry no same-session unmutated baseline, so they cannot by themselves distinguish "the mutation worked" from "something was already broken and everything was red." Later campaigns in this program run a baseline first. The earlier results are not thereby wrong, but they are weaker than they appear.
- **A merge commit is not the same fact as code being in the tree.** Where this record claims something shipped, prefer the tree check over the merge SHA. Both halves of the ping contract were verified this way in `origin/main` with a working control.
- **Two nondeterministic tests were live in the streamer suite throughout this program** (#759, #762). Any single red on `Smoke (macos-latest)` in this window should be checked against those two before being attributed to a change.


## Definition of done — audited from the remote, 2026-09-04 ~05:25 IDT

Verified against GitHub and the workspace documents rather than against this file's own narrative, because STATUS is the document most likely to flatter the work it records.

| # | Criterion (from `finish-2026-09-02/PLAN.md`) | Verdict |
|---|---|---|
| 1 | C1 item 1 merged; item 2 merged **or** explicitly deferred with the layer named unknown | **MET** — #942 merged `c64fab5e`; item 2 deferred, layer recorded UNKNOWN with the elimination evidence |
| 2 | C2 merged, **or** its defects filed as issues with the analysis attached | **MET** — #948 merged `69083367`; four issues filed (#946, #947, #756, #757), #947 closed |
| 3 | R2 presented and the user's choice recorded in the decisions log | **MET** — decision recorded verbatim (*"hand-off to proceed with 2+4"*); R2 code merged `04090873` |
| 4 | G-1 **and G-2** cleared on the R row, with evidence | **NOT MET** — G-1 cleared on four legs; **G-2 not cleared**: no capture, no canary, no Android ciphertext claim |
| 5 | R3 opened, green, and **not** merged | **NOT MET** — R3 **not opened**; its gate is G-2, which did not land |
| 6 | `STATUS.md` current, and a close-out note added to the streamer record | **PARTIAL** — STATUS current; close-out note pending until #759 lands |

**Both unmet items have one root cause and it is not execution.** G-2 needs a packet capture; the capture needs a `sudo` grant on `/dev/bpf*`; the grant did not survive macOS re-creating the device nodes and could not be re-run with the user asleep. R3's gate is G-2. So a single unavailable human action accounts for both, and no amount of agent effort substitutes for it.

**What was done instead of nothing:** G-2 is prepared to the point where the remaining work is one sitting — the app is built from `c64fab5e` and installed on the Xiaomi at versionCode 63, the artefact verified with controls and observed running, the method settled, the runbook carrying exact commands and the interface-derivation procedure. The row is written up as *prepared-and-unrun* with "no Android ciphertext claim, the stage-2 flip is not cleared" stated flatly rather than softened.

**R3 not being opened is the correct outcome, not a shortfall.** Opening it would have required either its gate to be met or the gate to be waived, and a blanket "proceed without asking" is not the explicit go in the user's own words that the flip requires.

## Outside every track (the user's)

- **Export compliance (review H3 — a present exposure, not a stage-2 gate):** every build since 204 ships `@stablelib` X25519/ChaCha20-Poly1305/SHA-256/HMAC under `ITSAppUsesNonExemptEncryption: false` (reverted by #862 on 08-25 after ASC rejected build 205). The declaration is false today, whatever the `e2ee` flag does; the ANSSI → Apple approval (and the BIS self-classification) is on the critical path for *any* release, and R's stage 2 additionally needs an E2EE build in testers' hands.
- **#760** — human review of the Hebrew, Russian and Arabic encryption copy.

## Decisions log

- 2026-09-04 ~07:50 IDT — Closed the programme's code work with two of six criteria unmet, and recorded that plainly rather than letting seven merged pull requests imply completion.
The Android row has no capture, no canary and no ciphertext claim, so the staged default flip is still ungated and was never opened.
Both gaps trace to a single unavailable human action rather than to execution, and the row is prepared to the point where the remainder is one sitting with the devices.

- 2026-09-04 ~07:50 IDT — Two nondeterministic tests unrelated to this programme were found and fixed the same day.
Both surfaced only because the programme merged often enough to roll the dice repeatedly, and both sit in tests whose purpose is proving nothing leaks, so each spurious red read as a security regression on first sight.

- 2026-09-04 ~08:45 IDT — Installed a persistent capture-permission daemon at the user's instruction and verified it by capturing rather than by reading permissions.
Checking that the device files carry the right group would have proved only that a `chgrp` ran; running an actual three-packet capture without privilege proves the thing that matters.
The decisive observation is that a device node recreated after the install came back with the correct group, since node recreation is precisely what destroyed the manual grant twice the previous night.
One expectation was wrong and is recorded as such: group membership was live in an existing shell without a re-login.
The superseded manual command was struck at the top of both the handover and the runbook rather than merely omitted, because a runbook that still instructs a dead step is worse than one that never mentioned it.

- 2026-09-04 ~05:35 IDT — Left two pull requests open in one repository rather than unwinding a crossing, and recorded why the rule was not the point.
The hold and the conditional-go push crossed, so the second pull request existed before the instruction meant to prevent it arrived.
The diffs are disjoint, documentation against one source file and its test, so the interaction the rule protects against cannot occur, and drafting or closing would have discarded an in-flight verification run for no gain.
The agent surfaced it immediately and declined to draft or close on its own judgement, on the grounds that the action is outward-facing even though it is cheap and reversible.

- 2026-09-04 ~05:35 IDT — Recorded that the programme's one-pull-request rule has always meant one human pull request, since bot-authored ones were open throughout.
As written the rule was in continuous violation while the hazard it names never occurred, and stating that prevents a later reader from treating a night of dependency bumps as evidence the rule was ignored.

- 2026-09-04 ~05:20 IDT — Recorded a suite failure as this owner's orchestration error after the agent that hit it attributed the cause to itself.
Four agents were dispatched onto one machine and left running full suites in parallel in sibling worktrees of the same repository, each standing up real loopback servers, reaching ten concurrent test processes.
The resulting port collision failed a test connected to neither change and named it confidently, which is the same signature as the rest of the night's family.
The agent's own account was generous and the record says what actually happened instead, because a process note that blames the wrong layer teaches the next agent to look in the wrong place.

- 2026-09-04 ~05:20 IDT — Merged the rollout change after a re-run passed eleven of eleven on the identical commit, confirming the earlier diagnosis by behaviour as well as by reading.
The prior occurrence found in continuous-integration history, on a different operating system two days before the branch existed, is what closed the question; the re-run only agreed with it.
A local loop returning no failures in twenty-five attempts was correctly declined as evidence, since a one-to-two percent per-run probability predicts exactly that and an underpowered null is consistent with a claim without supporting it.

- 2026-09-04 ~05:30 IDT — Merged the mobile pull request and then closed its issue by hand, having found that a squash merge ignores closing keywords written in commit bodies.
The keyword lived in the second commit's message, which the squash discarded, so the issue stayed open despite the work landing.
Recorded because every future squash-merged pull request in these repositories inherits the same trap, and the fix is to put the keyword in the pull request body.

- 2026-09-04 ~05:30 IDT — Refused to merge a pull request with a failing check, and refused the two shortcuts that would have made it green.
Re-running the job until it passes would say nothing about whether the failure was real, and reproducing locally first cannot distinguish the two hypotheses because this machine carries the stale dependency link that one of them blames.
The leading hypothesis is that broadening the boot warning's trigger made it fire in a smoke test that reads boot output, which would be a genuine defect rather than an environmental one.
The alternative hypothesis is falsifiable in a useful direction: continuous integration installs from the lockfile, so if the known local failure appears there too, the diagnosis recorded against it is wrong.

- 2026-09-04 ~05:20 IDT — Corrected a closure this record had already published: the remedy that closed the credential stop-work had not in fact happened when it was reported.
Killing the rig's processes and confirming its ports free is not destroying the rig, and the scratch home directory survived with the API key, the runtime database and the server's static identity private key inside it.
The more sensitive artefact was the identity key, which nobody had raised an alarm about, and it stood for about two hours after this record described the rig as gone.
What surfaced it was the requirement attached to the ruling that the remedy be reported as a measurement rather than an assurance, which is the difference between a remedy and a claim about one.

- 2026-09-04 ~05:20 IDT — Declined to authorise an emulator after its cost was enumerated rather than assumed, and sent it to the handover as a costed option instead.
It is three to four gigabytes beyond a ceiling the user set while they are unreachable, and it is now a rebuild rather than a download because the environment it needs was destroyed on this orchestrator's own teardown instruction.
The evidence it would produce is real but bounded to client behaviour, and the user can obtain strictly better evidence on physical hardware within a day.
The guess that an image might already be on disk was a hope, and the agent tested it with a control rather than inheriting it.

- 2026-09-04 ~05:10 IDT — Declined to rewrite public history over a hostname exposure and escalated it to the user instead, recording the reasoning so the decision can be judged rather than assumed.
The identifiers are on the default branch of a public repository, where the rules forbid force-pushing without an exception for orchestrator judgement, and the earlier rewrite tonight was of a branch this session owned with no pull request against it.
The same identifiers already appear in that repository's own instructions and deployment skills, predating this programme entirely, so scrubbing eleven programme files would leave the hostname in place while costing a rewrite of a signed accepted document.
Whether the user treats their own hostname as public is a fact about their intent that cannot be read off the filesystem, which makes it theirs to decide rather than mine.
The mirror already prevents new instances, leaving a visible inconsistency between older and newer documents that is the correct state until they rule.

- 2026-09-04 ~05:10 IDT — Named the family rather than only its members, after a fifth distinct mechanism produced the same symptom in one night.
A regular expression dialect silently ignoring an escape, a linter honouring an ignore file over a mandated directory, a log nothing had ever connected to, a control searching for a word that was not present, and a shell that does not split an unquoted variable.
Five causes with one signature: an empty result that reads as an answer, and in three of the five the control failed identically to the check, which is what made the failure look like a finding.

- 2026-09-04 ~05:00 IDT — Struck through a superseded claim in place rather than only appending its correction, after an agent pointed out the owner had restated a mechanism it had already disproved.
The dead version had reached the public branch before the retraction arrived, so the record held both with nothing marking which was live, and a reader stopping at the first would have taken the wrong mechanism away.
Appending a correction is sufficient for a log and insufficient for a record people read in pieces.

- 2026-09-04 ~05:00 IDT — Accepted an agent's correction to an owner instruction that was not runnable, and declined the workaround it offered against itself.
Rebasing before committing cannot work while automatic stashing is disabled, and that refusal is the configured behaviour working rather than an obstacle to route around.
The agent named a stash-based alternative and recommended against it; that recommendation was taken, because putting a whole change through a stash to satisfy an ordering preference is churn when a commit will do.
The safeguard that makes the revised order equivalent is that a rebase changing any line of the diff requires fresh approval rather than riding the old one.

- 2026-09-04 ~03:35 IDT — Retracted a lesson this record had already published, after its author re-tested and found the mechanism wrong.
The claim that a log file existing is not a log file recording was false: the bundler was recording, and the file was empty only because no client had ever connected to it.
The retraction matters more than the original entry, because the published version would have sent the next session to replace a tool that works, which is a worse outcome than the gap it was written to close.
The corrected rule is firmer: before grepping any log for the absence of a behaviour, establish that the subject was connected to that log during the window, since a log that never saw its subject is indistinguishable from the behaviour not happening.

- 2026-09-04 ~03:35 IDT — Treated a linter finding as invalidating prior evidence rather than as a bug to fix going forward.
The linter honours the ignore file, the ignore file excludes the worktree directory, and the programme mandates that directory for all streamer work, so the instrument was blind in exactly the place the rules require people to stand.
It exits zero and reports checking nothing, which is the worst failure mode available: it does not error, it agrees with you.
Any streamer track in this programme that reported a green lint from a worktree path did not lint, and that reaches back over several already-merged pull requests.

- 2026-09-04 ~03:35 IDT — Ruled a reported credential exposure contained, chose destruction over rotation, and declined to wake the user.
The credential reaches one isolated scratch process, has no path to anything the user owns, touched no file or repository, and dies with a teardown already scheduled this session.
Destroying the rig removes what the key opens rather than merely the key, which is the stronger remedy, but two verifications were added so that the remedy is reported as a fact rather than a promise.
Waking someone unreachable at three in the morning is warranted when an exposure is live, widening, or theirs to contain, and this is none of the three.
The general trap is worth more than the incident: a command-line argument is not a private channel, and the caller's own discipline about never printing a secret is irrelevant when a process launcher echoes the line.

- 2026-09-04 ~03:05 IDT — Traced a silent search failure to its mechanism and recorded it as a method trap, correcting one of this owner's own diagnoses.
`git grep -E` is POSIX ERE and does not support `\s`, so the pattern matched nothing and its positive control failed identically, which is what let the emptiness look like an answer.
The owner had concluded the form was blind and moved on, which was right for the wrong reason, and a right conclusion for the wrong reason is how a method note comes to teach the wrong lesson — a retracted trap earlier in this same session had exactly that shape.

- 2026-09-04 ~03:05 IDT — Ruled that §4's isolated adversarial verifier is not triggered by the ping change, and recorded the reasoning rather than the ruling alone.
The diff is confined to the hub, the record layer and context are untouched, and the change routes traffic through the record layer rather than altering it.
The rekey concern is structurally void rather than small, because in-place rekey was abolished by the user's own earlier ruling and the code carries only comments asserting its absence.

- 2026-09-04 ~03:05 IDT — Required an analysis of a property the fix creates that nobody had raised: a regular, predictable ciphertext emission on an idle sealed socket.
It is not expected to be a weakness, but a reviewer should be able to see it was considered rather than work out for themselves that it was, and the note must say what an on-path observer newly learns.

- 2026-09-04 ~03:05 IDT — Corrected the phone runbook immediately rather than deferring it, after the interface hazard turned out to be a choice the rig makes rather than an ambiguity it sits in.
The runbook stated one interface as a fact when it is only today's enumeration outcome, and a document that can be followed incorrectly by someone who never reads the method section is a trap rather than a runbook.
The worst case is a VPN interface winning the enumeration, which would put device traffic outside the local network the ciphertext claims are scoped to, making it a scope violation rather than a missed capture.

- 2026-09-04 ~02:58 IDT — A client-log source this owner had relayed to another agent as fact turned out to be empty, and was caught before it produced a conclusion.
The Metro log named as C1's client log holds 735 bytes of startup banner and has recorded nothing for sixteen hours, so a cadence grep against it would have returned nothing and been read as the storm failing to reproduce.
The replacement is `adb logcat`, proven with a non-zero positive control, needing no taps and timestamping at OS level.
The general lesson is recorded for §14: a log file that exists is not a log file that is recording, and the check is a byte count and a positive control rather than the file's presence.

- 2026-09-04 ~02:58 IDT — Accepted an ordering constraint that had gone unstated and took the hardware negative control it makes available.
Because the merged fix is what stops the storm, reproducing the storm requires pre-fix JavaScript, and a dev client loads its bundle from whichever Metro it is pointed at, so one native shell serves both phases.
Running pre-fix then post-fix on the same device and rig makes the pair a negative control on a fix that merged tonight with no device evidence behind it, at the cost of one extra bundler.

- 2026-09-04 ~02:58 IDT — Authorised a no-root capture fallback as a hedge while keeping the permission grant preferred, and required the fallback be validated without a human before it is ever needed.
The grant observes the LAN segment that every other ciphertext claim in the accepted report rests on, whereas the relay observes a relay hop and therefore proves what leaves the device rather than what crosses the segment.
Its compensating strength is complete payload capture in both directions with no dissection gap, and mixing methods across rows is a readability cost rather than a correctness one.
It exists because the grant has now failed to land twice, and a third discovery should not happen with the user standing at the phone.

- 2026-09-04 ~02:49 IDT — The user delegated commit and PR approval to the owner for the remainder of the run and left for at least sixteen hours.
Planning assumes the longer reading of that absence, because every decision that differs between sixteen hours and sixteen days costs less when taken conservatively.
The delegation was treated as moving approval rather than removing review: agents still present a staged diff and a verbatim message, and the owner still reads the diff rather than the summary.

- 2026-09-04 ~02:49 IDT — R3 is held both unmerged and **unopened** despite the blanket approval, and no feature-flag default flips.
Its gate is G-2, G-2 needs a capture, and the capture needs a grant that will not arrive, so the gate is simply unmet.
Independently of that, a blanket "proceed without asking" is not the explicit go in the user's own words that the stage-2 flip requires, and it closely resembles the "owner relays enthusiasm" case R's brief instructs it to refuse.

- 2026-09-04 ~02:50 IDT — Caught a cross-agent wire-contract risk before either half landed, and froze the shape.
C2 had already staged the ping frame into the mobile message union with a test pinning it, while P was about to design the same frame in the streamer, neither agent aware of the other.
The shape is fixed at the existing declaration and the 30 s cadence is load-bearing on both sides, with any change routed through the owner rather than chosen by either agent.
This is the same mismatch class that already cost the program a day, since defect 1 was a delivery-shape difference on this exact receive path.

- 2026-09-04 ~02:52 IDT — Chose teardown over holding the rigs, against the original stand-it-up-once policy.
That policy was written for a single continuous session; across a gap of a day or more an idle scratch rig is a liability, and the documented rebuild is about ten minutes.
The consequence is recorded rather than left implicit: the phone keeps a stale server entry pointing at a rig that no longer exists, which is exactly the condition that made this session's pairing fail at its first tap.

- 2026-09-04 ~02:26 IDT — User decided R2 as "2+4": option 2 as the disposition and a new option 4 as the replacement rule, with option 5 considered and dropped.
Option 4 was not among the three options R drafted; it came out of a brainstorm grounded in the mobile client's pinning behaviour, which showed the feared harm is largely unreachable through the mechanism D-8 forbids.
Dropping option 5 also drops its prerequisite: option 5 would have made the pinned-device count load-bearing, and that count deliberately includes rows whose pairing never completed (#744), so a ghost row could have locked an operator out of disabling.
Under 2+4 the count stays advisory, which is the regime it was designed for, and that is a large part of why the combination is cheap.

- 2026-09-04 ~02:35 IDT — Merged #942 after rebasing onto latest `main`, and recorded what was not done rather than implying a full re-verification.
The five mutations were not re-run locally after the rebase because the sub-agents did not survive an overnight session resume and C1 was gone.
The two commits `main` gained have zero file overlap with the change and CI re-ran the full suite on the rebased head at 23/23, which is the real gate but is weaker than a local mutation re-run.
Stating the gap is the point: a merge justified by a summary is exactly the shape of evidence this program keeps having to correct.

- 2026-09-04 ~02:36 IDT — At the user's request R and C2 were re-dispatched on Fable 5.1, replacing the Sonnet-tier assignments, as a bounded trial on well-specified work.
The orchestrator seat stays on Opus 5 because the remaining risk is not generation but disbelief: five false-passes-in-waiting were caught in two days, and G-2 is the one row left that can certify nothing while looking green.
The stopped Sonnet R session had already recorded the decision and re-verified the mobile citations, and that work was preserved rather than redone.

- 2026-09-02 ~20:52 IDT — User approved C1's item-1 commit and the opening of the mobile PR, and independently confirmed the uniform verdict scope the owner had already ruled.
The approval followed a review of the actual diff rather than of C1's summary, with two of its claims re-checked against `origin/main`.

- 2026-09-02 ~20:52 IDT — A dispatch instruction written by the owner was found to be wrong by the control it mandated.
The brief named `E2EE_CTX_UNKNOWN` as a retryable `/open` refusal for the positive control; `mapOpenFailure` has no branch for it, so a 409 at `/open` falls through to a non-retryable `E2EE_HANDSHAKE_FAILED`, and the code is produced only at `authed-fetch.ts:383` for a sealed request.
This is the positive-control rule paying for itself against the instructions rather than against the implementation, which is the case it is least often expected to catch.
C1 corrected the control to a 5xx and pinned the 409 reading with a second test, so the ambiguity is nailed down rather than routed around.

- 2026-09-02 ~20:52 IDT — Mutation M2 initially came back green, which was the campaign working rather than failing.
The race test asserted on the error code, and the mutated build re-reached a server still answering 403, re-earned the verdict and returned an identical code, so the test could not fail.
Re-cutting the assertion onto requests reaching the server is the correct fix rather than merely a convenient one, because requests reaching the server is what a storm physically consists of and the error code was only a proxy for it.

- 2026-09-02 ~20:52 IDT — C1 disclosed reverting a mutation with `git checkout --` and wiping its own uncommitted fix, then recovering within one step when the next patch anchor failed.
Every number in the mutation table was re-measured afterwards against a re-confirmed green baseline, which makes this a recovered process error rather than a compromised result.
It is recorded because a disclosed slip is what makes the surrounding evidence trustworthy, and an undisclosed one would have made all of it unverifiable.

- 2026-09-02 ~20:35 IDT — Split G-1 rather than holding it, after independently verifying that the streamer cannot emit a WebSocket continuation frame at any payload size.
`src/ws-hub.ts:299` is the only server-to-client send path and passes no options, and `ws` 8.21.3's `send()` hardcodes `fin: true`, so the row as written could never close on evidence that meant anything.
A gate that cannot be satisfied is not a high bar but a broken instrument, so the capture half now clears R2 and the client-side fragmentation test — the only strong evidence about the client — moves to C2 and gates the stage-2 flip instead.
This is recorded as a deliberate change to the gate, not a reinterpretation of it, so a later reader can hold the decision to account rather than discovering the bar moved quietly.

- 2026-09-02 ~20:35 IDT — Required G to run the large-payload capture anyway, despite the dependency read already settling what the server does.
A dependency read establishes what the code should do and a capture establishes what the wire did, and the divergence of those two is the reason this program exists.

- 2026-09-02 ~20:35 IDT — Rejected one of G's proposed §14 traps before it was recorded, having reproduced the opposite result.
`git grep` against `origin/main` with a quoted pathspec returns real hits under a passing positive control, so the clean false negative G hit is almost certainly this machine's known unquoted-glob trap, where the shell expands the pattern against the working directory instead of the tree.
The false-negative shape G identified is real and the cause is the shell rather than the tool, and a trap recorded with the wrong cause teaches the next session to avoid a working tool while leaving the actual hazard in place.

- 2026-09-02 ~20:24 IDT — Ruled that C1's permanent verdict stays uniform across all non-retryable codes rather than being narrowed to `E2EE_DEVICE_REVOKED`.
The decisive fact is that `E2EE_HANDSHAKE_FAILED` is the code behind the storm that occurred unprompted in the field, so narrowing would have fixed the case produced deliberately in a lab and missed the case that happened on its own.
Fewer branches was a second reason and would not have been sufficient alone.
The accepted cost is that a mispinned device stops self-healing and needs one Retry tap, which C1 surfaced rather than buried: through a tunnel every device collapses to `127.0.0.1` and shares one failure budget, so a self-healing storm is not a device-local kindness but a cost paid by every other device on that server.
Reversal is a one-line change if the trade proves wrong in the field.

- 2026-09-02 ~20:24 IDT — Required that "recovery is one Retry tap" be proven by a test rather than asserted, since the entire stickiness trade rests on the user having a reachable way out.
If a standing verdict ever produced a state with no Retry affordance the trade would silently become "the device is stuck", and nobody would discover it until a user did.

- 2026-09-02 ~20:24 IDT — C1's negative control was executed against unmodified `origin/main` rather than delivered as a mutation or an argument, which is the requirement met literally and better than the fresh-`serverId` workaround originally planned.
The alternative would have needed a test-only disable flag, which is a stubbed seam on precisely the transition under test and is what §4 forbids.
Because the temporary file was deleted after the run, the observation was ordered into the findings note verbatim, with mutation M1 named as its permanent successor — an observation that survives only in a message is one a later reader cannot distinguish from a reasoned claim.

- 2026-09-02 ~20:12 IDT — User confirmed an Android device is on hand and chose a local Android SDK install over an EAS cloud build, with the condition that build files are cleaned up after the tests.
That condition was converted into a verifiable teardown step rather than left as an intention: the pre-install state of `~/Library/Android`, `~/.gradle` and `~/.android` is recorded **before** the install, and teardown is reported as a comparison against that record.
A state that was never recorded cannot be restored, which is the same reasoning behind the port and process record G already took at session start.
The install is scoped to the platform and build-tools the project actually pins rather than a default full SDK, with no emulator images since real hardware is present, and anything materially larger returns to the user — they authorised an SDK, not an open-ended download.

- 2026-09-02 ~20:12 IDT — Required G to verify that the artefact it installs on the Android device actually contains `443771a8` before capturing any Android packet.
The finding that no Android tag carries that commit makes this the specific way G-2 could still produce a false pass: a build that cannot unseal a sealed frame yields a capture with nothing to find, which is indistinguishable from encryption working unless the build is checked.
That would be the third false-pass trap of the session, after the shared-subnet interface and the stale Android note.

- 2026-09-02 ~20:12 IDT — User is clearing the phone's stale server entries themselves before G's visit, so G writes taps for a clean pair and confirms their absence as its first observation at the phone rather than including removal steps.

- 2026-09-02 ~20:05 IDT — Independently re-verified G's finding 3 from the remote before letting it become a user-facing decision, rather than relaying it.
`443771a8` is an ancestor of `ios-v215` and of `origin/main` but of no Android tag; `android-v62` was cut at 01:30 today and the fix merged at 18:47, so the newest Android tag predates it by seventeen hours.
The check discriminates rather than always saying "missing" — `ios-v214` correctly reports missing and `ios-v215` correctly reports contains — which is what makes the negative result evidence.
Consequence: G-2 cannot run on any existing Android artefact, so the stage-2 flip's Android gate is now blocked on producing a build at all, not on capturing one.

- 2026-09-02 ~20:05 IDT — Ruled the cabled iPhone 13 Pro for G-1 rather than the 17 Pro D2 used, on the owner's authority.
Being cabled removes a class of interface ambiguity that finding 1 shows is capable of producing a clean-looking false pass, and the second device model partly answers D2's single-model limitation at no extra cost.

- 2026-09-02 ~20:05 IDT — Told G to park G-2 entirely rather than pick a build route on its own: no EAS build started, no Android SDK installed.
Both routes cost the user something irreversible-ish — a login or several gigabytes — and neither is worth spending before the prior question is answered, which is whether an Android device is physically available at all.
If it is not, G-2 is a hardware problem rather than a build problem and the stage-2 flip stays blocked regardless of which route were chosen.

- 2026-09-02 ~19:52 IDT — Dispatched G, C1 and R in parallel and deliberately held C2, after re-verifying every precondition from the remote rather than from the briefs.
Mobile `origin/main` had moved to `26815a16` (`ios-v215`), ahead of the pin STATUS carried, so both mobile-facing groups were pinned to the newer commit at kick-off instead of discovering the drift at rebase time.
The streamer's two open PRs (#753 dependabot, #754 Snyk) and mobile's one (#879 dependabot) were confirmed to be bot-originated, so neither slot is held, and all three were left untouched.
The rig was confirmed down; the only Threadbase process running is the user's production menubar server on `:8766`, and every agent was told explicitly not to touch it or the real `~/.threadbase`.
C2 is held rather than merely deprioritised: starting it now would produce a mobile branch that rots behind C1's rebase for no gain, and the two contend for one PR slot.

- 2026-09-02 ~10:31 IDT — Sealed pairing is confirmed by scratch rig events (`pair.token_exchanged`, `e2ee.context_opened`) and aggregate real database state (one device, one E2EE-required). Began the mandatory LAN plaintext positive-control setup without touching that rig: separate scratch `v1.73.0` server runs on :8791 with `--feature e2ee=false`, and the selected phone's LAN address was resolved. Required packet capture cannot start until the user authenticates local sudo; `sudo -n tcpdump -D` correctly refused with no password rather than prompting or bypassing. Do not scan the legacy QR until the capture is running; any stale QR will be regenerated.

- 2026-09-02 ~00:29 IDT — Re-verified scratch streamer health (:8790), Metro (:8081), selected wired phone, and authoritative DNS before restarting only the scratch rig with QR output enabled. A fresh one-time pairing QR was rendered directly in the live session from a scratch-only log; no link, API key, token, device key, or hostname was copied into evidence. Awaiting attended scan and encrypted-server confirmation before any D2 capture starts.
- 2026-09-02 ~10:38 IDT — Attended sealed pairing succeeded (the phone displayed the same public identity code as the scratch rig; the server recorded `pair.token_exchanged` and `e2ee.context_opened`). The phone then showed “The server is busy; retrying shortly.” Sanitized local request evidence establishes a reproducible functional failure, not a Cloudflare health or Access failure: `POST /api/e2ee/open` reached the sealed scratch server but received 61 HTTP 429 responses after 20 HTTP 200 responses. The 200s show repeated WS context opens followed by `GET /ws → 200` and immediate reconnect scheduling. The streamer rate limiter allows five opens per device key per rolling 60 seconds; the reconnect loop exhausts it. No packet capture, cleartext claim, or Access pass was recorded.
- 2026-09-02 ~10:41 IDT — User force-quit the iPhone app, stopping the reconnect storm. Restarted only the scratch :8790 process after verifying its exact listener and preserving its previous scratch-only log; the durable scratch DB remains present, `/healthz` is healthy, and no pairing or production service was changed. The next foreground open is a clean, bounded reproduction window (at most five WS opens before the limiter), with a fresh sanitized log.
- 2026-09-02 ~10:44 IDT — The first reset process did not persist after its non-interactive launcher exited, so the tunnel briefly returned 502. It was stopped before retry. A server startup line exposed masked credential material to the local diagnostic stream; no usable secret was copied into evidence. On user direction, the exact scratch-only output file was deleted (not recoverable), and the isolated :8790 rig was relaunched through a portable deny-list filter before output reaches disk. Local and edge-forced `/healthz` are 200, and the fresh filtered log contains no credential terms. Awaiting one controlled phone retry; no new encrypted-open request has reached this instance yet.
- 2026-09-02 ~10:48 IDT — User removed the prior server from the iPhone, stopping its background reconnect state. Minted a fresh one-time scratch pairing QR. The first renderer output was rejected before use because its background was invalid; the token-bearing temporary image and raw output were removed. A renderer was validated against a non-secret payload, then a new 396×396 QR was rendered from a fresh token; its raw source and intermediate SVG were removed immediately. Awaiting attended scan and encrypted-server acceptance.
- 2026-09-02 ~10:51 IDT — The phone correctly rejected the 10:48 QR: its renderer had encoded the banner’s server URL rather than the `threadbase://pair` payload. Root cause confirmed in the pinned CLI’s `printServerBanner` implementation. Removed that image, minted a new token, and rendered only the exact `threadbase://pair` payload into a fresh 610×610 QR. Raw pairing output and SVG intermediate were removed. Awaiting scan.
- 2026-09-02 ~10:53 IDT — The corrected QR reached the app, but pairing reported an unexpected server response. Before response diagnostics could run, the shell failed to create a temporary file with `no space left on device`. Direct filesystem verification showed only 114 MiB free on the Data volume (100% capacity; inodes were available). D2 paused without deleting user data.
- 2026-09-02 ~11:05 IDT — User explicitly approved removal of 22 clean, post-ship worktrees after per-path cleanliness and merged/closed-PR checks. Removed all 22 through Git; four Streamer worktrees required `--force` solely because each had a verified-clean submodule. Pruned 22 already-missing Streamer worktree registrations. Data-volume free space increased from 114 MiB to 38 GiB. The active D2 mobile worktree and all dirty/unresolved worktrees were preserved. Disk exhaustion no longer blocks the pending pairing-response diagnosis.
- 2026-09-02 ~11:10 IDT — With disk space restored, the phone successfully paired against the pinned scratch `v1.73.0` server (`/api/pair/exchange` 200). The earlier exchange 500 is therefore consistent with exhaustion, not a version mismatch. The functional blocker remains distinct: immediately after pairing, five WS encrypted contexts opened successfully and then further opens received 429 because the WS reconnect loop does not stay connected. No sealed transport capture or acceptance claim has been made.
- 2026-09-02 ~11:25 IDT — Root cause isolated from the installed iOS runtime and pinned client source: React Native 0.86 delivers binary WebSocket message data as `ArrayBuffer`, while `services/ws-client.ts` accepts a sealed frame only when it is `instanceof Uint8Array`. The first valid sealed server frame is consequently rejected locally, triggering the observed reconnect loop and eventual `/api/e2ee/open` 429. This is a mobile client defect; no code change has been made and no D2 acceptance claim has been made.
- 2026-09-02 ~11:40 IDT — Mobile fix in progress on `fix/e2ee-ws-arraybuffer`: sealed WebSocket receive now converts React Native’s `ArrayBuffer` delivery shape to `Uint8Array` before record unseal. The new regression test first failed against the defect and now passes; focused WS suite (41 tests), ESLint of the two changed files, and TypeScript typecheck pass. A native device rebuild is blocked by Xcode’s `AssetCatalogSimulatorAgent` CoreSimulator failure before installation, not by app compilation or signing. Because the change is JS-only, a fresh Metro server from the fixed worktree is running on :8081 for the existing installed development build. Functional iPhone rerun and sealed transport capture remain pending.
- 2026-09-02 ~11:16 IDT — The scratch streamer had stopped, which explained the subsequent “Could not reach the server to open an encrypted context” message: local `:8790` was closed and the tunnel returned 502. Restarted the same isolated rig with its existing scratch identity/config; local and edge `/healthz` both returned 200. The phone then opened exactly one REST context and one WS context, both 200, and no further WS `/open`, reconnect, refusal, or 429 occurred for more than 45 seconds. This is strong transport-stability evidence for the JS fix, but not D2 acceptance: complete functional UI confirmation and sealed capture are still required.
- 2026-09-02 ~11:18 IDT — User confirmed the iPhone’s stale error cleared and the paired tunnel server now visibly shows Connected. This completes the D2 named-tunnel functional connection prerequisite, but it is explicitly not a wire-secrecy result. The next gate is the mandatory LAN plaintext positive-control capture, then its identically driven sealed capture. `tshark` is installed; privileged capture is blocked only because the local sudo credential is not cached.

- 2026-09-02 ~00:27 IDT — Selected iPhone became paired and enabled Developer Mode. The mandated `scripts/dev-device.sh --no-watchman` path initially stopped at Expo's CocoaPods step because the shell selected macOS Ruby 2.6, which lacks `Gemfile.lock`'s Bundler 4.0.16. Root cause confirmed by comparison: Homebrew Ruby 4.0.6 provides Bundler 4.0.16 and `bundle check` resolved the lock with no dependency change. Re-ran the identical device script with only Homebrew Ruby prepended to PATH; Xcode completed, Threadbase bundle `com.ronenmars.threadbase` version/build `1` is installed on the selected device, and Metro owns :8081 (`packager-status:running`). The build-generated Podfile.lock drift was restored and the installed Pods manifest synchronized; the worktree is clean.

- 2026-09-02 ~00:04 IDT — User selected `Rbv1000-13-Pro` for D2. Direct device inspection reports physical iPhone14,2, wired and booted, but `pairingState: unpaired` / tunnel disconnected; its UDID is recorded only in the local device tooling output, not this ledger. No build, install, pairing link, or app interaction was attempted because completing the trust pairing requires the user at the phone.

- 2026-09-01 ~20:51 IDT — Routed user-selected scratch hostname `<test-tunnel-host>` to the dedicated D2 tunnel and restarted only the scratch rig with that public URL. The initial `route dns` command inherited an unrelated global Cloudflare config and briefly selected tunnel `3a522409-2e47-49af-8de6-fc1841e3224b`; immediately corrected it using `--config /dev/null --overwrite-dns` and explicit D2 tunnel ID `877706d3-1f47-4ac9-8832-c970547a379e`. `cloudflared tunnel info` confirms the D2 connector, and a request forced through Cloudflare's authoritative edge IP returned `/healthz` 200. Local macOS DNS has not propagated yet, but authoritative DNS has. The unauthenticated 200 establishes that Cloudflare Access is not configured on this scratch hostname, so the required Access functional pass cannot yet begin.

- 2026-09-01 ~20:40 IDT — Cloudflare API retry succeeded. Created dedicated named tunnel `tb-e2ee-d2-20260901` ID `877706d3-1f47-4ac9-8832-c970547a379e`; its credentials are retained only in the machine's Cloudflare credentials store. No DNS route, ingress rule, or connector was created, so it is isolated from traffic until a scratch hostname is selected and the connector is explicitly launched with its credentials file and `--config /dev/null` toward `127.0.0.1:8790`.

- 2026-09-01 ~20:38 IDT — On the user's explicit instruction, unloaded the verified launchd production service `com.ronen.threadbase`; its child exited and :8766 has no listener. Installed `tshark` 4.6.8. Installed exact `@threadbase-sh/streamer@1.73.0` under a new scratch HOME, set a fresh key via stdin, and started a detached `serve --port 8790 --feature e2ee=true --no-pair-qr --verbose` process; `/healthz` is healthy and lsof shows only scratch runtime/cache/scanner DBs. Three Cloudflare API calls (list twice, create once) timed out; dedicated tunnel `tb-e2ee-d2-20260901` was not created and no DNS state changed. The remaining D2 setup blockers are a stable wired phone and Cloudflare API access plus a chosen scratch hostname.

- 2026-09-01 ~20:26 IDT — **D2 kicked off.** The coordinator fetched refs and independently confirmed mobile #934 `MERGED` at `b0e55f658c477c22f79b250b201a50037d6adbcf`, #927 `MERGED` at `bb5cd7f0de1363fdedb4c01b73f78fe7c66eaa14`, and streamer `v1.73.0` at `ab15fc2c2dc8231816a95bb836fb05545d51f11c` (with `v1.72.0`/`v1.72.1` also present). The requested `../tb-mobile-worktrees/e2ee-rest-envelope` was clean and detached at mobile `origin/main` `b0e55f65`; no source changed. Start-state recorded: no simulators booted and no listener on :8790. The iPhone is paired over local network but disconnected during detailed inspection; packet decoder `tshark` is not present, so D2's mandatory decode-before-grep positive control is pending rather than substituted.

- 2026-09-01 ~20:10 IDT — **XC2 PR #934 opened** (`feat(e2ee): add REST envelope transport`, `5709337d`). Coordinator verified: mobile open PRs are **#934** and **#879** only (#932–#929 merged earlier 2026-09-01); `origin/main` = `8e27ea72` (#933); branch is one commit ahead with merge-base `8165616e` — rebase required before merge. CI on #934 all green at check time. `tracks/STATUS.md` XC2 row updated from "implementing / no PR / six open" to match. D2 and R2/R3 remain gated on XC2 merge; R1 is unblocked on streamer tags but not started.
- 2026-08-31 ~18:32 IDT — **XC1 finding, independently confirmed by the coordinator against `v1.72.0:src/e2ee/noise.ts`: the streamer's psk-less `/open` "IK" is not spec-pure `Noise_IK_25519_ChaChaPoly_SHA256`.** `writeMessage1`/`readMessage2` call `state.mixKey(e.publicKeyRaw)` on the initiator/responder ephemeral **unconditionally**, on both patterns — the surrounding comment says "In a PSK handshake the token also calls MixKey (spec §9.2)" and then the code runs it regardless of `pattern`. Not exploitable (both sides do it identically, strictly more mixing, not less) and not being changed — the tagged `/open` fixture is the frozen contract XC1 is pinned to. Risk is purely documentation/domain-separation drift: the protocol *name* advertises a handshake a third-party spec-pure implementation would not reproduce, and "fixing" it later without a coordinated fixture regeneration in both repos would silently break interop with only "Noise: authentication failed" to debug. XC1 documented this in a prominent comment in `services/e2ee/noise.ts`. **Action:** relayed to X-server to check whether it independently hit the same thing; NONCE-DESIGN §11 should eventually note the ephemeral-mixing deviation, or the streamer should regenerate the vector — neither happens without a coordinated cross-repo change, and none is authorized right now. Same file/class as C-4 (the msg2 `provisional` fixture contradiction).
- 2026-08-31 ~18:07 IDT — **#743 shipped early, inside W1b, unannounced.** `v1.72.0` added `contextInvalidators`: `registry.destroy()`/`destroyOwned()`/`clear()` now clear `#send`/`#receive` and null `#responses`, and `sealResponse` throws through `requireRest()` once it is null — independently confirmed by the coordinator reading `v1.72.0:src/e2ee/context.ts` directly. Three X-server documents state the opposite ("destroy is unmapping, a held context seals identically either way"). Effect: sealing a `device-revoked` refusal *after* the destroy no longer returns an identical response — it throws, handing the phone an undecryptable dead end on the one refusal §9 marks non-retryable. Actions approved: the call-order spy test is replaced by a black-box test (BRIEF-2b §6's own contingency), and ADVERSARY-BRIEF row 23's "the ordering is not observable at this commit" caveat is corrected before the adversary is spawned; `#window` stays out of the invalidator (`requireRest()` already throws before it would be consulted). **Rule:** when a track's brief carries an "if X ships, do Y" contingency, re-check X against the tag at rebase time — the contingency fired here and nothing else would have caught it.
- 2026-08-31 ~18:05 IDT — X-server framing-header remedy: `own()` was correct but untested and one refactor from wrong; replaced by the framework accessor `c.req.header`, whose immunity was verified in `@hono/node-server` 2.1.1 `dist/index.mjs:84` rather than assumed. The middleware's own comment claimed a polluted `content-length` denies *every* body-bearing request; corrected — an own property shadows the prototype, so only the *absent* header is poisonable. Reachable vectors are a bodiless GET (via `transfer-encoding`) and a chunked request (via `content-length`), and those are the two tests. **Rule:** state a pollution vector at the width it was measured at; "every request" claims are where these comments rot.
- 2026-08-31 ~18:06 IDT — The streamer stash queue is shared across all worktrees in the checkout. X-server's rebase used a path-scoped, labelled stash and dropped only its own entry; an unrelated `feat/prompt-contract-foundation` WIP entry sits beneath it untouched. **Rule:** never `git stash pop` in this repo without reading `git stash list` first — the top entry may belong to another session.

- 2026-08-31 ~18:00 IDT — **X-server recovery: the six-path SHA baseline STATUS.md line 19 claimed was "now taken for all six" was never persisted to a tracked file and did not survive the session boundary.** Recovery discharged instead by the tracked 57/10 diff stat (matches `HANDOFF-2026-08-30.md`), `tsc` exit 0, and 46/46 targeted tests green — a stranded mutant in an untracked file would have had to leave that count green, which is the definition of an uncounted mutant. **Program rule:** a pristine manifest is evidence only once it is written into a tracked `tracks/` file in the same report that claims it. X-server's new manifest goes into `tracks/X-server/PLAN-X-server.md`.

- 2026-08-31 14:09 IDT — **Release-notes fix committed and safely remote; PR waits on repository sequencing.** User approved `fix(release): render conventional commit entries in notes`; commit `87354b1` passed the commit hook and was pushed with matching local/remote SHA. Unrelated PR #749 is still open, behind `main`, and red on lint. Its four test files do not overlap this fix, but changing or closing it is outside W1b authority, so no second Streamer PR was opened.

- 2026-08-31 11:10 IDT — **Release-notes fix staged for approval.** A fresh `fix/release-notes-rendering` worktree was created from release commit `d7a27ab`; the historical #742 worktree was preserved. The real configured release-notes plugin test was seen RED on 10.3.0 with `expected ... to contain '### Features'`. Pinning the preset to 9.3.1 is the minimal fix; it restores the W1b feature entry without changing workflow or product code. The focused six tests, TypeScript, pinned Biome across 473 files, direct real-commit control, diff check, and lock-protected full suite all passed. The exact three-file staged diff awaits user approval before commit.

- 2026-08-31 10:52 IDT — **W1b released; downstream tag gate open.** PR #748 squash-merged as `fd89defc`; semantic-release workflow `33369508885` published `v1.72.0` at release commit `d7a27ab`, four platform tarballs, `manifest.json`, npm package, and Homebrew formula. Post-merge CI `33369508896` passed every job, including Node 22/24 full tests and macOS/Windows smoke. Direct tag reads proved all 13 W1b paths, the exact real-upgrade ticket predicate, and the frozen pure-verdict `authenticateContext` seam. X-server and XC1 must consume exact tag `v1.72.0`, not the merge commit. Contradiction found: direct `generateNotes` with the pinned `conventional-changelog-conventionalcommits` 10.3.0 completes but emits only the 1.72.0 heading; the published release body and tagged changelog are likewise header-only. A version-only 9.3.1 control emits the expected feature entry, isolating a preset/writer compatibility defect outside PR #748's scope.

- 2026-08-31 10:23 IDT — **PR #748 F1 fixed and green**: two real-path tests were seen RED before the fix, each failing `registry.ticketCount` with `AssertionError: expected +0 to be 1`. The middleware now spends a ticket only when `GET`, `/ws`, and Hono's exact `Upgrade: websocket` predicate all match; the preserved ticket then succeeds on a real upgrade and receives both sealed startup frames. Local focused tests passed 63/63; TypeScript, no-NUL, diff check, and pinned Biome over 478 files passed. The full suite passed 256 files and 2653 tests, with one unchanged random-port `EADDRINUSE` failure; its permitted isolated rerun passed 16/16. User approved commit `4edfa6c9` (`fix(e2ee): spend a ws ticket only on a real upgrade`), it was pushed to the existing PR, and all 11 refreshed GitHub checks passed without retry. PR #748 remains open and unmerged.

- 2026-08-31 09:02 IDT — **PR #748 independently reviewed.** A fresh session with no branch context, working only from `tracks/W/REVIEW-PROMPT-PR748.md`, tried to falsify ten invariants against the diff and could break none of the sealing, ticket, counter, principal or bounds paths; the six security-relevant tests it spot-checked all discriminate, with real positive and negative controls.
One finding, **F1** (low, CONFIRMED by trace): the ticket branch is gated on the path alone, so a `/ws` request that carries a valid `X-TB-Ticket` but is not a WebSocket upgrade — no `Upgrade: websocket`, or any non-GET method — spends the single-use ticket, promotes its context out of provisional to the 24 h lifetime, and then 404s without ever attaching a socket. The 10 s first-frame deadline is armed inside `WSHub.addClient`, which never runs, so the orphan has no clock; it is the last sibling of the two orphan paths this PR closes deliberately. No plaintext, no key exposure, bounded by the 4-per-device cap and by needing a valid ticket, and the same residual shape §13 already accepts for a mismatched upgrade — hence not a merge blocker.
Fix ruled: gate the branch on `method === "GET"` plus hono's own upgrade predicate, mirrored **exactly** — stricter than hono's would let a real upgrade fall through to bearer/`?key=` auth and become a legacy plaintext socket, which is worse than the bug. Dispatched to Codex with `tracks/W/CODEX-BRIEF-PR748-F1.md`, red-first tests required, in the existing `feat/e2ee-ws-sealing` worktree.

- 2026-08-31 08:43 IDT — **W1b PR gate green**: user approved the exact 13-path scope and message `feat(e2ee): seal WebSocket transport per device context`; commit `6d71e7743b712530d9d621a2756e93ba10eaafe6` was pushed as `feat/e2ee-ws-sealing`, and streamer PR #748 opened against `main`. All 11 checks passed without retry: Node 22 and Node 24 full suites, macOS and Windows smoke, build, lint, setup, cache, gate, link check, and Snyk. PR is open and mergeable. Remaining W1b gates: separate merge approval, refresh/rebase onto current `origin/main`, squash merge on green, then wait for and independently verify the semantic-release tag. X-server remains frozen until the tag exists.

- 2026-08-31 07:49 IDT — **W1b fix round 4 isolated acceptance complete**: fresh detached adversary and final-review trees independently matched the exact 13 frozen blob hashes. The adversary ended `could not break it`; the reviewer reported no Critical, Important, or Minor findings and ended `Approved`. Both independently passed TypeScript, no-NUL, pinned Biome 2.5.10 over 478 files, and `git diff --check HEAD`; the adversary also passed its independent crypto/context, pairing/protocol, capability, hub/no-bypass, device, and composed production-object probes. The only pre-staging gate left is one passing suite under the atomic `/tmp/tb-streamer-suite.lock`. It has not started because the 10-CPU host remains materially saturated (one-minute load roughly 20–29 with measured CPU idle 0–8%), the same class of condition that produced prior unchanged shutdown-hook timeouts. No unrelated user process will be stopped to manufacture a green run.

- 2026-08-30 07:13 IDT — **Task 0 recovery baseline**: user approval to continue in the existing W1b and X-server worktrees recorded (their staged, unstaged, and untracked states are the primary artefacts). Codex `/root` is recovery coordinator; W1b is reserved to the single Codex implementer task `/root/task1_w1b_recovery` before any product edit; X-server is frozen with no active writer and reserved to `/root/task2_xserver_recovery` only after W1b releases. The old Claude sessions are evidence sources, not current writers. Read-only checks found no `/tmp/tb-streamer-suite.lock`, no task-specific Git lock, and no evidence of another writer touching those trees. W1b's nine source fixes landed before the weekly-limit stop, but its tests and verification did not; X-server's header remedy is mid-transition. Immutable SHA-256 manifests and complete Git state are in `tracks/W/REPORT-W1b.md` and the Task 0 report.

- 2026-08-30 03:29 IDT — **Sub-agents do not survive a usage-limit boundary** (X-server: `SendMessage` to its implementer → "No agent named … is reachable"). Rule: after any limit gap, assume every sub-agent is dead, verify with a round-trip, respawn with a self-contained brief; never carry state on trust. X-server: header fix dispatched to a fresh implementer; §6a now 12 steps (sweep = every bracket or dotted read on wire-shaped objects); BRIEF-2b asserts `principal.deviceId` from the context even when the row disagrees, and a throwing `revoked_at` accessor → `no-device-store`; adversary brief 29 rows (pollution row; denial counts as a finding).

- 2026-08-30 03:27 IDT — Header-read remedy refined (W, probed): Hono `c.req.header()` is immune (fetch `Headers.get()` → null when absent); W1b's files already use it, so no sweep there. Shipped exposure in v1.71.0: `e2ee.routes.ts:87` `Number(req.headers["content-length"])` on the public `/open` path (self-inflicted 400 under pollution, not a bypass) → W1b item nine. Three out-of-track raw reads filed as streamer **#747**. Program rule: prefer the framework accessor; `own()` only on raw Node objects that must stay raw.

- 2026-08-30 03:23 IDT — X-server `own()` sweep: rung 5a read `req.headers["content-length"]`/`["transfer-encoding"]` unguarded — Node builds `req.headers` with `Object.prototype`, so a polluted prototype yields unauthenticated denial of the whole REST envelope (both directions). Fix `own()` on both reads; revert mutation red under pollution. **Program rule extended:** the pollution sweep covers bracket reads on attacker-shaped objects (headers, query, parsed JSON), not only `??` defaults; W told to sweep its header reads in the same refreeze.
- 2026-08-30 03:25 IDT — Usage limits cut the owner, W's implementer and X-server at ~00:26; W verified its tree (no half-applied fix, no stranded mutation) and restarts the eight fixes (+ header sweep) from the top. Rule: children report after each fix batch and before long campaigns, so evidence survives a mid-run termination.

- 2026-08-30 00:26 IDT — W1b adversary round 1: one break (`??` vs `own()` on `firstFrameMs` — same class as W1a round 5, in new code) + three gaps on the frozen `authenticateContext` contract + two cheap + two residuals; owner: fix all eight before merge; principal `deviceId` now from the context by construction (§13 invariant structural); re-campaign + suite + refreeze + targeted re-adversary. Lesson for the briefs: the `own()` rule needs a grep-able check in the campaign (every `options.x ??` at a constructor boundary).

- 2026-08-30 00:17 IDT — Briefs corrected for ruling B3 (no in-place rekey, NONCE-DESIGN §6): D2's "foreground rekey" row is now a context rollover (new context, 10 s drain of the old, then `E2EE_CTX_UNKNOWN`); X-client's brief and adversary list updated likewise. D2 runbook pre-written in PLAN-D.md §14 against the current mechanism.

- 2026-08-30 00:15 IDT — **D1 accepted.** All seven pairing gates from #674 now have hardware evidence (`tracks/D/D1-REPORT.md`). Findings filed, none blocking: streamer #744 (orphan row after lost msg2 — pending label, no auto-expiry), mobile #920 (§4.4 revoked-device recovery unreachable from the app), mobile #921 (react/react-dom pin mismatch breaks the web target). D holds for D2.

- 2026-08-30 00:10 IDT — W1b judgement calls kept: `no-device-store` → 503 `STORE_UNAVAILABLE` (a store fault is a statement about the server, never a revocation claim), propagated to X-server for REST; the 10 s deadline closes with `E2EE_CTX_UNKNOWN` (literally true; no fifth code). `server.test.ts` port TOCTOU flake filed as streamer #746.

- 2026-08-29 23:53 IDT — D1 row 6: the server's revoked-device recovery (design §4.4, `repairStmt` clears `revoked_at` on a same-key re-pair) cannot be invoked from the app: delete/edit wipe `D_priv`, re-scan of a known URL is blocked. Mobile gap to file (re-authorize affordance keeping `D_priv`); not a stop; row 6 evidence records shipped behaviour with the gap named.

- 2026-08-29 23:47 IDT — Seam last word: `authenticateContext` failure reasons are **three**: `device-revoked` (set `revoked_at` or missing row), `credential-mismatch` (other device or no device), `no-device-store` (null/unreadable store, `get()` throws) — X-server maps 403/401/403, all sealed; W maps to close reasons. Both branches add `e2eeContext` to `AppEnv["Variables"]` — second rebaser keeps one declaration.

- 2026-08-29 23:33 IDT — Orphan-row finding closed: rig artefact (two proxy URLs → two per-URL phone keys); phone and streamer behave as designed and a same-URL retry self-heals. Reviewer: no access exposure (the dropping party holds neither token nor `D_priv`), but **not cosmetic** — orphan rows blind the design §2.6 rogue-device detection path (`GET /api/devices`) and inflate R1's pinned-device count; recorded on streamer #744 with a pending-until-first-contact **label** as the fix (no automatic expiry: the server cannot tell an orphan from a paired-but-idle device, and deleting it would surface a false `E2EE_DEVICE_REVOKED`), after the transport lands. R's brief should note the count pollution.

- 2026-08-29 23:30 IDT — D1 row 2 finding: a dropped or tampered msg2 leaves an unrevoked, admin-capable orphan device row (`register()` runs before the client acknowledges; by design the token is spent on a successful exchange). Owner read `devices.repository.ts::register()` on origin/main: same `e2ee_static_pub` re-pairs onto the existing row, so two orphan ids mean two different phone static keys — if confirmed, a mobile defect vs CLAUDE.md §3 (`D_priv` load-or-create, reused on retry). Assessment pending reviewer: dead rows (no token holder), hygiene/trust-list defect, not a credential leak.

- 2026-08-29 23:15 IDT — Handoff for a non-Claude agent written at the user's request: `tracks/HANDOFF-2026-08-29.md` (program, rules, state per track, the frozen `authenticateContext` seam, how to proceed). Keep it current on every gate. D row 2: tampered msg2 via a byte-flipping reverse proxy on `POST /api/pair/exchange`, not a rogue server.

- 2026-08-29 23:08 IDT — Owner + reviewer, `authenticateContext` seam frozen for W1b's merge: input is the resolved `E2eeContext` (unknown-ctx is each caller's concern); result `{ok:true, principal} | {ok:false, reason}` with no status/body/logging/destroy; reasons: `device-revoked` (set `revoked_at` **or** missing row **or** no/unreadable store), `credential-mismatch` (includes a credential naming no device — the shared key); `Object.hasOwn` on `revoked_at`; invariant `principal.deviceId === context.deviceId`. Seal-then-destroy is unobservable at this tag because `registry.destroy()` only unmaps (W) — pinned instead by a call-order test in X-server's caller (18 real mutations kept); the unmapping-not-invalidation gap filed as streamer #743, not for either open PR. Zero-on-destroy not filed: `KeyObject` cannot be wiped, JS copy already zeroed at import.
- 2026-08-29 23:06 IDT — User: app loaded on the phone; relayed to D to retry the run.

- 2026-08-29 23:05 IDT — Owner + reviewer, X-server slice 2 sequencing: **option 3 completed by 1** — build the envelope middleware now (2a) against a test-supplied downstream, wire principal/revocation/capability (2b) after rebasing onto W1b's merge; copying W1b's `context.ts` hunk rejected (two copies of a helper in a tag-frozen crypto module). 2a is reviewed as an interface-locking pass only — nothing counted or reported verified until 2b's real-chain positive control and the full 18-mutation campaign run on the rebased bytes. W1b must merge with the Q1 pure-verdict helper already in its bytes.

- 2026-08-29 22:55 IDT — X-server slice 1 (RFC-6479 window + `context.ts` seam): X-server ran the campaign itself (implementer idle without a report) — 8 red + M5′ red; M5 struck as a provably unreachable no-op mutant, not counted. **Program rule:** mutation drivers verify restoration by sha256 against pristine copies, because `git diff --quiet` is blind to a mutant left in an untracked file. Two fixes before freeze: the `#isSeen` comment — **22:58 corrected by X-server's probe:** neither the guarded nor the unguarded form can fail open, because both rejections precede `#markSeen` whatever their order; the ordering decides only which §9 code a stale-slot counter gets (recoverable `E2EE_CTX_UNKNOWN` is correct). Owner's "unset slot accepts silently" claim withdrawn. M9 (block swap) red on the diagnosis test and a below-window-not-recorded test with a block-swap mutation.

- 2026-08-29 23:20 IDT — Owner + reviewer, X-server Q1–Q7: **Q1** `authenticateContext` is a pure verdict; destroy only on `revoked_at` (an unforgeable DB fact), never on a mismatched credential (an attacker-supplied header) — otherwise any on-path observer of the plaintext `X-TB-Ctx` could kill a victim's REST context per request; the REST caller seals its refusal with the live context before any destroy. **Q2** `X-TB-Env` carries the 46-byte empty sealed record for bodiless requests; a request with both a sealed body and `X-TB-Env` is rejected. **Q3** buffer the only ndjson stream. **Q4** no multipart at the tag (uploads are raw-body POSTs) — reviewer's rev-1 M8 corrected. **Q5** sealed 304 via `X-TB-Env` bound to the request counter — one general rule for bodiless framing in both directions. **Q6** status outside the envelope; 200→5xx rewrite = denial, accepted. **Q7** status map as proposed. Structural invariant endorsed: acceptance runs only inside a successful `unsealRequest`, so a refused request cannot receive a sealed body.

- 2026-08-29 23:05 IDT — D reproduced the cause of its two simultaneous kills: a harness limit — three tool-tracked background tasks die together at the next turn boundary, two survive (rig+tunnel combined into one wrapper + the build survived a boundary). Not an interrupt in the pane. macOS has no `setsid(1)`; the next relaunch uses a Python `os.setsid()` wrapper with PID files for true detachment. Rule for every track running long-lived rigs: at most two tool-tracked background tasks, or detach.

- 2026-08-29 22:35 IDT — Owner + reviewer (first use of the standing design-question rule): **Q1** a ticketed WS upgrade carries no `Authorization` — the reviewer traced that the context is set in exactly one place together with a principal from the context's own device row, so a sealed socket can never carry a foreign principal; a bearer beside a ticket must name the same device (shared key = mismatch); capabilities/revocation read fresh at upgrade. Amendments: one shared ctx→principal helper in `context.ts` for both channels; the invariant as a test; client rules frozen. **Q2** option 2 — attach at upgrade, 10 s first-valid-sealed-inbound-frame deadline (any sealed frame, not coupled to `register`; 15 s the only fallback), `everSealed`-at-attach + `sendTo` drop-and-close already in the tree and now tested; the owner's 60 s liveness rule and option 4 both withdrawn on W's findings. Residual: a thief holds a slot on an *orphaned* context receiving only low-frequency broadcasts for ≤10 s.

- 2026-08-29 22:40 IDT — Owner: proposed 60 s liveness-on-sealed-inbound-frames for sealed sockets; W checked the v1.71.0 wire contract — `ping` is server→client only and every client→server type is user-driven — so the rule would have reaped a phone that is merely reading. Withdrawn; revised to the 10 s first-sealed-frame deadline alone (the thief cannot seal; the client sends `register` on open, now a contract item). Standing-rule loop working as intended: the track corrected the owner before the reviewer ruled.

- 2026-08-29 22:25 IDT — User: standing rule — a design question from a track goes to `e2ee-mega-brain-reviewer` with the owner's opinion and suggestion first; the owner and reviewer converge; the track receives the agreed solution. Applied immediately to the bearer-on-ticketed-upgrade ruling (sent for review; W proceeds but does not freeze until the verdict).

- 2026-08-29 22:20 IDT — Owner: W1b made the ticket a header, which removes the only reason the credential was ever in the URL — so a ticketed upgrade sends no bearer at all (a bearer in a header is plaintext to the tunnel). Implemented in W1b rather than deferred, because X-server has not started and `auth.middleware.ts` is untouched; X-server's kick-off now says W1b owns the `/ws` branch of that file. Also recorded: W1b's N-seals cost is fixed per call (~6.5 µs), not per byte — batching beats a faster cipher; a hub-side frame check was deleted because no mutation could reach it ("a safeguard whose mutation was never seen red verifies nothing").

- 2026-08-29 21:55 IDT — Owner: a duplicate `e2ee-owner [2f959c]` existed ~21:36–21:50 (user-created, exited on its own or closed); children switched to ref-qualified addressing of the owner permanently. W applied the rename rule back to the owner's bare-name instruction and adopted it only because it was strictly safer under either authorship — the correct reflex. D's ref change (d9cb6a → 8a6dc5) is a user-initiated transcript resume, accepted after D re-verified every precondition itself.

- 2026-08-29 21:25 IDT — Owner: streamer v1.71.0 (f95150cc) verified — W1a's commit is an ancestor, all artefacts present at the tag. Release fix #742 → dd74849f restored the pipeline. W1b started in parallel with X-server (disjoint files; second to merge rebases and re-runs its campaign). X-server kick-off placeholders filled: tag v1.71.0, NONCE-DESIGN rev11 §13 as the contract, `unsealUnchecked` as the window seam, fixture keys named.

- 2026-08-29 21:15 IDT — Owner: Group F closed (four PRs, four issues, zero defects introduced, two branch mix-ups caught pre-push). D1 fired on P+M+F closed with the streamer pinned at 1.70.6 — the record layer is merged (#741) but untagged until the release fix lands, and D1 exercises only the pairing path 1.70.6 carries; D2 pins the later tag.

- 2026-08-29 19:35 IDT — Owner: `npx semantic-release --dry-run --no-ci` on a feature branch exits 0 without reaching `generateNotes` ("configured to only publish from main") — a false green produced while verifying the fix for a false green. Evidence accepted instead: `generateNotes` called directly with the repo's preset config over the real commits since v1.70.6 — throws under 10.4.0, completes under 10.3.0.

- 2026-08-29 19:30 IDT — Owner: W1a merged (#741 → 9b5f367d). The streamer release pipeline has been broken since dependabot #733-era bump 15696278 (conventionalcommits 10.4.0 vs writer 8.4.0) and reported success twice because `generateNotes` only runs on a releasing commit — W1a's `feat` was the first. Fix assigned to W as `fix(release): pin conventionalcommits to 10.3.0` (exact pin; the caret let it drift), proven with a dry run showing next version 1.71.0; that PR cuts the tag X-server keys off. Program rule kept: children key off tags, never merge commits.

- 2026-08-29 18:20 IDT — Owner: W1a's sixth adversary round is the acceptance — every hardened property held under a harder attack than the round that found it. Six adversary rounds total: 1 (6 findings, 3 serious), 2 (4), 3 (2 + a contract gap), 4 (3), 5 (6, one genuine security defect), 6 (1 unreachable + 4 hardenings + 1 doc). Remaining six items taken with mutations; no round 7 — owner reads the delta.

- 2026-08-29 18:00 IDT — Owner: biome-bump rebase confirmed non-inert (2.5.10 lockfile vs 2.5.3 binary via the shared symlink; three genuine unused imports); rule for streamer tracks: report the linter binary's version, never trust a green from an unverified binary, and never `--write` with a tool version that would reformat base-owned files. M78's three attempts recorded as "red for the wrong reason is not evidence"; two racing suite runs on one log killed and re-run once. Round-6 adversary told explicitly that a clean table from a real attempt is a valid outcome.

- 2026-08-29 17:30 IDT — Owner: W's own verification pass produced false negatives by grepping for names a refactor had renamed (`channelBindsTarget` → `validateTarget`) — the third distinct form of the clean-looking-false-negative trap in this track; noted so reviewers grep for the property, not the symbol. Rebase onto the biome bump treated as a real change: pre-rebase greens do not carry, `npx biome --version` reported because `node_modules` is the shared symlink and may not match the lockfile.

- 2026-08-29 14:15 IDT — Owner, round 5 rulings: (1) the 426 helper fails closed — only `e2ee_required === 0` is unpinned, everything else (missing repo, missing row, true/"1"/undefined) refuses; (2) traffic keys become `KeyObject`s from `allocUnsafeSlow` copies zeroed after import, no getter returns key bytes, every public Buffer is an unpooled copy — the Buffer pool walk through a public `ctxId` is not fixable by hiding; (3) all nonce/window/registry state `#private`; (4) no `??` defaults at trust boundaries — `prologue` required, `ephemeral` via `Object.hasOwn`, one `own()` helper for the rest; (5) one `assertBytes` (Uint8Array, BYTES_PER_ELEMENT 1, byteLength) for every byte field, `pair-request.ts` as the reference; (6) `recordAad` enforces channel↔target itself. Round 6 required; "last round" retracted because round 5 found something real.

- 2026-08-29 13:50 IDT — Owner: the defect class "guard hardened against a missing argument, silent on an empty one" has now appeared three times in W1a (PSK truthiness, Noise pattern selector, `assertTarget` presence) — two found by the adversary, one by the implementer noticing the pattern in its own fix. Round 5 row 6 is a complete sweep of every truthiness guard on Buffer/string/array/number fields in `src/e2ee/*` and the route, each with a verdict. Also: the hardened driver halted on twelve invalidated patch targets after the AAD/ticket refactors — a silent-coverage-loss failure caught one round after the rule was made program-wide.

- 2026-08-29 13:15 IDT — Owner, round 4: `RestResponseSealer.seal` must assert its target (`recordAad`'s 32-byte check was unreachable because `sealWith` concatenated the target beside the header — now passed into `recordAad`); PSK length-checked (`!psk || psk.length !== HASHLEN`) — an empty Buffer is truthy and ran a full `IKpsk1` with a constant binding, the exact thing §11 rejected; timing oracle on the replay cache recorded as an accepted residual; `Object.hasOwn` for the pattern default, copied `HandshakeKeys` buffers, `#private` ticket table. Mini round 5 (6 rows) is the last gate unless it finds something real.

- 2026-08-29 12:55 IDT — Owner: program-wide mutation rule extended — **absence of a failure line is not evidence; only an observed red is.** W's hardened driver found that a mutated module that fails to parse had been read as "property held" (M58, the `#private` safeguard) — same class as the silently deleted tests. Drivers must report `BROKEN — did not run` for a non-parsing mutant. Also recorded: W1a's soft-reset rebase initially reverted the user's dependabot lockfile bump; taken from the new base and verified with `git diff --quiet 15696278 -- package-lock.json`. Replay indistinguishability pinned as a byte-identical-response test with its own red mutation.

- 2026-08-29 12:30 IDT — Owner: W's implementer's mutation driver applied patches outside its `try/finally`, so a session death mid-mutation could leave a mutation applied in the tree (it did not this time — verified clean). Rule for every track running mutations: revert in `finally`, assert `git diff --quiet` after every mutation, and check for a stranded mutation first after any interruption. W1a's uncommitted work rebases onto 15696278 taking `package.json`/lockfile from the new base.

- 2026-08-29 10:00 IDT — Owner, round 3: **`#private` fields** replace the enumerable/inspect-based redaction on every key-bearing class — a `#field` is not a property, so no rendering or cloning mode can reach it and correctness no longer depends on a hide list (third defeat of the weaker mechanism: `enumerable:false` → `showHidden`; inspect-custom → `customInspect:false`). `replays.record()` moves below the device-row check. Retry rule (fresh `writeMessage1` per attempt) into §11 and X-client. E-3 indefinite-outage wording into R. Process: W's implementer was still on rev6 — rev8 to be delivered and `diff`-verified before the next freeze. The adversary's first key detector reported `RecordState` clean and was wrong (Buffer renders as a decimal `Uint8Array` under `showHidden`); the mandatory unredacted negative control exposed it — the third round in which a control, not a detector, found the truth.

- 2026-08-29 09:40 IDT — Owner: W1a implementer's re-run mutation campaign caught its own regression — a bad slice in its edit silently deleted four route tests (budget ordering, unpaired-flood charge, per-device lockout, per-device keying) and **the suite stayed green**; six previously-red mutations went green, which is how it surfaced. Rule confirmed for every track: the mutation campaign is re-run on every refreeze, never carried forward. Replay cache built insertion-ordered rather than LRU (a hit is a replay; refreshing would let an attacker pin a slot) — accepted as strictly safer.

- 2026-08-29 09:05 IDT — Owner: round 2 (54 attacks / 26 rows) — six fixes hold; four availability/hygiene findings. **Approved a msg1 replay cache keyed on the cleartext ephemeral `e`** (repeat `e` = replay by definition; rejected before DH, charged to the source bucket, LRU 65 536 × 24 h, inserted only for msg1s that reached the handshake) — closes E-1 and E-2 with no protocol change; residual (post-eviction replays cost one DH pair and one device slot each) documented, protocol-level freshness noted for v2. E-3 is inherent to a shared-IP tunnel → doc + R. E-4: `SymmetricState`/`CipherState`/handshake states get the redaction helper. Round 3 = focused fresh adversary (replay cache, E-4, the six fix rows, budgets) as the acceptance.

- 2026-08-29 08:10 IDT — Owner: the isolated adversary's first W1a round: 6 succeeded / 14 rejected / 2 partial. **Three of the six (A, C, F) sat in code the owner had read and passed** — recorded as the reason the verifier structure exists. Rulings: A → the sealer tracks outstanding + a monotonic high-water + a 1024-bit answered bitmap; re-accept throws; below-high-water-and-unanswered → `E2EE_CTX_UNKNOWN`, above → `E2EE_SEAL_FAILED` (also closes 18, no evicted set). C → eviction computed over existing contexts only, new one exempt; §8/§15 unified. F → exhaustive switch + runtime literal check. B → one inspect-custom redaction helper on every key-bearing object incl. the registry. D+E → charge the IP failure bucket on unknown/revoked key too, raise it to ~30/min, keep 5/min per device, document the shared-IP tunnel residual for R. Adversary demo keys scrubbed before anything enters `tracks/`. **A second isolated adversary round on the fixed tree is the acceptance.**

- 2026-08-29 07:55 IDT — Owner: W1a's three fixes verified in source by W and accepted; `unsealUnchecked` (the REST sliding-window seam, added after the freeze) KEPT — the tag freezes `record.ts`, so the seam must ship in it; it is restricted to the REST request channel, advances nothing, and `unseal`'s auth-then-sequence order survived the `openFrame` extraction. Adversary spawned isolated (detached at 91ce3f18 + staged diff, `diff -r` clean, anonymous one-shot Opus 5, spec-only). Its 20-row table is W1a's acceptance.

- 2026-08-29 07:22 IDT — Owner: usage-limit pause 06:00–07:20 (second of the program). Nothing merged or opened meanwhile; children continued under their standing instructions (F2 awaiting Maestro + owner conditions, W1a fixes, D holding).

- 2026-08-29 05:40 IDT — Owner read of W1a (`.worktrees/feat/e2ee-record-impl`, suite 2540/0 on run 2; run-1 red = pre-existing `watch-for-jsonl` 30 s hook flake, verified untouched since #593). Core accepted. Returned three required fixes: (1) `/open` charges an IP bucket only after a failed handshake and never consults it first — garbage msg1s cost two DH each, unbounded; (2) `noise.ts` picks the psk-less `IK` pattern whenever `psk` is absent — a forgotten `psk` at pairing silently drops the token binding; make the pattern explicit and fail closed both ways; (3) `retire()`d contexts stay in the registry until someone `get()`s that exact id — never, for a replayer — so memory grows per captured msg1; sweep per device on `open()`, and make `destroy()` drop tickets for a never-registered ctxId. W's own six NONCE-DESIGN corrections (msg1 `{ v, kind }`, msg2 shape, rate-limit policy-not-arithmetic, 10 s drain, "rejected" wording, §17 row) accepted → rev4.

- 2026-08-29 04:04 IDT — F: a status check addressed by bare agent name reached a stale earlier sub-agent instance (names collide across turns; the live one is suffixed `-3`); the stale instance found the F2 implementer's uncommitted work in the shared worktree and correctly stopped and asked. No damage. Rule sharpened for all groups: address sub-agents by their exact spawned name/agentId, never a bare name a previous turn also used; a re-woken stale instance must be told to go idle.

- 2026-08-29 03:50 IDT — Owner: re-review 2 (`tracks/REVIEW-2026-08-29-mega-brain-2.md`) accepted. Reviewer independently confirmed the stop-work exposure was nil (own grep; keys = public #631 vector output). Gate items before W1a's tag, all ruled do: N-M1 (§8 tables still carry the abolished rekey trigger and HKDF ctxId), N-H1 (REST context advertised 24 h but provisional 30 s until first use — advertise the provisional deadline), N-M2 (pin REST target canonicalization: raw incoming URL, %2F fixture), N-M4 (`/open` vector), N-M5 (negative vectors). Non-gating: N-M3 (rate limit collapses behind the tunnel — per-device cap is the bound), N-M6 (sealer eviction must be retryable), N-M7 (evict provisional before live, honour the drain), HandshakeKeys hygiene assertion, six adversary rows. N-L1 (Cloudflare Access rejects credential-less sealed requests) → D2 named-tunnel-behind-Access pass and R's rollout guide. F held to confirming `ServerEditModal` add mode reaches the same fetch-error surface before deleting `AddServerScreen` (already in F2's plan).

- 2026-08-29 03:25 IDT — Owner: W verified the scrub with a planted-decoy positive control then an exact-value grep across both worktrees, the scratchpad, 22 evidence files, suite logs and `tracks/`: **zero hits outside three fixture files** — and the two traffic keys are byte-identical to `clientToServerKey`/`serverToClientKey` in `__tests__/fixtures/noise-ikpsk1-vectors.json`, **public on origin/main since #631 (8ed91593)**: the record vectors were derived from the committed handshake vector's `split()`. No non-public value was exposed; nothing pushed. The mechanism (enumerable `k` printed by vitest's differ / `util.inspect`) was real and is fixed with a red mutation; `JSON.stringify` is not a protection (throws on the bigint). Recommendation to the user: continue; keep the §6 trigger as written.

- 2026-08-29 03:08 IDT — **User ruled B3 in W's pane: Alternative B — no in-place rekey.** A key is never replaced inside a context; a new key is a new context; `rekey()`, `bytesSealed`, epoch and the cross-generation replay case deleted; CLAUDE.md §3's "counter surviving a rekey" retired with the reason in NONCE-DESIGN §6. Owner's parallel ask withdrawn.
- 2026-08-29 03:08 IDT — **STOP-WORK (CLAUDE.md §6) raised by W**: an `expected Context{…} to be null` assertion serialised nested `RecordState`s including `k` (both 32-byte traffic keys) into vitest output and the implementer's mutation-evidence files. Keys are ephemeral test-harness keys, never a device's; nothing pushed. Implementer fixed it (`k` non-enumerable, test + red mutation) and reports the dumps scrubbed; `JSON.stringify` throws on the bigint counter so it is not a protection. Owner: W1a paused; W verifies the scrub by grep with a zero count; put to the user with a recommendation to continue. Also: implementer found two §15 mutations that could not go red (`direction`/`counter` bound in nonce AND AAD) — rows rewritten to full-binding mutations; implementer's deletion edits to `server.ts`/`misc.routes.ts` reverted to re-exports.

- 2026-08-29 03:07 IDT — User: "go" typed directly in F's pane; owner confirmed the hold is over (review applied, no finding against F or D) and released D's pre-setup too (runbook → owner approval → rig). B3: user advised to pick option 1 (no in-place rekey) in W's pane.

- 2026-08-29 02:20 IDT — Owner: `e2ee-mega-brain-reviewer` report accepted (`tracks/REVIEW-2026-08-29-mega-brain.md`). No finding against NONCE-DESIGN §2–§5 or the WIP record.ts; the three blockers are unspecified seams. **Ruled**: B1 `/open` = `Noise_IK_25519_ChaChaPoly_SHA256`, prologue `threadbase-e2ee/1 open`, no PSK, vectors committed; B2 `RestResponseSealer.seal(requestCounter)` + one-sealed-response-per-accepted-counter + principal from context (no `Authorization` on sealed requests) + `sha256(method‖path‖query)` in the REST AAD; H1 provisional contexts dying with the 30 s ticket, per-device cap, rate limit on `/open`; H4 authenticate before sequence-check; M1 server-assigned `ctxId` + wire-encodings table; M2 bounded reader for `/open`; M4 `destroyDevice`; M7 re-export not move; M11 negative vectors; M12 ticket in a WS header; L1–L12 as written; adversary brief gains the eight verifier rows. **Escalated to the user**: B3 (retire in-place rekey — recommended, but it retires CLAUDE.md §3's "counter surviving a rekey" guardrail), H3 (export declaration false today), N1 (D-8 option 3, decided at R2). Ruling (b) endorsed by the reviewer; a bounded resync on reconnect rejected.

- 2026-08-29 01:51 IDT — User: hold F and D until the independent review (`e2ee-mega-brain-reviewer [5e0bbf]`) lands. Owner: kick-offs had gone out at 01:55/01:57 — converted to holds (read, re-verify, arrival report only); W's implementer continues but nothing commits and no adversary spawns until the review's findings are applied.

- 2026-08-29 01:45 IDT — Owner (**user may overturn**): design.md §4.3 ("the transport context follows the socket") contradicts mobile-design.md §4.3 ("socket reconnect: reuse the context, fetch a fresh ticket"), and a device-wide context shared by socket and REST would (a) leave the 2 s HTTP replay fallback without a context when the socket is down and (b) turn in-flight frames lost at a drop into an R2 gap on reconnect — a self-inflicted close loop. Ruled: **one context per channel instance** — each WebSocket opens its own context (lives and dies with the socket, counters from 0 as a new context), REST keeps a long-lived per-device context (24 h / 1 GiB / foreground rekey, sliding window by X-server). Two IK handshakes per device; the single-use ticket already implied an open per socket. W1a corrects both design docs. Also: `summarizeQuery` gets an always-reduced sensitive-key set in W1a (the ticket test uses an all-digit ticket, otherwise it could never have failed); `E2EE_EXCHANGE_VERSION` collapses into the canonical constant under `src/e2ee/`.

- 2026-08-29 01:10 IDT — Owner: Group M closed. Three lessons adopted as program rules: (1) **audit verdicts get an isolated second reader** (a context-free agent corrected M's first table from five holds to four) — added to F's brief, applies to every remaining audit; (2) a passing suite is not evidence a guard holds — only a mutation that stays green proves the gap, and two of M's three defects were found exactly that way; (3) one interop run against the real pinned streamer (~20 min) found the only defect no static reading could (an invalid curve point) — X-client and D keep interop in the bar. F and D pre-setup gates fired; both sessions await the user.

- 2026-08-28 22:55 IDT — Owner: Group P closed. Net: one PR (#739, test + comment), zero code defects in the merged pairing contract; the gap was coverage (the only registration-failure test stubbed the repository). Streamer #590's Phase 2 checklist is closed out by comment; the issue stays open for Phases 3–5.

- 2026-08-28 22:45 IDT — Owner: the streamer full suite is ~18 min alone (1079 s), so serialized rounds are ~36 min per two tracks — tracks iterate on targeted `vitest run <file>` and take the lock once per commit-approval request. W0 implemented by W's orchestrator itself is accepted (mechanical, no crypto); from W1a on, the implementer sub-agent writes and the orchestrator only reviews — reviewer ≠ author is the separation that matters where a plausible wrong answer passes review. Same rule applies to M's PR (c).

- 2026-08-28 22:40 IDT — Owner: M amended (b) with a header stating the test pins the web constant, not Metro's wiring (jest resolves the bare specifier to the native module permanently; adding `web` to `haste.platforms` would change resolution repo-wide — not done). Wiring proof routed to **Group D row 7 (web build in a browser)**. Full-rigour results on the merged tip: G1 and G3a/G3b GREEN with the guards removed (2140/2140 — the defects were real), G2 RED (#900 works); all seven reddening mutations red, incl. M4 (outer envelope wins: `Expected "authenticated-machine" / Received "outer-machine"`) and M6 (web completes an encrypted pairing and pins). Operational rules for all groups: full suites in background/parallel (a 600 s foreground kill left a mutation applied; SHA fingerprinting caught it); the repo's isolation re-run rule is load-bearing at 228 suites.

- 2026-08-28 22:35 IDT — Owner: M's PR (b) read and cleared (3 test files, +252; scanner/paste/web surfaces; three mutations red, controls green; explicit-path import of `secure-store.web.ts` works under the unchanged preset). Second two-writers near-miss in M: the orchestrator edited the audit tree before the sub-agent had acknowledged its move; the sub-agent found foreign dirty files, refused to stash under the stashing policy, and stop-worked — the harness working. Rule strengthened and broadcast: **a dispatched agent is presumed resident in its tree until it acknowledges a move**. Sub-agent M1 evidence: `app/pair.tsx:66` `!== undefined` → truthiness makes an empty-`spk` deep link render "Pairing failed" with a live "Try again" — the downgrade on screen.

- 2026-08-28 22:20 IDT — Owner: mobile **#900 MERGED → e35124b0** (item 7 closed, 10/10 green). M recorded a process fault of its own: it re-pointed the audit worktree to the (b) branch while its sub-agent was under instruction to run mutations there — two writers in one tree, the §6 hazard; nothing corrupted (tree clean, no measurement yet), sub-agent moved to its own worktree with a real `npm ci`. Rule broadcast to P and W: **a mutation-running sub-agent gets its own worktree from dispatch**; the orchestrator is the likely second writer because it merges.

- 2026-08-28 22:15 IDT — Owner: M corrected its own table — **item 6 is a coverage defect**: the root `jest-expo` preset's haste platforms are `android/ios/native`, so no `.web.ts` is ever resolved by jest and `secure-store.web.ts:17` can flip to `true` (web writes `D_priv` to `localStorage`) with the suite green. Lesson for every mobile track, X-client especially: a `.web.ts` guard needs a test that imports it by explicit path. Item 1 sharpened: deleting `serverPublicKey: parsed.spk` at `PairScannerModal.tsx:89` or `useTBPair.ts:80` silently pairs an E2EE QR in plaintext with the suite green — those two deletions are (b)'s mutations; the `__DEV__` mutation is withdrawn. Item 6 folded into (b). Item-5 modal gap and the onboarding `publicUrl` drop (`ConnectStep.tsx:167-177`) → Group F. M's sub-agent did deliver (21:54); the idle notices preceded its content — "idle" ≠ "done".

- 2026-08-28 22:05 IDT — Owner: M's named sub-agent `mobile-pairing-trust-boundary-engineer` signalled idle twice with no report; M did the audit, interop and PR (a) first-hand. Rule broadcast to P, M, W: results come from one-shot synchronous Agent calls (report = tool result); named teammates only for back-and-forth and only without a `tools:` frontmatter (a pinned list drops SendMessage — the known trap in the global rules); an idle notice is never a deliverable. Mobile **#900 open** (3c7ca880, user-approved 21:49), CI watching.

- 2026-08-28 21:48 IDT — Owner: P's full-suite runs were being killed while W's `vitest run` (PID 48174) ran in a sibling worktree; a 3-day-old orphaned mobile jest (PID 23450, PPID 1) is also on the box. Rule: streamer full suites serialize across worktrees via `mkdir /tmp/tb-streamer-suite.lock` (rmdir in a trap); targeted runs and lint stay concurrent. The orphan is raised with the user, not killed by the owner.

- 2026-08-28 21:40 IDT — Owner: M's audit accepted. Both defects are harness holes, not shipped downgrades (production `__DEV__` is false; the guards are correct); fixes are test-only, two PRs. #698's "wrong `v` is a hard error" is superseded by the shipped behaviour (bad `v` dropped at parse, version enforced at the handshake so a one-character edit cannot reach a downgrade) — owner corrects the issue text. Two new mobile issues to be filed by M (DEV-only unverified fingerprint P3; unordered double persist in `addServer` P2 → F candidate).

- 2026-08-28 21:35 IDT — Owner (**user may overturn**): design.md §3.3 ("counter never reset") contradicts §4.3 ("reset to 0 only as part of a rekey"). Ruled: **the counter does not reset on rekey** — Noise §11.3 semantics, key replaced, counter continues; the 2^64−1 refusal is the only ceiling; no epoch in the AAD. Sides with §3.3 and CLAUDE.md §3 ("the counter surviving a rekey"), and removes the trap of a Noise-shaped `rekey()` helper silently diverging. §4.3 gets a one-line correction in W1a's PR; NONCE-DESIGN.md is the resolving document. Also ruled: 24 h destroys the context (client re-opens), rekey triggers are 1 GiB and foreground; strings frozen at W1a's tag: `E2EE_CTX_UNKNOWN`, `E2EE_DEVICE_REVOKED`, close reason `E2EE_SEQUENCE_VIOLATION`; AAD version reuses `E2EE_PROTOCOL_VERSION` (=1, `misc.routes.ts:119`); W0 no-bypass = regex scanner test, type-level variant deferred.
- 2026-08-28 21:21 IDT — Owner: usage-limit pause ate the 15-minute stagger; M and W kicked off one minute apart rather than waiting another 15 — the stagger guards against simultaneous limit stalls, and a further delay costs more than it protects at this point in the evening.

- 2026-08-28 20:57 IDT — Owner: #737 squash-merged on 12/12 green (user go 20:35 covered rebase-then-squash on green). Docs-only, so semantic-release cuts no tag; P and W rebase onto 76d6d420 before opening PRs.

- 2026-08-28 19:40 IDT — Owner: read `CLAUDE.md`, plan revision 1, `remaining-work.md`, `plan.md`, `design.md` §3–§10, `mobile-design.md` §3.3–§10, `dilemmas.md`, `understanding.md`, `parallel-kickoff.md`, issues #590/#698 and the prior program's briefs, all spec files cited from `origin/main`.
- 2026-08-28 19:50 IDT — Owner: re-verification. Streamer #630 MERGED 08-16; #649 (msg1/msg2 authentication, conditional QR, mandatory registration) MERGED 08-18; **#674 flipped `E2EE_SUPPORTED` to `true`, MERGED 08-23 → v1.69.0, with its five device-evidence gates unchecked and no evidence comment** — deployments unaffected because the `e2ee` flag defaults `false`. Mobile #768 MERGED 08-20, #766 08-18, #782 wired the gate 08-20, #833 modal fix 08-22. #759's code path exists (`stores/servers.ts:292`, `pair-exchange.ts:596`), issue open. Current streamer tag v1.70.6. `parallel-execution-plan.md` never committed on `docs/e2ee-parallel-plan` — becomes PR 0.
- 2026-08-28 19:55 IDT — Owner: plan revision 2. P and M become audit/close-out tracks at their original tiers (a wrong "it holds" still ships silently); F shrinks to #759 evidence + #831; D loses the flip and gains D2; **X-client absorbs the mobile WS half (revision 1 had no owner for it) and rises to Opus 5 / high with an isolated verifier** — an eighth deviation beyond the seven agreed flags, for the user to confirm or veto; X-server keys off W1a's release tag as well as the approved `NONCE-DESIGN.md` (it imports the context registry — a code dependency). W split into W0 / W1a / W1b so X-server can start on W1a's tag while W1b is in review. The user-gated flip is now R's stage-2 flag default.
- 2026-08-28 20:00 IDT — Owner: the strict NO-GO in `CLAUDE.md` §2 names `E2EE_SUPPORTED`; that constant is already `true` on `main` by the user's own merge. Treated as: the NO-GO now binds the stage-2 `e2ee` default (the only flip left that changes deployments) and stage 3. Flagged in the deliverable rather than silently re-interpreted.
- 2026-08-28 20:05 IDT — Owner: wave 1 stays P, M, W as instructed even though D1's preconditions may close within days; D1 fires by owner message on P/M/F close-out, not on a date.
- 2026-08-28 20:35 IDT — User: go on PR 0 and wave 1. 20:36 commit approved on the staged diff and verbatim message; #737 opened. Wave-1 kick-offs wait on the user creating the three sessions; P first, M +15, W +30.
- 2026-08-28 20:45 IDT — Owner: applied `orchestrator [2b0bc8]`'s nineteen review findings (list in the plan's decisions log). Net tier changes: X-server → Opus 5 / high, D2 → Sonnet 5 / medium; session names `e2ee-Xserver-opus5-high`, `e2ee-D-sonnet5-medium`. R1 now fires a wave earlier.
- 2026-08-28 20:15 IDT — Owner: verifier sub-agents are spawned by the group session as fresh `general-purpose` agents with only the spec paths and the worktree commit, never as teammates and never with the implementer's context. Group sessions do not message each other; `e2ee-owner` is the only join point.
