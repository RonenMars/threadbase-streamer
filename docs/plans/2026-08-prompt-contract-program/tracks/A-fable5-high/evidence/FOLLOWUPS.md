# Group A — engineering follow-ups from the Phase 3 gate (2026-08-28)

Not filed by the orchestrator; owner files. Verified state = scorecard rows (all PROVEN BY LIVE CAPTURE unless noted).

| # | Repo | Proposed title | Verified state | Done looks like |
|---|---|---|---|---|
| 1 | tb-streamer | `P3: fix Claude fixture/manifest drift (2.1.214 → 2.1.247; add queue-operation, ai-title, atis-latch)` | C0 | `provider-fixtures.test.ts` + `providerHealth.ts` pin the installed version and the manifest lists all 27 captured frame kinds; health no longer reports `version_unverified` on 2.1.247. |
| 2 | tb-streamer | `P3: never log Claude result frames verbatim (permission_denials carries tool_input content)` | C02 | Content-free logging test asserts no `tool_input`/Write body from a denied `result` frame reaches any log; MUT (log the frame) fails. |
| 3 | tb-streamer | `P3: streamer-owned single-writer and exclusive-handoff enforcement per session id` | C07, C09 (Claude interleaves), X0.3, X01, X07 (Codex refuses) | One live structured client per session id; attach to a session with a live terminal owner is refused by the streamer; reconnect = quiesce old client then attach; two-client test proves exactly one writer. |
| 4 | tb-streamer | `P3: streamer-owned prompt-shape validation, fail-closed refusal and expiry for structured prompts` | C03, C04 (Claude: no validation, partial accepted, dialogExpiry does not bound stdio park), X0.4-flagged, X05-flagged (Codex: no id validation, refusal hidden from model) | Malformed/partial/mis-keyed answers return typed `incomplete_answer` with zero bytes to the provider; refusal is surfaced to the user; expiry is streamer-timed (`CLAUDE_CODE_USER_DIALOG_TIMEOUT_MS` documented for Claude). |
| 5 | tb-streamer | `P3: provider crash cancels the parked prompt (no replay)` | C06 (parked AskUserQuestion persists unsettled, `--resume` does not re-arm), X04c (turn `interrupted`, nothing re-delivered) | On provider exit every open prompt for that session settles as `cancelled:provider_exit` and subscribers receive the cancel event; reconnect shows no ghost prompt. |
| 6 | tb-streamer | `P3: assign sequence numbers on ingest for structured events` | C05, X03 (no wire sequence on either provider) | Every structured event carries a streamer-assigned monotonic `seq`; subscribe returns snapshot + `seq`; gap detection tested by dropping one event. |
| 7 | tb-streamer (+ tb-mobile display) | `P3: map secret tri-state to unknown for Claude- and Codex-sourced prompts` | D12 via C03 (no secrecy field on Claude frames), X0.6-flagged (`isSecret` model-chosen) | Contract mapper sets `secret: "unknown"` for both providers; test flips to `true` and fails; mobile masks input on `unknown`. |
| 8 | tb-mobile | `P3: structured activity view — explicit interrupted terminal state per item` | X03 (interrupted command gets no `item/completed`), C05 corpus | Fixture-backed view renders `interrupted` for items with no completion after turn interrupt; no synthesized terminal text. |
| 9 | tb-streamer | `P3: Codex adapter (internal-only) gated on default_mode_request_user_input negotiation` | X02/STOP-T7, X0.4–X0.6-flagged | Adapter refuses to expose questions unless `experimentalFeature/list` reports the capability enabled by default; approvals-only otherwise. |
