# Orchestrator notes for GO-NO-GO (owner inputs, 2026-08-28)

Codex (default-config scorecard ACCEPTED by owner):
1. X01 + X07 ⇒ Codex external-session support can only ever be exclusive handoff after terminal exit, never concurrent (decides D6 / external-session UX; product-owner sign-off).
2. X03: no sequence numbers (emittedAtMs + ids only); interrupted command gets no item/completed ⇒ display-architecture gap the structured view must absorb (doc assumed sequence gaps detectable).
3. X04c (no replay after provider death) + X06 (no stdin/resize to agent-spawned processes; server-created ids) ⇒ D15 "not preserved" ⇒ Codex structured sessions internal-only / product-limited.
4. X02/T7 ⇒ Codex questions internal-experiment-only until default-on and negotiable without hidden config. Flag-gated rows are mechanics-only.
5. X0.4-flagged (mechanics only): with the flag, item/tool/requestUserInput emitted with questions[].id and id-keyed answer settles the turn. **No server-side question-id validation** — an answer keyed by a non-existent id is passed to the model unvalidated. ⇒ D9 consequence: streamer must own id validation; provider will not reject a mis-keyed answer.
6. Flag-gated pass complete (6 mechanics rows, all pass, verified: 0 shell_snapshots, 0 auth/credential copies, 0 key-shaped strings in preserved evidence). Findings for the contract: every question needs non-empty options (free text only via isOther:true); isSecret is model-chosen ⇒ not a trustworthy privacy signal (D12: streamer tri-state stands); isBlocking:false while the turn blocked (D11); pending question survives client SIGKILL and is re-delivered with the same id on resume; refusing a request with a JSON-RPC error makes the server hand the model `{"answers":{}}` — model not told of refusal ⇒ fail-closed must be streamer-side and visible to the user, not delegated to the provider.
7. Hygiene finding (not a probe row): Codex writes `CODEX_HOME/shell_snapshots/*.sh` capturing the login-shell env incl. real API keys. Deleted from evidence. The real `~/.codex/shell_snapshots/` (11 files) presumably holds the same — user's machine, untouched, flagged to user.
8. Owner (D12 verdict line): Codex isSecret is model-chosen ⇒ a Codex-sourced prompt cannot carry secret:true/false truthfully; Phase 2 tri-state maps it to `unknown` for Codex, same as PTY-scraped prompts, until the provider validates it.
9. Owner (hard requirement for any Codex structured adapter): streamer owns the whole answer boundary — validate ids (#700 registry) and surface refusals to the user, because the provider silently proceeds with `{"answers":{}}`.
10. Claude STOP-T2 (C07, PROVEN BY LIVE CAPTURE): `--resume <live sid>` is not an exclusive attach — two processes appended to one transcript, both exit 0, DAG cross-linked; no byte tear observed this run. NC: nonexistent sid refused. ⇒ streamer must own single-writer enforcement for Claude; reconnect = exclusive handoff only.
11. Claude prober final (halted at C07). D3-material findings:
   - C03 GAP: no live `request_user_dialog` frame in `-p` stream-json even with supportedDialogKinds declared (CLI returns -32602); dialog kinds + {behavior:completed/cancelled} are DOCUMENTED-TYPED from the binary only. AskUserQuestion arrives as can_use_tool; MCP forms as elicitation. Single/multi/free answers accepted verbatim, no validation (multi = comma string); partial answer accepted as complete ⇒ streamer must validate shape (fail-closed stays streamer-side, as for Codex). Refusal ⇒ typed error, zero answer bytes.
   - C02 caveat: result.permission_denials[].tool_input echoes full Write content ⇒ content-bearing frame; content-free logging must not log result frames verbatim.
   - C04: override var CLAUDE_CODE_USER_DIALOG_TIMEOUT_MS (empirical); settings dialogExpiry=60s does NOT bound a stdio permission-prompt park (remote clients only); default park >400s ⇒ streamer owns expiry.
   - C05: 27 frame kinds, 0 gaps, but no wire sequence number (stdout order only) — same display-architecture gap as Codex X03.
   - C06: provider crash with parked prompt ⇒ AskUserQuestion tool_use persisted UNSETTLED; --resume does not re-arm (pending_user_dialog_requests re-arms live sessions only) ⇒ streamer must treat provider crash as cancelling the parked prompt (mirrors Codex X04c).
   - C0: 3 envelope types absent from streamer manifest: queue-operation, ai-title, atis-latch; `permission-mode` not observed.
   - Hygiene: 12 credential copies deleted; user email leaked via init/account block into P04 frames + probe .claude.json — scrubbed/deleted; 0 email/oauth/token/Bearer strings remain.
