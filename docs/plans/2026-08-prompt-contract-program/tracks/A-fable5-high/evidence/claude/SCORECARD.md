# Claude gate scorecard — Claude Code **2.1.247** (Fable 5 / high, track A)

Binary: `/opt/homebrew/bin/claude` → `/opt/homebrew/Caskroom/claude-code@latest/2.1.247/claude`
sha256 `5086b9b64d8bb842e1f599cdd3767ab08c6b2266e462fcc5686ae4b019cca8f7`; `claude --version` = `2.1.247 (Claude Code)`; `which -a claude` = single hit (that path). Every probe ran with `CLAUDE_CONFIG_DIR=<probe>/claude-home` (PROTOCOL §8), auth seeded from Keychain into a file store (macOS per-config-dir keychain service means the redirected home has no keychain entry — the `.credentials.json` copy is required; content never logged). Real stores unchanged: `~/.claude/projects` → `/Users/ronenmars/dotfiles/ai-tools/claude/projects` (881 entries, identical before/after C01); `~/.codex` untouched (Codex isolation preserved). Authoritative runtime object throughout: the claude child process driven over `-p --input-format stream-json --output-format stream-json` with control-request/response frames (NOT the SDK wrapper, NOT the TUI except C09 which was not reached).

**C07 fired stop-work T2** (two writers on one transcript); the owner then lifted the halt for C08/C09 (capture-only — see STOP-T2.md "Owner ruling"). Both ran. **C09 reproduces T2 from the terminal side (concurrent-success with cross-linked DAG, FAIL-for-product).**

| id | probe | verdict | tag | evidence dir | PC | NC/MUT | flipping control | time_spent | notes/gaps |
|---|---|---|---|---|---|---|---|---|---|
| C0 | version drift 2.1.214/239-241/247 | fail (drift confirmed) | PROVEN BY LIVE CAPTURE | (all P0*) | n/a | n/a | n/a | 10m | Every 2.1.247 transcript line carries `version:"2.1.247"`. The streamer fixture pins `2.1.214` and asserts `[...versions].toEqual(["2.1.214"])` (`provider-fixtures.test.ts:111-121`) + `providerHealth.ts:21 captured:["2.1.214"]` → a 2.1.247 capture **fails** that version-pin test and lands in `version_unverified` (newer-than-captured). Of the fixture's 6 `envelopeTypes` (user, assistant, attachment, last-prompt, mode, permission-mode): **5 still emitted** (user/assistant/attachment/last-prompt/mode); `permission-mode` not observed (not triggered — needs a mid-session permission-mode change). **3 new envelope types 2.1.247 emits that the manifest omits: `queue-operation`, `ai-title`, `atis-latch`** (the "exercises every claimed type" test tolerates extras; the version test does not). |
| C01 | session identity | pass | PROVEN BY LIVE CAPTURE | P01-session-identity | hand-append visible via same reader | MUT wrong-path assert fails; ID before/after dir listing | MUT sentinel-at-wrong-path | 6m | `system/init` emitted only at first turn, not on `initialize` ack. One file `<sid>.jsonl`, all lines `sessionId==init sid`, `result.session_id` equal; real store unchanged. |
| C02 | can_use_tool allow vs deny (Write) | pass | PROVEN BY LIVE CAPTURE | P02-can-use-tool | allow → file exists w/ payload; tool_result "File created" | NC deny → absent, `permission_denials[]`; MUT | deny run | 5m | Gate only with `--permission-prompt-tool stdio`. Wire keys: `subtype,tool_name,display_name,input,description,permission_suggestions,tool_use_id`. Note: `result.permission_denials.tool_input` echoes full Write `content` — content travels in the result frame. |
| C03 | request_user_dialog kinds + partial/unknown | pass (with a documented gap) | PROVEN BY LIVE CAPTURE | P03-dialogs | auq-single / elicit-form answered, answer reaches model | NC: auq-deny, url-undeclared, fabricated bogus; MUT | auq-deny vs auq-single | 14m | AskUserQuestion arrives as `can_use_tool`; MCP form as `elicitation` (mode=form, requested_schema). Single/multi/free all accepted verbatim (no option validation; multi = comma-joined string). **Partial answer accepted as complete** (no incomplete marker; model noticed). Refusal `{behavior:"deny"}` → typed error tool_result, **zero answer bytes**, `permission_denials[]`. Fabricated: unknown request_id ignored; **bogus `behavior` → "invalid permission result" error, answer never lands**; error-subtype **settles** a can_use_tool; malformed stdin line → CLI exit 1, no result. **GAP:** no live `request_user_dialog` frame in `-p` stream-json — even with `supportedDialogKinds:["mcp_url_elicitation"]` the CLI returns JSON-RPC `-32602 Client does not support URL-mode elicitation`. Dialog kinds + `{behavior:"completed"/"cancelled",result}` schema are DOCUMENTED-TYPED from the binary only; `resume_return`/`cost_threshold` gated behind GrowthBook/thresholds, out of time-box. |
| C04 | expiry + env override | pass | PROVEN BY LIVE CAPTURE | P04-expiry | env10s deadline moves to injected value | NC answer-in-time wins; auqtimeout/settings/default do not expire; MUT | env10s vs answer | 22m | Override var (found empirically, not docs) = **`CLAUDE_CODE_USER_DIALOG_TIMEOUT_MS`** (baseline env has no dialog/timeout var). Default park does NOT auto-cancel a stdio `-p` prompt within 400s. **`settings.json dialogExpiry=60s` did NOT bound the stdio park (no settlement in 120s)** — the setting governs a *remote forwarded* client; a stdio host is not "remote". `askUserQuestionTimeout` is a different (auto-continue) knob, also no park expiry. Expiry settlement carries zero answer bytes (safe default = cancelled); after expiry no `result` within 60s (D11). |
| C05 | display corpus | pass | PROVEN BY LIVE CAPTURE | P05-display-corpus | all 12 categories triggered, 0 gaps (`corpus.md`, 27 frame kinds) | MUT removes interrupt trigger → `corpus-mut.md` gaps=interrupt,error | MUT | 12m | Deliverable `corpus.md` + `corpus-mut.md`. Interrupt handshake, dual error surfaces (tool_result `is_error` + `result/error_during_execution`), ordering = stdout line order only (no wire sequence number; `stream_event.event.index` is the content-block index). Extra non-transcript frames: system/status, thinking_tokens, background_tasks_changed, task_started, rate_limit_event. |
| C06 | failure paths | pass | PROVEN BY LIVE CAPTURE | P06-failure-paths | each prompt confirmed parked; ps before/after | NC no-pending resume usable | resume-usable vs orphan | 9m | (a) stdin-close mid-turn → provider exits clean, no orphan. (b) SIGKILL / (c) SIGTERM with pending dialog → AskUserQuestion tool_use **persisted but unsettled** (orphaned). (d) `--resume` after crash: prompt **NOT re-armed** (`pending_user_dialog_requests` empty — that field re-arms only a *live* session), orphan harvested, session usable (`PHASE2-OK`, same sid). **Recovery path exists (no T8); pending prompt reconciled by abandonment not re-delivery (no T6). Streamer must treat a provider crash as cancelling the parked prompt.** |
| C07 | two control clients on one UUID | fail — **T2** | PROVEN BY LIVE CAPTURE | P07-two-clients | each sentinel via result text | NC wrong-uuid refused (`No conversation found`) | one file, two live writers | 4m | **STOP-T2.** `--resume <sid>` of a **live** session is accepted (not exclusive, no file lock); client A and client B both append to the one `<sid>.jsonl`, interleaved by wall-clock (A2-user 12.276 / B-user 12.338 / A2-assistant 13.995 / B-assistant 14.202), both exit 0. Bytes intact this run (parses, unique uuids) but the conversation DAG is cross-linked. Directly backs CLAUDE.md §2 "never combine/ migrate transports for one session" — **the CLI provides no single-writer guard; the streamer must.** |
| C08 | agent-spawned interactive stdin/resize/interrupt | pass (capture) | PROVEN BY LIVE CAPTURE | P08-agent-interactive | child confirmed running before interrupt (ps) | NC no-stdin identical outcome; interrupt-kills-child | nostdin vs stdin | 11m | **No interactive stdin channel:** the Bash-tool child gets a CLOSED stdin — `readline()` returns EOF instantly (`GOT['']`), our sentinel never arrives; nostdin NC identical, proving fed bytes never reach the child. **Resize N/A** (stream-json has no PTY / no resize control frame). **Interrupt reaches & kills the child** (`sleep 40` child killed, `late.txt` side effect suppressed, result frame returned). A raw non-JSON stdin line is not a channel — it aborts the turn (C03). |
| C09 | terminal-origin continuity (D5) | **fail (for product)** — concurrent success w/ cross-linked DAG | PROVEN BY LIVE CAPTURE | P09-terminal-continuity | all 4 sentinels A–D in the one file; terminal uuid from disk | halt-on-NEW checked each phase (no 2nd file, no tear); terminal usable after attach (C landed) | one file, two origins `cli`+`sdk-cli` | 24m | Real pty (`script -q`) terminal session `e3cce96f…` + two structured `--resume` clients ALL wrote the single transcript, concurrently with a live terminal that stayed usable. Entry origins in the one file: `cli`×6 (terminal) + `sdk-cli`×8 (structured). **Classified: concurrent-success-with-cross-linked-DAG → FAIL for product (no clean exclusive handoff exists); NOT fork-corruption (terminal kept working) and NOT clean-handoff.** Terminal-side confirmation of C07/T2. No NEW anomaly (a repeat interleave is not new per owner ruling), so no halt. |

Tags per PROTOCOL §4. `pass` rows are PROVEN BY LIVE CAPTURE with PC + NC/MUT + EE all present for 2.1.247.
