# Track program status

Owner: session `orchestrator` (renamed by the user 16:37; was `ai-investigation-claude-40` after a usage-limit pause 13:47–16:20, and `-67` before that; peers re-addressed each time). Sampled every 25 minutes (cron `21cf77dd`, from 07:47 2026-08-28); updated on every report as it arrives.

Last update: 2026-08-28 16:22 IDT (owner back from usage-limit pause; status re-requested from B (#721) and C (phase C) under the new address; streamer main is v1.70.3 via #725 (outside this program); A, D closed; E held.)

| Group | Session | State | Last report | Next gate | Blockers |
|---|---|---|---|---|---|
| A — Phase 3 gates | `fable5-high` (session closed 16:40) | **Codex default-config scorecard complete & accepted 08:13** (spot-checked X01, X06 on disk): X0.1–0.3 pass; X02 fail/STOP-T7; X01 refused-terminal-intact (no concurrent continuity, handoff only after terminal exit); X03 pass with gaps (no seq numbers); X04 reconnect pass, provider-death = deterministic loss; X05 pass; X06 stdin/resize fail, interrupt pass; X07 pass. **Both gates complete; GO-NO-GO accepted 08:59** (2 additions: secret→unknown for Claude; product-owner items marked). Verdicts: Claude 2.1.247 CONDITIONAL GO for a one-provider internal pilot (7+1 conditions); Codex 0.150.1 NO-GO for production, internal-experiment adapter only; first production provider Claude. C08 fail-for-D15, C09 "concurrent success with cross-linked DAG" = product fail. **GROUP A CLOSED 09:03.** GO-NO-GO.md FINAL; FOLLOWUPS.md (9 entries, unfiled — owner files); scorecards 10 + 20 rows; STOP-T2/T7 final; hygiene clean; evidence 682 MB (codex-home junk prune approved with manifest). | 18:25 PO decisions recorded (GO-NO-GO §7: watch read-only + exclusive handoff; non-interactive pilot, PTY opt-in) | — | — |
| B — #703, #871 | `opus5-high` (session closed 18:09) | usage-limit stall 09:00–11:20; **both plans approved 11:27**: #703 Candidate A proven, narrowed to `state === "resolved"` (cancelled-but-pending = refused = "open"), `promptState` additive; #871 `gateKey` = gateId unless it contains `::` (namespaces disjoint by construction), orphan comment struck. **#871 staged diff + commit APPROVED (owner read the diff in the worktree; `git tag --contains 22dea01a` → v1.69.4 verified; issue #871 text corrected from 1.69.6)**: **PR #887 open (1b805c55), **#887 MERGED 11:48 → mobile main e1d1fdeb; #871 closed**. **#703 APPROVED 11:47** (suite 2436 pass/0 fail 830 s; mutation 1 → test 1 fails, mutation 2 → only test 8 fails; owner read the handler diff): **#718 MERGED → 81067096, release v1.70.1 (7cde4b9a), no flake, #703 closed. GROUP B CLOSED 12:03**; findings: ws-client onerror = mobile #886 (already filed), tsconfig `__tests__` exclusion → streamer issue filed. Stood down 12:03. **RE-OPENED 12:22: streamer #720 (P1)**; brief dispatched 12:23 (`streamer-answer-settlement-engineer`). Key fact: `transition()` throws on terminal records, so dropping the re-check alone → unhandled throw; in-flight marker required; swallow boundary = own-write cancel only. Owner ruling: exactly one terminal `prompt_event` (`resolved`), legacy `permission_cancelled` may still go out. **#720 plan APPROVED 12:30**: `providerClosed` at the detector call site, `answering`/`deferredClose`, re-checks kept (queue is per-entry only — issue corrected), deferred close applied on failure, wire order cancelled→resolved→200; legacy `/permission/answer` folded in; T1–T6 + M1–M3 (addendum accepted 12:34; 2b statuses confirmed 4×400 from log); `prompt-answer-pty.test.ts` replaces `sendKeys` = why #700 was green. Filed #722 (#703 answered-branch unreachable on Claude, product call). **#720 verified 12:50** (base T1/T6 red, T5 pinned, M1–M3 red, suite 2442/0). 3b ruling (a): ship; transient split to **#724**; T1 carries no transient assertion. **#720 staged diff APPROVED 13:20** (owner read source: `providerClosed` deferral, marker after pre-checks, settle on every non-resolving exit, legacy route resolves `answered_legacy` right after the write) → **PR #726 MERGED 13:30 → 4b8957d6**; **released v1.70.2 (4be88979), CI 10/10 first run, #720 CLOSED, worktree removed 13:33.** **#721 brief dispatched 13:38** (`streamer-shape-classification-engineer`): root at `detectQuestionFromScreen.ts:130` (one question, `multiSelect: false` hardcoded) → `ptyPromptAdapter.ts:47`; mapper faithful, scraper lossy; plan must name which of representation/detection/glyphs it ships, never guess `single`, unanswerable must land in a bucket 40ac02ac already fails closed on; single-select gate unchanged = positive control. **Plan APPROVED 13:45**: leading-marker rule ("any option marked" → multi + strip; unknown bracketed token → refuse; else single), zero bytes structural via `UnsupportedPromptShapeError`; real-screen-path reproduction showed `resolveAnswer → "\r"`. Finding: `fail-closed-answer.test.ts:26` hand-builds `multiSelect: true` that no producer emits — suite vacuous; real-path case required. Legacy-client caveat ruled acceptable (400 toast beats writing `\r`); mobile P3 to follow. Addendum 13:47: harness validated vs prod (`byteLen: 1` at the 200); glyph strip is load-bearing (host-side toggle churns promptId via `questionContentKey`); this write creates #724's orphan; toolUseId length collision filed as **#727**. **Sub-agent capped 13:46 mid-impl; resumed 16:23** from `PLAN-streamer-721.md` in `.worktrees/fix/multi-select-shape-classification` ; **16:37: rebased onto 6ff5957e, 4 files staged (+318/-5: detector +50, detector tests +112, fail-closed real-path test +43, fixture +118), suite 2452/0 (4 shards; CI unsharded is the gate), 5 mutations red incl. M4 on a pre-existing assertion**. **#721 MERGED c925017f → released v1.70.4 (04d67605) 17:25**, CI 10/10, shards reconciled vs CI (248 files/2457 tests both sides), #721 closed, worktree removed. **#723 dispatched** plan-light (`streamer-boot-log-engineer`, real-logger capture test, masked-prefix positive control, one mutation); key rotation flagged to user as operational. **#723 staged diff APPROVED 17:53** (`cli/boot-log.ts` seam kept; `Refs #723`; banner never printed the key — issue text corrected; blinded-harness mutation) → PR → tag → Group B close-out. Mobile legacy-path P3 = **#893** (owner, 16:25). **#723 → PR #729 → b61a4a61 → v1.70.5 (main 8b47fd72). GROUP B COMPLETE 18:06** — 5 tracks, 5 merges, 4 releases; #723 open pending the user's key-rotation decision; worktrees removed; stood down. | 18:06 close-out | — (session closable) | — |
| C — cross-version probe | `opus5-medium` (session closed 18:39) | checkpoint 1: 1.69.6 rig up on :8769. **Isolation incident** (THREADBASE_CONFIG_DIR doesn't move cacheDir; ~2 min with two streamers on the prod cache.db; prod PID 58199 alive; prune 0 rows) → rig now under scratch HOME, zero real-home handles. Ruled 08:04: continue (integrity_check on copy = ok). **Checkpoint 2 accepted 08:29**: app on pinned bundle (served-bundle discriminators from 40ac02ac), paired to rig; isolated `.claude` profile (user's real allow-rules were suppressing gates); curl provocation gates; Maestro/simctl instead of unavailable expo-local MCP. **Row 1 (positive control, 1.69.6) PASS 08:33** — legacy `permission` frame, no `revision`, 0 `prompt_*` frames (grep-validated), card 1:1, scoping proven server-side (subscriberCount), content-free log holds. Row 2 PASS 08:38 (200 on /permission/answer with contentKey+gateId proven via rejection ladder; exactly one PTY write, provider-side). Observation: answer teardown minted a transient fresh gateId (open→cancel in 51 ms) — required phase-B sub-row 3b. Row 4 PASS 08:39 (Escape → `permission_cancelled`, zero bytes with positive control; facts: legacy cancel frame is identity-free; gateId minted per gate not per content). Row 5: 5a PASS, 5c being re-run clean, 5b NOT RUN (no ghost state on legacy). **Harness defect found & fixed by verifier**: client-B script sent on-screen index as array position → silently took "don't ask again", persisted `Bash(curl *)` in the rig; remediated in scratchpad; harness now forbids permissive options. **Finding filed** as streamer security issue (legacy route accepts any in-range position). Sub-rows 2b and 3b required in phase B. After usage-limit rebuild: 5(c) re-run PASS (hard drop via simctl terminate), client-B pre-flight PASS, causal control on the settings rule (rule present → no gate; deleted + fresh process → gate). Row 6 server half PASS (one execution; app 200, client B 409 `gate_closed` 301 ms later). Findings: the losing client addressed the **teardown transient** (born 13 ms after the winner's 200, lived 79 ms) — third sighting (13/29/4 ms); loser cannot tell "answered by another" from "vanished" on legacy (`gate_closed` for both). Sub-row 3b widened: can a second client target the transient on v1.70.0, and does the loser get `already_resolved` distinctly. **Inverted race NOT RUN on 1.69.6 (accepted 11:4x)**: the app withdraws the card on `permission_cancelled` before a human could tap, so the loser never gets to lose — question moves to phase-B 3b (does a resolved `prompt_event` withdraw the same way; `already_resolved` vs `prompt_cancelled` distinct?). Falsifiability run: three refusals + control, zero bytes (3 tool_use/3 tool_result vs three 200s); one row held back by C for per-row reconciliation. contentKey content-addressed finding → appended to streamer #709 (same root as optionIndex). **Row 8 PASS 11:44** (app gates the send locally: draft kept, zero requests, positive control sends 200 after resolve) + server-only **8b** approved (direct POST /input vs open gate). Phase-B carry-forwards: local send gate still engaged on the contract path?; `keys` bypasses the open-prompt check (#692 design — re-verify, not file). **Row 9 PASS 11:49** (fresh session+socket, positive control, 128-frame census: 0 `prompt_*`; method warning: `grep promptId` false-positives via `conversation_event` provider relay — phase B keys on frame `type`). Transient 4th sighting at 0 ms. **5(a) hard-drop PASS 11:52** (simctl terminate; replay_permission 68→68→68→71: nothing to a dead socket, nothing on relaunch alone — session list does not subscribe — replay only on session-screen subscribe; card complete). C sent the verifier back for 8b before close-out (PHASE-A-CLOSEOUT.md). **8b PASS; PHASE A COMPLETE & ACCEPTED 12:01** (PHASE-A-CLOSEOUT.md 467 lines; 9 PASS, 5(b) + row-6 UI-loser NOT RUN with reasons; exit criterion "new mobile / old streamer degrades" MET; method catch: raw-ANSI grep false negative, redone de-escaped). **Phase B dispatched on v1.70.0** (scratch HOME, :8770, provenance re-verified, type-keyed detection). **Phase-B checkpoint ACCEPTED 12:04**: rig 1.70.0+source on :8770 (`promptContract {schemaVersion 1, atomicAnswer true}`), isolation clean from first boot, profile sha-identical, bundle provenance re-verified. Wire-shape rulings: snapshot is a list + sequence; open/updated both actionable; idempotent replay tested in row 2. v1.70.1 promptState delta noted for 8b only. **Phase-B row 1 PASS 12:08** (strong control: legacy `permission` present 1 ms after `prompt_event`, ignored by app; gateId == promptId; card provenance proven by contract-only `title: "Approval"`; snapshot unconditional `prompts=0` on quiet session, mid-gate sequence 1/open/rev 1; opaque optionId UUIDs → 2b target; per-row `promptState` grep = version integrity). **Row 2 FAIL 12:18 — P1, filed streamer #720**: valid `/prompt/answer` → 409 `prompt_cancelled` after the write executed (3/3, zero 200s); root cause confirmed by owner in source (post-adapter state re-check in `performAnswer`; scraped-gate teardown cancels the record). Knock-on: #703 `promptState: "answered"` dead on scraped gates. Continuing: 2b, 7, 3 cursor-only, 3b obs, 5(a), 8/8b; BLOCKED on #720: 4, 5(c), 6, same-key replay. **2b PASS 12:21** (4 refusals incl. real-but-foreign optionId → 400, zero bytes, prompt untouched at rev 1 — #709 class removed on contract route; boundary constraint passed to #720 fix). **Row 7 FAIL 12:27 — P1, filed streamer #721**: mapper published a 2-question multi-select form as `questions 1, inputMode single` (glyphs in labels); app answered the shape it was told; route 200 `resolved` into a still-open form (one byte toggled a checkbox). D10 sub-checks NOT ASSESSABLE on 1.70.0. Cross-finding on #720: only 200 on the rig, differs from row 2 solely by screen disappearance. **Row 3 cursor-only PASS 12:33** (7 legacy : 1 contract frame; contract prompt has no `cursor` field — property, not sample; identity rule assembled: new gate → new promptId, state change → revision bump, cursor → nothing); content half BLOCKED on #720. **3b case (b) 12:40, 5/5**: teardown mints a fresh `open` promptId rev 1 (19–74 ms for gates; row 7 phantom still open 12 min later) → recorded on #720 as consequence 3 + fix requirement (post-answer snapshot has zero open prompts). Rig API key found in tap argv → owner scrubbed all tracks/ copies. **3b observed 12:44**: app rendered a card for the teardown-minted prompt (durable row-7 case) while the original reads `resolved/answered` — two records for one question. **Central cross-version result**: contract client (promptId-keyed) structurally more exposed to the transient than legacy (content-keyed). Ruling: server-side dedupe (#720), client keeps promptId. **5(a), 8, 8b PASS; PHASE B COMPLETE; PROBE-REPORT APPROVED 12:52 — Phase 2 exit criteria NOT MET** (legacy half met; contract half fails server-side: #720, #721, #724; app correct in every row; 5 cells BLOCKED). Evidence durable at `evidence-scrubbed/` (112 files); PHASE-B-CLOSEOUT reconciled (490 lines). **PHASE C GO 13:34** on v1.70.2 exact pin: row-3 content half + same-key replay first (stop for review), then 4, 5(c), 6, then live row-7 phantom (expected still present). **Phase C cells 1–2 PASS (reviewed by C 16:30 under the standing GO): #720 FIXED on 1.70.2** — row 2: 200 `resolved/answered`, 5/5 answers 200, 0 409s; same-key replay: byte-identical 200s, one tool_use, one artefact; row-3 content half PASS (`revision` = state counter; content change → new promptId). Rig pinned by exact dist hash after the verifier found its tripwire only bounded from below. Rows 4, 5(c), 6 + phantom re-provocation running. User confirmed `-40` in C's pane 16:34; cells 1–2 formally accepted by owner. **Rows 4, 5(c), 6 PASS 16:49 (accepted)**: card clears on `resolved` (no ghost state on the wire); terminal-only snapshot + hard kill → no card; race loser gets **`409 already_resolved` distinctly** (+301 ms, fresh key) — the separation 1.69.6 lacked. Phantom expected-still-present; narrowed: gate transients self-cancel ~350 ms, durable case = question menu behind #721 (noted on #724). Evidence 134 files. C flagged a coverage gap: no single-select AskUserQuestion was ever provoked on the contract path → **row 7a ordered 16:51** (minutes on the live rig) before the verdict is written. Close-out goes to PHASE-C-CLOSEOUT.md with a §10 pointer. **Row 7a PASS 16:59**: single-select question mapped honestly (`inputMode single`, options match screen), 200 `resolved`, one write, card cleared → #721 is shape-specific, not blanket. Transient lifetime by shape: gate 0–0.4 s, single-select menu **60 s**, #721 form **durable** (the "~355 s self-cancel" was the harness's own stop — corrected). #724 scope sharpened (commented). Verdict wording approved. **PHASE C COMPLETE; amended PROBE-REPORT (83 lines) + PHASE-C-CLOSEOUT (358 lines) APPROVED 17:04.** Verdict of record: Phase 2 exit criteria PARTLY met — legacy half met; contract half holds on v1.70.2 for single-select gates and questions; not met for the #721 shape (#724 residual behind it). App correct in every row of all phases. C stood down 17:05; phase D planned in PHASE-C-CLOSEOUT §8 (row 7 fail-closed sub-checks + #724 tiers measured separately), rig held. **PHASE D GO 17:27 on v1.70.4** (row 7 proper, #724 tiers, positive controls). **Cell 1 18:03: D10 PASS on 1.70.4** (zero tappable rows, no request, well-formed direct answer → 400 `unsupported_prompt_shape`, zero bytes; Escape sub-check withdrawn = the 60 s self-cancel; keys-vs-input asymmetry = #692 design). **New finding**: multi-question payload order-dependent — 6/11 degraded 1-question scrape, 5/11 correct 2-question transcript; frozen for the prompt's life. Owner traced in source: `handleLiveQuestion` replaces an authoritative transcript prompt without checking provenance (last-writer-wins). Confirmed 11/11 (degraded ⇔ screen/inferred, correct ⇔ transcript/authoritative) → **filed streamer #730 (P2)** 18:12; #721 stays closed. Cells 2 (#724 tiers by producer) and 3 (positive controls) running. **PHASE D COMPLETE; PHASE-D-CLOSEOUT (258) + final PROBE-REPORT (95) APPROVED 18:36 — Phase 2 SIGNED OFF against v1.70.4**: exit criteria met for every shape the app answers; #730 fidelity follow-up (~5.89 s transcript→screen overwrite, answer path works from either producer); #724 explained (screen prompt lives as long as its form; transcript prompt expires at 60 s); third withdrawal (filtered sample) recorded. **GROUP C CLOSED 18:36**: teardown verified (kill signals only, no real-home writes; prod streamer on 8766 healthy; 157-file scrubbed evidence, 3 hash-verified baselines). Simulator it booted was still open → shut down (not erased) 18:39; rule added to tb-mobile CLAUDE.md/AGENTS.md (mobile #896 MERGED aebb4235). Session closable. | 18:36 closed | row 7 result → #724 tiers → PHASE-D-CLOSEOUT + final verdict → E | — |
| D — #701, #702, #870 | `sonnet5-medium` (session closed 16:40) | **#701 MERGED** (PR #706 → `3ec9861a`, all checks green; no release commit — refactor: doesn't bump). **#702 MERGED** (PR #708 → `e71487c8`). Streamer side of D done; B hand-off performed 08:46. #870 (mobile): impl + i18n + eslint + component tests green (TerminalView 30/30 incl. ghost-refusal + falsifiability); **#870 staged diff APPROVED 11:52** (owner read both component diffs + en locale; 2663/2664 full jest, falsifiability done); commit body replaced (mid-sentence wrap, 3rd time); rebased onto #887, suite 2670/2671, commit c7d9c28b, **#888 MERGED → cad72d36, all 10 checks green. GROUP D CLOSED 12:00** (worktrees removed, sub-agents idle). | 12:00 final | — | — |
| E — TestFlight build | `sonnet5-low` | **Run 2 33187519976 SUCCEEDED 19:24: iOS build 210** (v1.0.0, main@020fa9e9, altool delivery 984bdfe9…, ASC state VALID), bump PR #899 MERGED, tag `ios-v210`. #704 client-floor clock started (commented). Run 1 failed on #895's jsi bump (reverted via #897). | 19:26 build 210 | proof deploy of #898 (macos-26 / Xcode 26.4.1) on user go → build 211; then stand down | user go |

## Decisions log

- 20:08 A: probe sessions run under redirected `CLAUDE_CONFIG_DIR` / `CODEX_HOME` with copied credential files (paths only in META, deleted at track end); T7/T9 do not halt the whole Codex track — dependent rows go unknown, the rest continue; 3 attempts / ~45 min per row cap.
- 20:09 C: row 3 provoked with Down-arrow `{ keys }` for the cursor-only repaint; client B is the WS/HTTP script; row 7 asks for `multiSelect: true` explicitly, 3 attempts then "not run".
- 11:48 B/D: #887 merges ahead of #870 (no PR open for #870; disjoint files); D rebases onto it. The "one PR at a time" rule serializes open PRs, not planned ones.
- 12:05 C: `@threadbase-sh/streamer@1.70.0` had written a caret range; 1.70.1 published 2 min after the install. Rig verified 1.70.0 four ways, spec rewritten exact without reinstall. PROBE-REPORT verdict scopes to 1.70.0 explicitly; 1.70.1 (= #703) delta is `promptState` on the 409 body only — listed as the sole delta under 8b.
- 12:20 Owner: phase-B row 2 FAIL is a #700 review miss (post-adapter state re-check was in the reviewed diff). Filed #720 P1; fix assigned to opus5-high; C continues non-blocked rows and reports 4/5(c)/6 as BLOCKED; phase-C re-run on the fixed build decided when it releases. E stays held (no verdict yet).
- 12:41 Owner: evidence taps log their argv incl. the rig `--key`; all copies under tracks/ scrubbed to `tb_REDACTED` (rig-local key, dies with the scratchpad). Rule: scrub taps before any copy leaves the scratchpad.
- 12:48 Owner: streamer boots with `log.info(`API key: ${apiKey}`, { apiKeyMasked })` (cli/index.ts:367) — full key in the JSON log beside the masked field; filed as P2 security. Evidence under tracks/ replaced wholesale from C's `evidence-scrubbed/` mirror (97 files verified clean by C).
- 16:24 C refuses to route approvals to `-40` until the user confirms it as the delegate in its terminal (correct per its rules); phase C continues on the standing GO. Phase-C rig died with the verifier's own usage limit at 11:09Z; restarting on the exact 1.70.2 pin with provenance re-verified and a log-gap note. Cells 1–2 evidence on disk, results pending proper rows.
- 16:30 User confirmed `-40` as delegate in opus5-medium's terminal; B had already accepted. Owner defaults (user silent): #724 accepted as cosmetic for TestFlight, required before App Store; #723 queued for B after #721.
- 17:04 Owner: Phase 2 signed off for single-select paths against v1.70.2; multi-select/unknown held for #721. E fires after #721 releases and C runs row 7 proper on it.
- 17:27 Owner: #723 third bullet (rotate any key already pasted into a shared log) is an operational action for the user — flagged by B; not tracked as code work.
- 18:31 User: no streamer log ever shared → no key rotation; #723 closed.
- 18:57 User-chosen upgrade (after RN 0.87.1 found to need Expo SDK 58 canary): expo ^57.0.18 + react-native 0.86.3 + @react-native/jest-preset ^0.86.3 (SDK 57 pins), drop patches/expo-modules-jsi+57.0.4.patch, deploy.yml + e2e.yml iOS jobs → macos-26 / Xcode 26.4.1. Worktree ../tb-mobile-worktrees/upgrade-expo-57-0-18 (branch chore/expo-57-0-18-macos-26); jest 2671/1 skipped, tsc clean, pods re-resolved; **PR #898 MERGED** (all checks green); a deploy run on the new main is the archive proof — after run 2 finishes, with the user's go.
