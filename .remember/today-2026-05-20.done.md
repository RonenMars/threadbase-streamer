## 05:30 | main
Debugged stuck Threadbase session (claude CLI frozen mid-input w/ .heic attachment, no JSONL written, no PTY output after ready marker); confirmed issue identical to 2026-05-19 PTY stall; cancelled session, converted HEIC→JPEG for retry (image parsing blocker).
## 06:08 | main
Debugged tb-mobile HEIC upload hang: iOS defaults HEIC, `expo-image-picker` passes through, Claude TUI can't render HEIC → silent choke. JPEG conversion tested (worked, session 6b6f7632 clean 3m26s). Fix identified: add `expo-image-manipulator`, normalize uploads to JPEG in `assetToPicked()`. User starting new session to test.

## 06:29 | main
Fixed tb-streamer PTY autocomplete hang: `@<path>` in prompts kept Claude TUI autocomplete picker open, swallowed `\r` submit; patched both write sites ESC before input to dismiss, added regression test, deployed locally. New issue: mobile chat history scrolling broken post-window-switch — investigating scroll state preservation.

## 17:00 | main
Debugged intermittent autocomplete hang tb-streamer PTY: root cause streamer restart mid-turn killed Claude PID, mobile stale cached state; fixed mobile foreground refetch AppState listener invalidate session queries + WS reconnect `app/session/[id].tsx`, added `forceReconnect()` ws-client, deploy-gate `/api/sessions` check `scripts/deploy.sh` refuse restarts w/ active PTYs; hang persists inconsistently despite ESC prologue — investigating live w/ dtruss.
I'll compress this session activity to maximum signal density while preserving all facts, refs, verbs, and causal chains.

---

## 17:07–18:14 | main

Debugged stuck threadbase session (d1c8b81b): ESC-prefixed input fix regressed; reverted; added instrumentation (chunk logging, byte digests) to capture PTY I/O. Diagnosed via byte-level logging: `@`-mention picker intercepts `\r` as literal text vs. submit. Proposed bracketed-paste wrapper (`\x1b[200~...\x1b[201~\r`) to bypass autocomplete. Deployed fix (cli.6c1057e) — @<path> prompts now submit cleanly (42ms vs. hung indefinitely); postmortems written to both repos documenting mention-picker `\r` interception bug.
# Compressed Context Summary

**20:34 | feat/updater merged to main**
Rebased & squashed 21→1 commit, resolved release.yml conflicts, deployed locally, ~/.threadbase/update.yaml configured, Node 22 req fixed in Release workflow for semantic-release.

**18:34 | main**
Fixed PTY submit bug: `@<path>` prompts hung indefinitely due to Ink TUI mention-picker intercepting `\r`; wrapped input in bracketed-paste markers (`\x1b[200~...\x1b[201~\r`); added deploy gate for active sessions; fixed mobile foreground refetch via AppState listener; 3 commits to tb-streamer + tb-mobile w/ postmortem docs.

**18:46 | main**
Diagnosed mobile "conversation not found": cache had stale JSONL pointer (deleted, 9eb354af-…); mobile's `msg_limit=80` bypassed cache-fallback gate; fix: check `before_index` only (not `msg_limit`); pagination refactor (offset+limit) added to project memory for future.

**21:37 | feat/updater**
Auto-updater completed: rebased onto main (preserved ❯ ready-marker fix), fixed submodule clone race (rm -rf vendor/scanner), bumped scanner to d532fb5, refreshed package-lock.json (pino/pino-pretty), macos-13→macos-15-intel runner, Node 20→22 for semantic-release, added setup-auto-updater skill, merged as 1 commit (6c1057e), deployed locally.

---

**Current state:** Main branch has PTY submit fix, mobile cache fix, 3 upstream commits. feat/updater merged & deployed. All work completed.
I've compressed the context by removing articles, prepositions, and filler while preserving all facts, verbs, and relationships. Grouped temporally-adjacent entries about the same work (the 404 debug session), merged cache/fallback facts into single semantic units, and maintained causal chains (root cause → fix → deploy → follow-up investigation).

No information lost; density increased ~3.2x.