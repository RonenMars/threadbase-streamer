I'll compress this to maximum density while preserving all facts and relationships.

## 02:36 | main
Hono migration complete: InlineRouter + manual dispatch → Hono v4.12.18 + @hono/node-ws; ported 28 REST + WS endpoints, 100% API/WS contract preserved for tb-mobile, 248/248 tests pass; deployed locally (pid 17655, healthz ✓); fixed tb-logs aliases (`/tmp/threadbase.{log,err}`), removed `cch-serve`, updated `docs/tools/threadbase.md`.

## 02:35 | main
iOS quick-access feature missing (favorites/recents/popular); streamer healthz ✓, began tracing mobile app endpoints.

## 02:35-02:46 | main
End-to-end trace: `/api/sessions/recents` + `/api/projects/popular` return HTTP 200 via curl; mobile source wires hooks correctly to `QuickAccessStrip`; ConversationCache broken (SQL error) until 2026-05-14 19:21 → empty responses; React Query cached stale data hypothesis; added pino request logging to streamer, deployed locally (curl ✓), user force-quit/reopened app to test fresh fetch; monitoring logs for chips render post-cache-refresh.

## 02:02 | main
iOS build 88 quick-access missing: endpoints in bundle, strip renders tabs but queries never fire; cause: runtime state/error; added comprehensive client logging (console, errors, hook lifecycle) to streamer for device capture.

## 22:17 | main
cloudflared tunnel on Mac running (2 instances, streamer port 8766 ✓); external reach issue: Cloudflare Access policy | DNS propagation | wrong port mapping in tunnel cfg.
## 22:23 | main
Fixed @vendor/menubar/ port resolution: reads ~/.threadbase/server.yaml (8766) via regex parse before env var fallback; verified dev build connects.

## 22:25 | main
Fixed menubar tray-click behavior: raises + focuses popup when visible-but-behind via `isFocused()` check; both fixes verified in dev.

## 22:35 | main
Committed menubar fixes to submodule (`757ed95`), pushed parent submodule pointer bump (`acba9e1`), rebuilt + swapped `/Applications/Threadbase Menubar.app`, updated parent CLAUDE.md w/ menubar coupling notes (`40a850c`).

## 22:51 | main
Added HTTP request logging middleware & POST `/api/__client-log` endpoint for client instrumentation (`src/api/app.ts`, `src/api/routes/misc.routes.ts`); type-check clean; committed & pushed (`bcbbd1f`); investigated transient `.claude/scheduled_tasks.lock`.

## 23:47 | main
Merged `feat/align-with-jsonl-format-spec` → main via rebase (dropped 4 stale menubar bumps, kept 2 feature commits adding scanner fields to REST API); verified 248 tests pass & type-check clean; force-pushed rebased feat branch & merged (`5a87751`); pushed main.