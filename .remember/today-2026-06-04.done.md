I'm Claude, your Agent. I'll help you with software engineering tasks using the tools available to me.

Since you're starting a conversation, let me understand the context: you're in `/private/tmp`, which isn't a git repository. Before I assist with any tasks, I should check what you'd like help with.

What would you like to work on?
# Compressed handoff

## 20:14-22:47 | Session diagnostics & bug analysis

**Stuck session 5d366c33:** Not stuck—legitimately `on_hold` (PTY killed by IdleSweeper, 4h idle); resume available from May-13 conversation (36 msgs). UX bug: mobile labels `on_hold` as `idle` w/o "tap Resume to reattach" clarification.

**Mobile session-resume bug:** Streamer derives `cwd` from encoded dir name instead of JSONL; Claude lookup fails → zombie "No conversation found" error. Fix: read `cwd` from JSONL first line before spawn (mirrors tb-scanner).

**Mobile recents tab bug:** `/api/sessions/recents` synthesizes rows from `ConversationCache`; mobile taps `/api/sessions/:id` with conversation UUIDs not in `SessionStore` → 404. Fix: return `{ conversations: [...] }` or route via `POST /api/sessions/resume`.

**iOS Simulator crash:** TurboModule deadlock during Expo reload (main thread blocks on turbo-module queue); issue in tb-mobile repo, not streamer/menubar.

**Streamer resume path bug:** Session lookup returns wrong `projectPath` (android instead of tb-mobile); `claude --resume` spawns in wrong dir → exit. Mobile correctly sends `{ sessionId }`; bug streamer-side in `handleResume`.

**Double-push in resume:** Redundant global `session_ready` listener in `_layout.tsx:121-128` pushes for every session start/resume despite all 7 entry points already navigating. Proposed: delete listener push (~3 LOC) instead of add suppress-Map; awaiting user direction.

**Merged:** PRs #20 (homebrew docs), #21 (milestone-b release notes) to main; dropped 4 untracked files.

---

**Next:** User direction on double-push fix approach; then address resume bugs (cwd JSONL read, recents 404, path resolution).
I'll compress this with maximum information density, preserving all facts and relationships.

## 23:05 | main
`/api/sessions/resume` derive `cwd` from JSONL not stale scanner index; regression test added; deployed prod cherry-pick.

## 23:19 | main
"No conversation found" bug: `/api/sessions/resume` read `projectPath` from stale scanner index vs JSONL's authoritative `cwd`; streamer fix: read `cwd` from JSONL (mirrors tb-scanner).

## 23:51 | main
conversation-cache security: `upsertFromScannerMeta()` return upserted IDs prevent FK violation warming agent-filtered conversation tails; regression test agent JSONLs excluded; warm-up loop populate only upserted IDs.