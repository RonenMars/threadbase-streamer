I'll compress this handoff with maximum information density.

## 06:26 | main
Fixed `/api/sessions/resume`: derive `cwd` from JSONL not stale scanner; added regression test; deployed PR #22; verified fix resolves scanner projectPath bug.

## 06:28-07:11 | fix/recents-session-not-found
Diagnosed: `/api/sessions/recents` wraps conversation UUIDs as session objects → mobile "Session not found". Impl: `type: "conversation"` discriminator on `/recents` items + `handleGetSession` fallback to `ConversationCache` for older builds. Live-tested—previously-broken UUID `a4ed8420-…` now 200 w/ `on_hold` status.

## 11:02 | main
Recents regression: `/api/sessions/recents` returns cached conversations as PTY sessions → mobile shows "No terminal output" on conversation history. Backlog: unified migration to `/project-chats`; in-progress: tb-mobile fix routing by `ProjectChat.type` discriminator.