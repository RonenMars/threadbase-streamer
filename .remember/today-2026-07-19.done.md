I'll apply maximum compression to this session log.

## 00:11 | main
`lint-changed.sh` auto-detects main repo vs `.worktrees` dispatch + pre-commit hook (PR #229); extended w/ `test:smoke` target (6 files, ~4s, #231 stacked); added VCS config to `biome.json`; wired hook setup via `npm prepare`; fixed stale `better-sqlite3` ABI mismatch in root `node_modules`.