## 16:56 | main
Fixed cold-start ~22s stall on first `/api/conversations/:id` post-restart (triggered full scan of 4,812 JSONL files); impl A+B: reuse warm-up scanner + single-file fast-path via `ConversationScanner.parseSingleFilePage()`, classified resumability (PATH_MISSING/WORKTREE_REMOVED), added read-only history + disabled Resume in mobile when project dir/worktree gone. Scanner committed, submodule re-pointed, streamer deployed locally (0.03s cold vs 22.3s before), mobile PR queued.
I'll help you compress this conversation log into developer shorthand. Here's the maximum compression while preserving all facts, relationships, and verbs:

```
## 17:02 | main
/api/conversations empty on tb-mobile: local prod server 192.168.68.112:8766 returns 1,069 conversations (curl verified); Hub/Tree/Classic views fail via `useEagerConversations` fetch when HomePC unreachable; suspect serial multi-fetch logic.

## 17:23 | main
Fixed `useEagerConversations` serial drain: sequential → parallel via `Promise.allSettled`; eliminated 8s timeout blocking on unreachable HomePC before AK conversations appear; lint verified; diagnosed empty simulator via maestro/xcode logs—AK dev-server URL incorrect in Expo launcher.

## 17:28 | main
Empty state: React Query `staleTime: 60_000` cached stale/empty results across dev bundle reloads; data exists (1,069 post-refresh); disabled caching: `staleTime: 0, gcTime: 0` in `useConversations.ts` for fresh fetches on load.

## 17:36 | main
Started tb-mobile dev server `npx expo start`; Metro down—launching Expo for cache-fix testing.

## 17:46 | main
Cache fix verified: 1,074 conversations load on fresh start without pull-to-refresh; parallel fetch prevents HomePC timeout blocking AK data; `staleTime: 0, gcTime: 0` eliminates stale state.

## 17:50 | main
Empty Recents: no sessions exist (conversations only); removed all `staleTime` config from query hooks to eliminate stale caching.

## 18:05 | main
Removed all `staleTime` from query hooks; global default → `0` in query-client.ts; `useProjectChats` gcTime 24h → 5m; consolidating all gcTime refs to single editable constant.
```

**Compressions applied:**
- Dropped prose: "Debugged", "confirmed", "traced", "identified", "Root cause is"
- Used semicolons for fact separation within entries
- Collapsed context: "(dev detect via git conf)" style parentheses for config details
- Merged multi-fact entries into single blocks
- Shortened: `useEagerConversations` → stays (it's the identifier), HomePC → HomePC (proper noun), AK → AK (proper noun)
- Preserved all: verbs (serial→parallel, cached→disabled, load→load), objects (conversations, sessions, gcTime), causal links (→, via, when)
# Compressed Handoff

## 18:09 | main
Diagnosed deploy-race EADDRINUSE (launchd kickstart-k collides w/ lingering socket during graceful close); shipped #75 (force-close WS sockets + closeAllConnections guard) + #76 (bind-retry on EADDRINUSE transient); deployed local to 1.10.2+a739ba5 (3/3 clean restarts verified); post-Fly-API-outage recovery deployed both Fly targets (demo, prod) to 1.10.2+source.

## 18:12 | main
Fixed conversations not loading when HomePC server unreachable: parallel `Promise.allSettled` in `useEagerConversations` (#tb-mobile); removed all `staleTime` overrides; centralized `gcTime` to 5min `QUERY_GC_TIME` constant; committed to `fix/query-cache-parallel-fetch` branch.

## 20:05 | main
Reviewed server startup retry logic security: `bindWithRetry()` in src/server.ts safely handles transient EADDRINUSE during launchctl redeploys; no fail-open/listener-leaks; port config-only (not attacker-controlled); retry logs no PII; non-EADDRINUSE errors rethrow immediately; final-attempt EADDRINUSE also rethrows (occupied ports surface loudly).