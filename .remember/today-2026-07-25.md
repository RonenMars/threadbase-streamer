# Context Compression

## 01:05–02:01 | fix/osc-777-conflation-and-status-line
**Session lifecycle & statusline**
- Investigated streamer `hold_session` SIGINT & mobile backgrounding; changed SIGINT to full grace timer
- Fixed `detectShellPrompt` false-positive on numbered lists
- Widened permission-mode union across Claude CLI
- Exposed `model`/`effort`/`permissionMode` on `GET /api/sessions/:id`
- Fixed end-of-turn OSC 777 misclassification (11 stranded sessions since Jul 7); root: `hasWaitingForInputOsc` regex anchor
- Mobile: filtered frozen counter & completed-turn timers from terminal; added thinking skeleton for silent-think
- **PRs:** streamer #273–275, mobile #389–391; cherry-picked to integration/missing-prs-2026-07-23 & integration-merge-354-355-376
- **Deploy:** streamer prod rolled out (1169/1169 tests); zero live PTY sessions at deploy time

## 20:27–01:40 | feat/per-server-claude-flags
**Allowlist + bypass-mode with biometric gate**
- Designed & implemented per-server Claude CLI flags (six curated flags + escape-hatch; `bypassPermissions`, `maxBudgetUsd`, etc)
- Widened `--permission-mode` to all six CLI values
- Added `PUT /api/config/claude-flags` (localNoAuth-gated)
- Verified `skipDangerousModePermissionPrompt` suppresses v2.1.x boot warning via `--settings` JSON
- Hardened mobile Q-card detector (contiguous numbered-block requirement; regression tested)
- Mobile UI: biometric-gated confirmation component + React Query hook + i18n all 4 locales
- Extended `/api/info` for mobile discovery
- **Test coverage:** streamer 1155/1155 green; mobile 18/18 green
- **PRs:** streamer #276 + mobile #392 against integration branches; cherry-picked to integration snapshots
- **Tag:** test-dev/v1.0.0-f8147ad-2026-07-24