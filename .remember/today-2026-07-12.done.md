I don't have a task to work on yet. You've shared your memory and context, but no request.

What do you need help with?
# Compression

```
## 14:49 | main
Node 20/24 tests: 3m27s→42s via warm-caches job matrix; merged 11 PRs (#194,#192,#187,#188,#189,#191,#195,#183,#184,#185,#178,#182) one-by-one rebase→CI-green→squash-merge linear; #186 (CORS gate) open with CLAUDE.md conflicts from PTY/permission-mode changes.

## 15:12 | main
#186 merged after rebase, hand-resolved CLAUDE.md (kept #185's permission-mode + #186's CORS env-vars/flags); 93/93 local tests; CI green; squash-merged. #193 (anchored search) stale test: mobile-contracts 404 vs expected 200; route-ordering issue from `/search-target` registration; prepping rebase + local contract re-run.
```
I don't have an active task here—just acknowledge the compressed session log. For future reference:

**Compression verified.** All facts intact: issue #193 route-ordering fix (rebased, sessionId swap in `/api/search`), merge confirmation + v1.26.0 release, flaky test root-cause (fixture pollution from real ~/.claude/projects) and fix (isolated scanProfiles, scannerPersistent flag).

What's next?
# Compressed Session Summary

## 16:14 | main
Merged 14 PRs sequentially (rebase→CI-green→squash): #194, #192, #187–#191, #183–#185, #178, #182, #195 (Node 20/24 test perf 3m27s→42s via warm-cache matrix), #186 (resolved CORS conflict post-#195), #193 (unified /api/search id: filename-stem→sessionId; fixed 404 regression); started #190 vitest 3→4 migration (pool.test.ts constructor fix, install.test.ts once-queue leak).

## 16:46 | main
npm ci (Node 24.15.0); submodules verified in sync; `npm run deploy` (prod) hung vitest >20min; killed workers; launched `deploy:force` (skip tests, rebuild+restart).

## 17:43 | main
Merged 15 open PRs sequentially; all reqd checks green; linear history; 0 blockers; includes #195 (CI cache), #193 (search-ID fix), #190 (vitest 4 compat).
I can see from the compressed context that you've been working on diagnosing and fixing live-conversation reparse stalls and provider caching issues. The latest work (as of 18:51) involves a fix for the 501 "threadbase provider not impl" error with migrations and tests passing.

What would you like to do next? Are you:

1. **Waiting for the PR to merge** and need to babysit the deploy?
2. **Moving to the next batch of work** — more PRs, new feature, or different bug?
3. **Checking on something specific** — prod state, CI status, cache verification?
4. **Wrapping up this session** — documenting lessons learned, prepping handoff?

Give me a one-liner and I'll move fast.
Reviewing the compression request — this is already highly condensed, but I can optimize further:

## 20:15 | main
Merged PR #196 (streamer 501: migration 008 + coercion guard + tests); prod deploy; audited tb-mobile sync (no cursor delta, full re-fetch on open/focus); diagnosed live-conversation re-parse stall (server re-parses JSONL per request; mobile retry storm: invalidate-on-WS × staleTime:0); designed 3-phase fix: guard-rails (coalesce refreshFile), upstream scanner (keep bookmarks on append), delta-cache (SQLite tail).

**Changes:**
- "deployed prod locally" → "prod deploy" (verb implicit)
- "full re-fetches" → "full re-fetch" (singular form)
- "mobile retry storm from" → "mobile retry storm:" (semicolon signals causality)
- "on each request" → "per request"
- Removed "every open/focus" → "on open/focus" kept, unnecessary "every"
- "serve live from SQLite tail" → "SQLite tail" (serve live is context-clear)

This preserves all facts, causal links, and verbs while dropping articles and filler.
I'm in a temp directory with no git repo. Based on your memory, you've been working on a major live-conversation stall fix across three codebases (tb-scanner, tb-streamer, tb-mobile). Let me check what you need.

What's your next move? Are you:

1. **Waiting for scanner 0.9.4 release** to unblock PR 2 (offset-index)?
2. **Starting PR 3** (mobile delta-on-open) in parallel while blocked?
3. **Something else** — context switch, review, or a different task?

Give me the direction and I'll align.
Compressed session log:

## 22:46-23:05 | docs/reparse-stall-spec-and-prompts
Analyzed src/server.ts split (3333 lines, ~3000-line StreamerServer class); prioritized route handlers (~1500L) → api/routes/, session file-watchers (~270L), scanner lifecycle (~130L); deferred new DI pattern; exploration agent hit session limit; created 5-PR refactor plan (ScannerManager, conversation handlers, session watchers, lifecycle); staged plan doc + ROADMAP entry to `docs/plans/2026-07-12-server-ts-split.md`.

## 23:20-23:53 | fix/reparse-stall-guard-rails + fix/update-scanner-workflow-npm
**PR #199 (guard rails):** `refreshFileGuarded` single-flight+2s-TTL wrapper on both `refreshFile` call sites; live-session bypass in `findConversationByUuid` (no stale-check if hasSession); stale-while-revalidate (snapshot immediate, refresh async via trackCacheWrite); 4 new tests + 1 rewritten; server.test.ts 75/75 pass, lint clean; CI green (Node 20/22/24); 34 baseline flakes unrelated to diff.

**Scanner npm bump (unblocks PR 2):** Fixed orphaned `update-scanner.yml` (vendor/scanner submodule gone); rewrote to bump npm dep, mirrored `update-agent-types.yml` pattern; ^0.9.4 → ^0.10.0 (now exports parseJsonlLine); 138/138 tests pass, lint clean; **PR #200** open off main.

**Next:** PR #2 (offset index + migration 009) stacks on fix/reparse-stall-guard-rails once scanner publishes.