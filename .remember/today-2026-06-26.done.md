## 12:24 | main
Menubar healthz filtering (tb-menu PR #8 + tb-streamer PR #131): tb-menu sends `X-Client: menubar` on healthz polls; tb-streamer filters these logs by default, `--log-menubar-requests` flag opts in.
Ponytail mode active — compressing to raw signal, chronological order, grouped by subject.

## 12:31 | main
Stopped dev streamer, pulled main, redeployed prod (v1.18.0); fixed stale `prod-suspended.json` marker blocking startup; identified deploy script should validate devPid liveness before honoring userHeld flag.

## 12:36 | main
Rebased feat/projects-api-paginated on main; diagnosed CI failure (`/project-chats` test hitting 404 from stale cache); removed test block from codex-api.test.ts; committed & pushed.

## 12:39–13:28 | main → docs/better-sqlite3-node-version-mismatch
Stopped dev streamer (PID 7805), pulled main, redeployed prod; diagnosed root cause (userHeld check ignoring dead PIDs); fixed launchd-entry.ts to validate devPid liveness before honoring userHeld; added test coverage for dead-dev case; committed to PR #133.