## 02:24 | feat/menubar-healthz-header + feat/menubar-healthz-filter
Added `X-Client: menubar` header to tb-menu healthz requests; tb-streamer: added `logMenubarRequests` flag to ServerConfig/ApiDeps + HTTP logger filtering menubar requests (logged only when flag enabled); PR #131 (streamer), PR #8 (menu).
I'm starting fresh in an empty temp directory with no git repo. Let me check what you need.

What task would you like help with? I'm ready to assist with code, debugging, file operations, or anything else.