I see you've provided compressed session notes. Since there's no active task or question, I'm reading this as context about recent work on a tb-streamer project involving:

- WS-first terminal replay feature (server-side session management, mobile HTTP fallback)
- Menubar auto-deploy logic across Unix/Windows (SHA-based staleness checks)
- Session creation hang fixes (UUID generation, `session_ready` timing)
- Documentation gaps (README architecture, CLAUDE.md modules/env vars)

Is there a new task you'd like help with, or are you sharing context for continuity? If you have work in progress or need to pick up where you left off, let me know what you'd like to do next.
## 21:00 | main
Menubar version-check & auto-deploy (deploy.sh/deploy-linux.sh/deploy.ps1): compare vendor/menubar/dist/.build-sha vs submodule HEAD; rebuild & relaunch if stale. Fixed predeploy dirty-check: ignore untracked files in git diff (not porcelain). Commits 4a50416, 51399b3, bd29705.